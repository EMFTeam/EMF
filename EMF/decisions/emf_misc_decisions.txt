

targetted_decisions = {
	emf_bring_child_home = {
		is_high_prio = yes
		
		filter = dynasty
		ai_target_filter = self
		
		from_potential = {
			ai = no
		}
		potential = {
			is_child_of = FROM
			liege = { not = { character = FROM } }
			not = {
				prisoner = yes
				is_ruler = yes
				is_adult = yes
				is_married = yes
			}
		}
		allow = {
			liege = {
				or = {
					ai = yes
					lower_tier_than = FROM
				}
			}
		}
		effect = {
			move_character = FROM
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 0
		}
	}
	
	emf_court_banishment = {
		filter = court
		ai_target_filter = self
		
		from_potential = {
			ai = no
		}
		potential = {
			host = { character = FROM }
			prisoner = no
			is_councillor = no
			not = {
				is_close_relative = FROM
				is_married = FROM
				is_consort = FROM
				dynasty = FROM
			}
		}
		allow = {
			FROM = {
				prestige = 50
				has_regent = no
			}
		}
		effect = {
			custom_tooltip = { text = emf_court_banishment_tt }
			hidden_tooltip = {
				FROM = {
					if = {
						limit = {
							any_realm_lord = { dynasty = ROOT }
						}
						random_realm_lord = {
							limit = { dynasty = ROOT }
							ROOT = {
								move_character = PREV
							}
						}
						break = yes
					}
					random_realm_lord = {
						limit = { is_playable = yes }
						ROOT = {
							move_character = PREV
						}
					}
				}
			}
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 0
		}
	}
}
