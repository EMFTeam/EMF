decisions = {

	violate_baqt = {
		potential = {
			ai = no
			NOT = { has_global_flag = violated_baqt }
			independent = yes
			any_realm_province = {
				has_province_modifier = baqt
			}
			OR = {
				religion = miaphysite
				AND = {
					religion = orthodox
					culture_group = east_african
				}
				religion_group = muslim
			}
		}
		allow = {
			OR = {
				trait = ambitious
				trait = zealous
				AND = {
					NOT = { trait = content }
					NOT = { trait = slothful }
					NOT = { trait = stressed }
					NOT = { trait = cynical }
				}
			}
		}
		effect = {
			set_global_flag = violated_baqt
			any_realm_province = {
				remove_province_modifier = baqt
			}
			hidden_tooltip = {
				any_independent_ruler = {
					any_realm_province = {
						remove_province_modifier = baqt
					}
				}
			}
		}
	}
}

dynasty_decisions = {

	#Bring player's children home (political marriages + misc. cases, esp. startup)
	bring_child_home = {
		is_high_prio = yes
		
		potential = {
			has_global_flag = project_balance
			had_global_flag = { flag = project_balance days = 1 }
			FROM = { ai = no }
			is_child_of = FROM
			liege = { NOT = { character = FROM } }
			NOT = {
				prisoner = yes
				is_ruler = yes
				is_adult = yes
				is_married = yes
			}
		}
		allow = {
			is_child_of = FROM
			NOT = {
				prisoner = yes
				is_ruler = yes
				is_adult = yes
				is_married = yes
			}
			# ^-- all previous allow triggers only present for informational purposes
			liege = {
				OR = {
					# can't yank kids from another player unless you outrank the player
					ai = yes
					lower_tier_than = FROM
				}
			}
		}
		effect = {
			custom_tooltip = {
				text = bring_child_home_ctt
				hidden_tooltip = {
					banish = FROM
				}
			}
		}
		ai_will_do = {
			factor = 0
		}
	}
}