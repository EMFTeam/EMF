# -*- ck2.scripted_effects -*-
# Written by zijistark via trait-notify.pl v1.0 on Mon Jul 11 07:33:56 2016 (Pacific)


#########################################
#  TRAIT NOTIFICATION SCRIPTED EFFECTS  #
#########################################


#### [1] Amateurish Plotter: amateurish_plotter ####
emf_notify_add_amateurish_plotter_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = amateurish_plotter }
			log = "WARNING: emf_notify_add_amateurish_plotter_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (amateurish_plotter)!"
		}
	}
	if = {
		limit = { NOT = { trait = amateurish_plotter } }
		add_trait = amateurish_plotter
		hidden_tooltip = { character_event = { id = emf_trait.1 } }
	}
}
emf_notify_remove_amateurish_plotter_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = amateurish_plotter } }
			log = "WARNING: emf_notify_remove_amateurish_plotter_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (amateurish_plotter)!"
		}
	}
	if = {
		limit = { trait = amateurish_plotter }
		remove_trait = amateurish_plotter
		hidden_tooltip = { character_event = { id = emf_trait.1001 } }
	}
}

#### [2] Flamboyant Schemer: flamboyant_schemer ####
emf_notify_add_flamboyant_schemer_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = flamboyant_schemer }
			log = "WARNING: emf_notify_add_flamboyant_schemer_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (flamboyant_schemer)!"
		}
	}
	if = {
		limit = { NOT = { trait = flamboyant_schemer } }
		add_trait = flamboyant_schemer
		hidden_tooltip = { character_event = { id = emf_trait.2 } }
	}
}
emf_notify_remove_flamboyant_schemer_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = flamboyant_schemer } }
			log = "WARNING: emf_notify_remove_flamboyant_schemer_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (flamboyant_schemer)!"
		}
	}
	if = {
		limit = { trait = flamboyant_schemer }
		remove_trait = flamboyant_schemer
		hidden_tooltip = { character_event = { id = emf_trait.1002 } }
	}
}

#### [3] Intricate Webweaver: intricate_webweaver ####
emf_notify_add_intricate_webweaver_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = intricate_webweaver }
			log = "WARNING: emf_notify_add_intricate_webweaver_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (intricate_webweaver)!"
		}
	}
	if = {
		limit = { NOT = { trait = intricate_webweaver } }
		add_trait = intricate_webweaver
		hidden_tooltip = { character_event = { id = emf_trait.3 } }
	}
}
emf_notify_remove_intricate_webweaver_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = intricate_webweaver } }
			log = "WARNING: emf_notify_remove_intricate_webweaver_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (intricate_webweaver)!"
		}
	}
	if = {
		limit = { trait = intricate_webweaver }
		remove_trait = intricate_webweaver
		hidden_tooltip = { character_event = { id = emf_trait.1003 } }
	}
}

#### [4] Elusive Shadow: elusive_shadow ####
emf_notify_add_elusive_shadow_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = elusive_shadow }
			log = "WARNING: emf_notify_add_elusive_shadow_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (elusive_shadow)!"
		}
	}
	if = {
		limit = { NOT = { trait = elusive_shadow } }
		add_trait = elusive_shadow
		hidden_tooltip = { character_event = { id = emf_trait.4 } }
	}
}
emf_notify_remove_elusive_shadow_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = elusive_shadow } }
			log = "WARNING: emf_notify_remove_elusive_shadow_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (elusive_shadow)!"
		}
	}
	if = {
		limit = { trait = elusive_shadow }
		remove_trait = elusive_shadow
		hidden_tooltip = { character_event = { id = emf_trait.1004 } }
	}
}

#### [5] Naive Appeaser: naive_appeaser ####
emf_notify_add_naive_appeaser_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = naive_appeaser }
			log = "WARNING: emf_notify_add_naive_appeaser_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (naive_appeaser)!"
		}
	}
	if = {
		limit = { NOT = { trait = naive_appeaser } }
		add_trait = naive_appeaser
		hidden_tooltip = { character_event = { id = emf_trait.5 } }
	}
}
emf_notify_remove_naive_appeaser_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = naive_appeaser } }
			log = "WARNING: emf_notify_remove_naive_appeaser_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (naive_appeaser)!"
		}
	}
	if = {
		limit = { trait = naive_appeaser }
		remove_trait = naive_appeaser
		hidden_tooltip = { character_event = { id = emf_trait.1005 } }
	}
}

#### [6] Underhanded Rogue: underhanded_rogue ####
emf_notify_add_underhanded_rogue_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = underhanded_rogue }
			log = "WARNING: emf_notify_add_underhanded_rogue_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (underhanded_rogue)!"
		}
	}
	if = {
		limit = { NOT = { trait = underhanded_rogue } }
		add_trait = underhanded_rogue
		hidden_tooltip = { character_event = { id = emf_trait.6 } }
	}
}
emf_notify_remove_underhanded_rogue_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = underhanded_rogue } }
			log = "WARNING: emf_notify_remove_underhanded_rogue_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (underhanded_rogue)!"
		}
	}
	if = {
		limit = { trait = underhanded_rogue }
		remove_trait = underhanded_rogue
		hidden_tooltip = { character_event = { id = emf_trait.1006 } }
	}
}

#### [7] Charismatic Negotiator: charismatic_negotiator ####
emf_notify_add_charismatic_negotiator_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = charismatic_negotiator }
			log = "WARNING: emf_notify_add_charismatic_negotiator_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (charismatic_negotiator)!"
		}
	}
	if = {
		limit = { NOT = { trait = charismatic_negotiator } }
		add_trait = charismatic_negotiator
		hidden_tooltip = { character_event = { id = emf_trait.7 } }
	}
}
emf_notify_remove_charismatic_negotiator_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = charismatic_negotiator } }
			log = "WARNING: emf_notify_remove_charismatic_negotiator_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (charismatic_negotiator)!"
		}
	}
	if = {
		limit = { trait = charismatic_negotiator }
		remove_trait = charismatic_negotiator
		hidden_tooltip = { character_event = { id = emf_trait.1007 } }
	}
}

#### [8] Grey Eminence: grey_eminence ####
emf_notify_add_grey_eminence_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = grey_eminence }
			log = "WARNING: emf_notify_add_grey_eminence_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (grey_eminence)!"
		}
	}
	if = {
		limit = { NOT = { trait = grey_eminence } }
		add_trait = grey_eminence
		hidden_tooltip = { character_event = { id = emf_trait.8 } }
	}
}
emf_notify_remove_grey_eminence_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = grey_eminence } }
			log = "WARNING: emf_notify_remove_grey_eminence_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (grey_eminence)!"
		}
	}
	if = {
		limit = { trait = grey_eminence }
		remove_trait = grey_eminence
		hidden_tooltip = { character_event = { id = emf_trait.1008 } }
	}
}

#### [9] Indulgent Wastrel: indulgent_wastrel ####
emf_notify_add_indulgent_wastrel_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = indulgent_wastrel }
			log = "WARNING: emf_notify_add_indulgent_wastrel_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (indulgent_wastrel)!"
		}
	}
	if = {
		limit = { NOT = { trait = indulgent_wastrel } }
		add_trait = indulgent_wastrel
		hidden_tooltip = { character_event = { id = emf_trait.9 } }
	}
}
emf_notify_remove_indulgent_wastrel_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = indulgent_wastrel } }
			log = "WARNING: emf_notify_remove_indulgent_wastrel_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (indulgent_wastrel)!"
		}
	}
	if = {
		limit = { trait = indulgent_wastrel }
		remove_trait = indulgent_wastrel
		hidden_tooltip = { character_event = { id = emf_trait.1009 } }
	}
}

#### [10] Thrifty Clerk: thrifty_clerk ####
emf_notify_add_thrifty_clerk_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = thrifty_clerk }
			log = "WARNING: emf_notify_add_thrifty_clerk_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (thrifty_clerk)!"
		}
	}
	if = {
		limit = { NOT = { trait = thrifty_clerk } }
		add_trait = thrifty_clerk
		hidden_tooltip = { character_event = { id = emf_trait.10 } }
	}
}
emf_notify_remove_thrifty_clerk_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = thrifty_clerk } }
			log = "WARNING: emf_notify_remove_thrifty_clerk_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (thrifty_clerk)!"
		}
	}
	if = {
		limit = { trait = thrifty_clerk }
		remove_trait = thrifty_clerk
		hidden_tooltip = { character_event = { id = emf_trait.1010 } }
	}
}

#### [11] Fortune Builder: fortune_builder ####
emf_notify_add_fortune_builder_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = fortune_builder }
			log = "WARNING: emf_notify_add_fortune_builder_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (fortune_builder)!"
		}
	}
	if = {
		limit = { NOT = { trait = fortune_builder } }
		add_trait = fortune_builder
		hidden_tooltip = { character_event = { id = emf_trait.11 } }
	}
}
emf_notify_remove_fortune_builder_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = fortune_builder } }
			log = "WARNING: emf_notify_remove_fortune_builder_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (fortune_builder)!"
		}
	}
	if = {
		limit = { trait = fortune_builder }
		remove_trait = fortune_builder
		hidden_tooltip = { character_event = { id = emf_trait.1011 } }
	}
}

#### [12] Midas Touched: midas_touched ####
emf_notify_add_midas_touched_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = midas_touched }
			log = "WARNING: emf_notify_add_midas_touched_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (midas_touched)!"
		}
	}
	if = {
		limit = { NOT = { trait = midas_touched } }
		add_trait = midas_touched
		hidden_tooltip = { character_event = { id = emf_trait.12 } }
	}
}
emf_notify_remove_midas_touched_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = midas_touched } }
			log = "WARNING: emf_notify_remove_midas_touched_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (midas_touched)!"
		}
	}
	if = {
		limit = { trait = midas_touched }
		remove_trait = midas_touched
		hidden_tooltip = { character_event = { id = emf_trait.1012 } }
	}
}

#### [13] Misguided Warrior: misguided_warrior ####
emf_notify_add_misguided_warrior_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = misguided_warrior }
			log = "WARNING: emf_notify_add_misguided_warrior_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (misguided_warrior)!"
		}
	}
	if = {
		limit = { NOT = { trait = misguided_warrior } }
		add_trait = misguided_warrior
		hidden_tooltip = { character_event = { id = emf_trait.13 } }
	}
}
emf_notify_remove_misguided_warrior_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = misguided_warrior } }
			log = "WARNING: emf_notify_remove_misguided_warrior_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (misguided_warrior)!"
		}
	}
	if = {
		limit = { trait = misguided_warrior }
		remove_trait = misguided_warrior
		hidden_tooltip = { character_event = { id = emf_trait.1013 } }
	}
}

#### [14] Tough Soldier: tough_soldier ####
emf_notify_add_tough_soldier_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = tough_soldier }
			log = "WARNING: emf_notify_add_tough_soldier_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (tough_soldier)!"
		}
	}
	if = {
		limit = { NOT = { trait = tough_soldier } }
		add_trait = tough_soldier
		hidden_tooltip = { character_event = { id = emf_trait.14 } }
	}
}
emf_notify_remove_tough_soldier_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = tough_soldier } }
			log = "WARNING: emf_notify_remove_tough_soldier_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (tough_soldier)!"
		}
	}
	if = {
		limit = { trait = tough_soldier }
		remove_trait = tough_soldier
		hidden_tooltip = { character_event = { id = emf_trait.1014 } }
	}
}

#### [15] Skilled Tactician: skilled_tactician ####
emf_notify_add_skilled_tactician_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = skilled_tactician }
			log = "WARNING: emf_notify_add_skilled_tactician_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (skilled_tactician)!"
		}
	}
	if = {
		limit = { NOT = { trait = skilled_tactician } }
		add_trait = skilled_tactician
		hidden_tooltip = { character_event = { id = emf_trait.15 } }
	}
}
emf_notify_remove_skilled_tactician_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = skilled_tactician } }
			log = "WARNING: emf_notify_remove_skilled_tactician_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (skilled_tactician)!"
		}
	}
	if = {
		limit = { trait = skilled_tactician }
		remove_trait = skilled_tactician
		hidden_tooltip = { character_event = { id = emf_trait.1015 } }
	}
}

#### [16] Brilliant Strategist: brilliant_strategist ####
emf_notify_add_brilliant_strategist_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = brilliant_strategist }
			log = "WARNING: emf_notify_add_brilliant_strategist_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (brilliant_strategist)!"
		}
	}
	if = {
		limit = { NOT = { trait = brilliant_strategist } }
		add_trait = brilliant_strategist
		hidden_tooltip = { character_event = { id = emf_trait.16 } }
	}
}
emf_notify_remove_brilliant_strategist_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = brilliant_strategist } }
			log = "WARNING: emf_notify_remove_brilliant_strategist_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (brilliant_strategist)!"
		}
	}
	if = {
		limit = { trait = brilliant_strategist }
		remove_trait = brilliant_strategist
		hidden_tooltip = { character_event = { id = emf_trait.1016 } }
	}
}

#### [17] Detached Priest: detached_priest ####
emf_notify_add_detached_priest_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = detached_priest }
			log = "WARNING: emf_notify_add_detached_priest_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (detached_priest)!"
		}
	}
	if = {
		limit = { NOT = { trait = detached_priest } }
		add_trait = detached_priest
		hidden_tooltip = { character_event = { id = emf_trait.17 } }
	}
}
emf_notify_remove_detached_priest_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = detached_priest } }
			log = "WARNING: emf_notify_remove_detached_priest_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (detached_priest)!"
		}
	}
	if = {
		limit = { trait = detached_priest }
		remove_trait = detached_priest
		hidden_tooltip = { character_event = { id = emf_trait.1017 } }
	}
}

#### [18] Dutiful Cleric: martial_cleric ####
emf_notify_add_martial_cleric_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = martial_cleric }
			log = "WARNING: emf_notify_add_martial_cleric_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (martial_cleric)!"
		}
	}
	if = {
		limit = { NOT = { trait = martial_cleric } }
		add_trait = martial_cleric
		hidden_tooltip = { character_event = { id = emf_trait.18 } }
	}
}
emf_notify_remove_martial_cleric_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = martial_cleric } }
			log = "WARNING: emf_notify_remove_martial_cleric_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (martial_cleric)!"
		}
	}
	if = {
		limit = { trait = martial_cleric }
		remove_trait = martial_cleric
		hidden_tooltip = { character_event = { id = emf_trait.1018 } }
	}
}

#### [19] Scholarly Theologian: scholarly_theologian ####
emf_notify_add_scholarly_theologian_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = scholarly_theologian }
			log = "WARNING: emf_notify_add_scholarly_theologian_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (scholarly_theologian)!"
		}
	}
	if = {
		limit = { NOT = { trait = scholarly_theologian } }
		add_trait = scholarly_theologian
		hidden_tooltip = { character_event = { id = emf_trait.19 } }
	}
}
emf_notify_remove_scholarly_theologian_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = scholarly_theologian } }
			log = "WARNING: emf_notify_remove_scholarly_theologian_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (scholarly_theologian)!"
		}
	}
	if = {
		limit = { trait = scholarly_theologian }
		remove_trait = scholarly_theologian
		hidden_tooltip = { character_event = { id = emf_trait.1019 } }
	}
}

#### [20] Mastermind Theologian: mastermind_theologian ####
emf_notify_add_mastermind_theologian_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = mastermind_theologian }
			log = "WARNING: emf_notify_add_mastermind_theologian_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (mastermind_theologian)!"
		}
	}
	if = {
		limit = { NOT = { trait = mastermind_theologian } }
		add_trait = mastermind_theologian
		hidden_tooltip = { character_event = { id = emf_trait.20 } }
	}
}
emf_notify_remove_mastermind_theologian_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = mastermind_theologian } }
			log = "WARNING: emf_notify_remove_mastermind_theologian_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (mastermind_theologian)!"
		}
	}
	if = {
		limit = { trait = mastermind_theologian }
		remove_trait = mastermind_theologian
		hidden_tooltip = { character_event = { id = emf_trait.1020 } }
	}
}

#### [21] Stressed: stressed ####
emf_notify_add_stressed_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = stressed }
			log = "WARNING: emf_notify_add_stressed_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (stressed)!"
		}
	}
	if = {
		limit = { NOT = { trait = stressed } }
		add_trait = stressed
		hidden_tooltip = { character_event = { id = emf_trait.21 } }
	}
}
emf_notify_remove_stressed_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = stressed } }
			log = "WARNING: emf_notify_remove_stressed_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (stressed)!"
		}
	}
	if = {
		limit = { trait = stressed }
		remove_trait = stressed
		hidden_tooltip = { character_event = { id = emf_trait.1021 } }
	}
}

#### [22] Depressed: depressed ####
emf_notify_add_depressed_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = depressed }
			log = "WARNING: emf_notify_add_depressed_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (depressed)!"
		}
	}
	if = {
		limit = { NOT = { trait = depressed } }
		add_trait = depressed
		hidden_tooltip = { character_event = { id = emf_trait.22 } }
	}
}
emf_notify_remove_depressed_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = depressed } }
			log = "WARNING: emf_notify_remove_depressed_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (depressed)!"
		}
	}
	if = {
		limit = { trait = depressed }
		remove_trait = depressed
		hidden_tooltip = { character_event = { id = emf_trait.1022 } }
	}
}

#### [23] Ill: ill ####
emf_notify_add_ill_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = ill }
			log = "WARNING: emf_notify_add_ill_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (ill)!"
		}
	}
	if = {
		limit = { NOT = { trait = ill } }
		add_trait = ill
		hidden_tooltip = { character_event = { id = emf_trait.23 } }
	}
}
emf_notify_remove_ill_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = ill } }
			log = "WARNING: emf_notify_remove_ill_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (ill)!"
		}
	}
	if = {
		limit = { trait = ill }
		remove_trait = ill
		hidden_tooltip = { character_event = { id = emf_trait.1023 } }
	}
}

#### [24] Pneumonic: pneumonic ####
emf_notify_add_pneumonic_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = pneumonic }
			log = "WARNING: emf_notify_add_pneumonic_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (pneumonic)!"
		}
	}
	if = {
		limit = { NOT = { trait = pneumonic } }
		add_trait = pneumonic
		hidden_tooltip = { character_event = { id = emf_trait.24 } }
	}
}
emf_notify_remove_pneumonic_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = pneumonic } }
			log = "WARNING: emf_notify_remove_pneumonic_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (pneumonic)!"
		}
	}
	if = {
		limit = { trait = pneumonic }
		remove_trait = pneumonic
		hidden_tooltip = { character_event = { id = emf_trait.1024 } }
	}
}

#### [25] Gonorrhea: syphilitic ####
emf_notify_add_syphilitic_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = syphilitic }
			log = "WARNING: emf_notify_add_syphilitic_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (syphilitic)!"
		}
	}
	if = {
		limit = { NOT = { trait = syphilitic } }
		add_trait = syphilitic
		hidden_tooltip = { character_event = { id = emf_trait.25 } }
	}
}
emf_notify_remove_syphilitic_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = syphilitic } }
			log = "WARNING: emf_notify_remove_syphilitic_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (syphilitic)!"
		}
	}
	if = {
		limit = { trait = syphilitic }
		remove_trait = syphilitic
		hidden_tooltip = { character_event = { id = emf_trait.1025 } }
	}
}

#### [26] Leper: leper ####
emf_notify_add_leper_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = leper }
			log = "WARNING: emf_notify_add_leper_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (leper)!"
		}
	}
	if = {
		limit = { NOT = { trait = leper } }
		add_trait = leper
		hidden_tooltip = { character_event = { id = emf_trait.26 } }
	}
}
emf_notify_remove_leper_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = leper } }
			log = "WARNING: emf_notify_remove_leper_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (leper)!"
		}
	}
	if = {
		limit = { trait = leper }
		remove_trait = leper
		hidden_tooltip = { character_event = { id = emf_trait.1026 } }
	}
}

#### [27] Wounded: wounded ####
emf_notify_add_wounded_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = wounded }
			log = "WARNING: emf_notify_add_wounded_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (wounded)!"
		}
	}
	if = {
		limit = { NOT = { trait = wounded } }
		add_trait = wounded
		hidden_tooltip = { character_event = { id = emf_trait.27 } }
	}
}
emf_notify_remove_wounded_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = wounded } }
			log = "WARNING: emf_notify_remove_wounded_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (wounded)!"
		}
	}
	if = {
		limit = { trait = wounded }
		remove_trait = wounded
		hidden_tooltip = { character_event = { id = emf_trait.1027 } }
	}
}

#### [28] Maimed: maimed ####
emf_notify_add_maimed_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = maimed }
			log = "WARNING: emf_notify_add_maimed_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (maimed)!"
		}
	}
	if = {
		limit = { NOT = { trait = maimed } }
		add_trait = maimed
		hidden_tooltip = { character_event = { id = emf_trait.28 } }
	}
}
emf_notify_remove_maimed_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = maimed } }
			log = "WARNING: emf_notify_remove_maimed_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (maimed)!"
		}
	}
	if = {
		limit = { trait = maimed }
		remove_trait = maimed
		hidden_tooltip = { character_event = { id = emf_trait.1028 } }
	}
}

#### [29] Infirm: infirm ####
emf_notify_add_infirm_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = infirm }
			log = "WARNING: emf_notify_add_infirm_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (infirm)!"
		}
	}
	if = {
		limit = { NOT = { trait = infirm } }
		add_trait = infirm
		hidden_tooltip = { character_event = { id = emf_trait.29 } }
	}
}
emf_notify_remove_infirm_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = infirm } }
			log = "WARNING: emf_notify_remove_infirm_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (infirm)!"
		}
	}
	if = {
		limit = { trait = infirm }
		remove_trait = infirm
		hidden_tooltip = { character_event = { id = emf_trait.1029 } }
	}
}

#### [30] Incapable: incapable ####
emf_notify_add_incapable_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = incapable }
			log = "WARNING: emf_notify_add_incapable_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (incapable)!"
		}
	}
	if = {
		limit = { NOT = { trait = incapable } }
		add_trait = incapable
		hidden_tooltip = { character_event = { id = emf_trait.30 } }
	}
}
emf_notify_remove_incapable_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = incapable } }
			log = "WARNING: emf_notify_remove_incapable_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (incapable)!"
		}
	}
	if = {
		limit = { trait = incapable }
		remove_trait = incapable
		hidden_tooltip = { character_event = { id = emf_trait.1030 } }
	}
}

#### [31] Drunkard: drunkard ####
emf_notify_add_drunkard_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = drunkard }
			log = "WARNING: emf_notify_add_drunkard_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (drunkard)!"
		}
	}
	if = {
		limit = { NOT = { trait = drunkard } }
		add_trait = drunkard
		hidden_tooltip = { character_event = { id = emf_trait.31 } }
	}
}
emf_notify_remove_drunkard_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = drunkard } }
			log = "WARNING: emf_notify_remove_drunkard_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (drunkard)!"
		}
	}
	if = {
		limit = { trait = drunkard }
		remove_trait = drunkard
		hidden_tooltip = { character_event = { id = emf_trait.1031 } }
	}
}

#### [32] Has Consumption: has_tuberculosis ####
emf_notify_add_has_tuberculosis_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = has_tuberculosis }
			log = "WARNING: emf_notify_add_has_tuberculosis_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (has_tuberculosis)!"
		}
	}
	if = {
		limit = { NOT = { trait = has_tuberculosis } }
		add_trait = has_tuberculosis
		hidden_tooltip = { character_event = { id = emf_trait.32 } }
	}
}
emf_notify_remove_has_tuberculosis_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = has_tuberculosis } }
			log = "WARNING: emf_notify_remove_has_tuberculosis_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (has_tuberculosis)!"
		}
	}
	if = {
		limit = { trait = has_tuberculosis }
		remove_trait = has_tuberculosis
		hidden_tooltip = { character_event = { id = emf_trait.1032 } }
	}
}

#### [33] Has Slow Fever: has_typhoid_fever ####
emf_notify_add_has_typhoid_fever_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = has_typhoid_fever }
			log = "WARNING: emf_notify_add_has_typhoid_fever_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (has_typhoid_fever)!"
		}
	}
	if = {
		limit = { NOT = { trait = has_typhoid_fever } }
		add_trait = has_typhoid_fever
		hidden_tooltip = { character_event = { id = emf_trait.33 } }
	}
}
emf_notify_remove_has_typhoid_fever_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = has_typhoid_fever } }
			log = "WARNING: emf_notify_remove_has_typhoid_fever_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (has_typhoid_fever)!"
		}
	}
	if = {
		limit = { trait = has_typhoid_fever }
		remove_trait = has_typhoid_fever
		hidden_tooltip = { character_event = { id = emf_trait.1033 } }
	}
}

#### [34] Has Camp Fever: has_typhus ####
emf_notify_add_has_typhus_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = has_typhus }
			log = "WARNING: emf_notify_add_has_typhus_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (has_typhus)!"
		}
	}
	if = {
		limit = { NOT = { trait = has_typhus } }
		add_trait = has_typhus
		hidden_tooltip = { character_event = { id = emf_trait.34 } }
	}
}
emf_notify_remove_has_typhus_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = has_typhus } }
			log = "WARNING: emf_notify_remove_has_typhus_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (has_typhus)!"
		}
	}
	if = {
		limit = { trait = has_typhus }
		remove_trait = has_typhus
		hidden_tooltip = { character_event = { id = emf_trait.1034 } }
	}
}

#### [35] Has the Plague!: has_bubonic_plague ####
emf_notify_add_has_bubonic_plague_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = has_bubonic_plague }
			log = "WARNING: emf_notify_add_has_bubonic_plague_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (has_bubonic_plague)!"
		}
	}
	if = {
		limit = { NOT = { trait = has_bubonic_plague } }
		add_trait = has_bubonic_plague
		hidden_tooltip = { character_event = { id = emf_trait.35 } }
	}
}
emf_notify_remove_has_bubonic_plague_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = has_bubonic_plague } }
			log = "WARNING: emf_notify_remove_has_bubonic_plague_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (has_bubonic_plague)!"
		}
	}
	if = {
		limit = { trait = has_bubonic_plague }
		remove_trait = has_bubonic_plague
		hidden_tooltip = { character_event = { id = emf_trait.1035 } }
	}
}

#### [36] Has Measles: has_measles ####
emf_notify_add_has_measles_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = has_measles }
			log = "WARNING: emf_notify_add_has_measles_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (has_measles)!"
		}
	}
	if = {
		limit = { NOT = { trait = has_measles } }
		add_trait = has_measles
		hidden_tooltip = { character_event = { id = emf_trait.36 } }
	}
}
emf_notify_remove_has_measles_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = has_measles } }
			log = "WARNING: emf_notify_remove_has_measles_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (has_measles)!"
		}
	}
	if = {
		limit = { trait = has_measles }
		remove_trait = has_measles
		hidden_tooltip = { character_event = { id = emf_trait.1036 } }
	}
}

#### [37] Has Smallpox: has_small_pox ####
emf_notify_add_has_small_pox_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = has_small_pox }
			log = "WARNING: emf_notify_add_has_small_pox_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (has_small_pox)!"
		}
	}
	if = {
		limit = { NOT = { trait = has_small_pox } }
		add_trait = has_small_pox
		hidden_tooltip = { character_event = { id = emf_trait.37 } }
	}
}
emf_notify_remove_has_small_pox_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = has_small_pox } }
			log = "WARNING: emf_notify_remove_has_small_pox_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (has_small_pox)!"
		}
	}
	if = {
		limit = { trait = has_small_pox }
		remove_trait = has_small_pox
		hidden_tooltip = { character_event = { id = emf_trait.1037 } }
	}
}

#### [38] Crusader: crusader ####
emf_notify_add_crusader_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = crusader }
			log = "WARNING: emf_notify_add_crusader_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (crusader)!"
		}
	}
	if = {
		limit = { NOT = { trait = crusader } }
		add_trait = crusader
		hidden_tooltip = { character_event = { id = emf_trait.38 } }
	}
}
emf_notify_remove_crusader_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = crusader } }
			log = "WARNING: emf_notify_remove_crusader_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (crusader)!"
		}
	}
	if = {
		limit = { trait = crusader }
		remove_trait = crusader
		hidden_tooltip = { character_event = { id = emf_trait.1038 } }
	}
}

#### [39] Bastard: bastard ####
emf_notify_add_bastard_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = bastard }
			log = "WARNING: emf_notify_add_bastard_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (bastard)!"
		}
	}
	if = {
		limit = { NOT = { trait = bastard } }
		add_trait = bastard
		hidden_tooltip = { character_event = { id = emf_trait.39 } }
	}
}
emf_notify_remove_bastard_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = bastard } }
			log = "WARNING: emf_notify_remove_bastard_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (bastard)!"
		}
	}
	if = {
		limit = { trait = bastard }
		remove_trait = bastard
		hidden_tooltip = { character_event = { id = emf_trait.1039 } }
	}
}

#### [40] Twin: twin ####
emf_notify_add_twin_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = twin }
			log = "WARNING: emf_notify_add_twin_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (twin)!"
		}
	}
	if = {
		limit = { NOT = { trait = twin } }
		add_trait = twin
		hidden_tooltip = { character_event = { id = emf_trait.40 } }
	}
}
emf_notify_remove_twin_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = twin } }
			log = "WARNING: emf_notify_remove_twin_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (twin)!"
		}
	}
	if = {
		limit = { trait = twin }
		remove_trait = twin
		hidden_tooltip = { character_event = { id = emf_trait.1040 } }
	}
}

#### [41] Legitimized Bastard: legit_bastard ####
emf_notify_add_legit_bastard_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = legit_bastard }
			log = "WARNING: emf_notify_add_legit_bastard_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (legit_bastard)!"
		}
	}
	if = {
		limit = { NOT = { trait = legit_bastard } }
		add_trait = legit_bastard
		hidden_tooltip = { character_event = { id = emf_trait.41 } }
	}
}
emf_notify_remove_legit_bastard_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = legit_bastard } }
			log = "WARNING: emf_notify_remove_legit_bastard_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (legit_bastard)!"
		}
	}
	if = {
		limit = { trait = legit_bastard }
		remove_trait = legit_bastard
		hidden_tooltip = { character_event = { id = emf_trait.1041 } }
	}
}

#### [42] Pregnant: pregnant ####
emf_notify_add_pregnant_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = pregnant }
			log = "WARNING: emf_notify_add_pregnant_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (pregnant)!"
		}
	}
	if = {
		limit = { NOT = { trait = pregnant } }
		add_trait = pregnant
		hidden_tooltip = { character_event = { id = emf_trait.42 } }
	}
}
emf_notify_remove_pregnant_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = pregnant } }
			log = "WARNING: emf_notify_remove_pregnant_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (pregnant)!"
		}
	}
	if = {
		limit = { trait = pregnant }
		remove_trait = pregnant
		hidden_tooltip = { character_event = { id = emf_trait.1042 } }
	}
}

#### [43] Excommunicated: excommunicated ####
emf_notify_add_excommunicated_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = excommunicated }
			log = "WARNING: emf_notify_add_excommunicated_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (excommunicated)!"
		}
	}
	if = {
		limit = { NOT = { trait = excommunicated } }
		add_trait = excommunicated
		hidden_tooltip = { character_event = { id = emf_trait.43 } }
	}
}
emf_notify_remove_excommunicated_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = excommunicated } }
			log = "WARNING: emf_notify_remove_excommunicated_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (excommunicated)!"
		}
	}
	if = {
		limit = { trait = excommunicated }
		remove_trait = excommunicated
		hidden_tooltip = { character_event = { id = emf_trait.1043 } }
	}
}

#### [44] Kinslayer: kinslayer ####
emf_notify_add_kinslayer_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = kinslayer }
			log = "WARNING: emf_notify_add_kinslayer_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (kinslayer)!"
		}
	}
	if = {
		limit = { NOT = { trait = kinslayer } }
		add_trait = kinslayer
		hidden_tooltip = { character_event = { id = emf_trait.44 } }
	}
}
emf_notify_remove_kinslayer_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = kinslayer } }
			log = "WARNING: emf_notify_remove_kinslayer_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (kinslayer)!"
		}
	}
	if = {
		limit = { trait = kinslayer }
		remove_trait = kinslayer
		hidden_tooltip = { character_event = { id = emf_trait.1044 } }
	}
}

#### [45] Homosexual: homosexual ####
emf_notify_add_homosexual_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = homosexual }
			log = "WARNING: emf_notify_add_homosexual_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (homosexual)!"
		}
	}
	if = {
		limit = { NOT = { trait = homosexual } }
		add_trait = homosexual
		hidden_tooltip = { character_event = { id = emf_trait.45 } }
	}
}
emf_notify_remove_homosexual_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = homosexual } }
			log = "WARNING: emf_notify_remove_homosexual_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (homosexual)!"
		}
	}
	if = {
		limit = { trait = homosexual }
		remove_trait = homosexual
		hidden_tooltip = { character_event = { id = emf_trait.1045 } }
	}
}

#### [46] Clubfooted: clubfooted ####
emf_notify_add_clubfooted_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = clubfooted }
			log = "WARNING: emf_notify_add_clubfooted_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (clubfooted)!"
		}
	}
	if = {
		limit = { NOT = { trait = clubfooted } }
		add_trait = clubfooted
		hidden_tooltip = { character_event = { id = emf_trait.46 } }
	}
}
emf_notify_remove_clubfooted_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = clubfooted } }
			log = "WARNING: emf_notify_remove_clubfooted_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (clubfooted)!"
		}
	}
	if = {
		limit = { trait = clubfooted }
		remove_trait = clubfooted
		hidden_tooltip = { character_event = { id = emf_trait.1046 } }
	}
}

#### [47] Harelip: harelip ####
emf_notify_add_harelip_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = harelip }
			log = "WARNING: emf_notify_add_harelip_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (harelip)!"
		}
	}
	if = {
		limit = { NOT = { trait = harelip } }
		add_trait = harelip
		hidden_tooltip = { character_event = { id = emf_trait.47 } }
	}
}
emf_notify_remove_harelip_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = harelip } }
			log = "WARNING: emf_notify_remove_harelip_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (harelip)!"
		}
	}
	if = {
		limit = { trait = harelip }
		remove_trait = harelip
		hidden_tooltip = { character_event = { id = emf_trait.1047 } }
	}
}

#### [48] Hunchback: hunchback ####
emf_notify_add_hunchback_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = hunchback }
			log = "WARNING: emf_notify_add_hunchback_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (hunchback)!"
		}
	}
	if = {
		limit = { NOT = { trait = hunchback } }
		add_trait = hunchback
		hidden_tooltip = { character_event = { id = emf_trait.48 } }
	}
}
emf_notify_remove_hunchback_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = hunchback } }
			log = "WARNING: emf_notify_remove_hunchback_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (hunchback)!"
		}
	}
	if = {
		limit = { trait = hunchback }
		remove_trait = hunchback
		hidden_tooltip = { character_event = { id = emf_trait.1048 } }
	}
}

#### [49] Lisp: lisp ####
emf_notify_add_lisp_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = lisp }
			log = "WARNING: emf_notify_add_lisp_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (lisp)!"
		}
	}
	if = {
		limit = { NOT = { trait = lisp } }
		add_trait = lisp
		hidden_tooltip = { character_event = { id = emf_trait.49 } }
	}
}
emf_notify_remove_lisp_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = lisp } }
			log = "WARNING: emf_notify_remove_lisp_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (lisp)!"
		}
	}
	if = {
		limit = { trait = lisp }
		remove_trait = lisp
		hidden_tooltip = { character_event = { id = emf_trait.1049 } }
	}
}

#### [50] Stutter: stutter ####
emf_notify_add_stutter_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = stutter }
			log = "WARNING: emf_notify_add_stutter_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (stutter)!"
		}
	}
	if = {
		limit = { NOT = { trait = stutter } }
		add_trait = stutter
		hidden_tooltip = { character_event = { id = emf_trait.50 } }
	}
}
emf_notify_remove_stutter_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = stutter } }
			log = "WARNING: emf_notify_remove_stutter_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (stutter)!"
		}
	}
	if = {
		limit = { trait = stutter }
		remove_trait = stutter
		hidden_tooltip = { character_event = { id = emf_trait.1050 } }
	}
}

#### [51] Attractive: fair ####
emf_notify_add_fair_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = fair }
			log = "WARNING: emf_notify_add_fair_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (fair)!"
		}
	}
	if = {
		limit = { NOT = { trait = fair } }
		add_trait = fair
		hidden_tooltip = { character_event = { id = emf_trait.51 } }
	}
}
emf_notify_remove_fair_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = fair } }
			log = "WARNING: emf_notify_remove_fair_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (fair)!"
		}
	}
	if = {
		limit = { trait = fair }
		remove_trait = fair
		hidden_tooltip = { character_event = { id = emf_trait.1051 } }
	}
}

#### [52] Ugly: ugly ####
emf_notify_add_ugly_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = ugly }
			log = "WARNING: emf_notify_add_ugly_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (ugly)!"
		}
	}
	if = {
		limit = { NOT = { trait = ugly } }
		add_trait = ugly
		hidden_tooltip = { character_event = { id = emf_trait.52 } }
	}
}
emf_notify_remove_ugly_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = ugly } }
			log = "WARNING: emf_notify_remove_ugly_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (ugly)!"
		}
	}
	if = {
		limit = { trait = ugly }
		remove_trait = ugly
		hidden_tooltip = { character_event = { id = emf_trait.1052 } }
	}
}

#### [53] Dwarf: dwarf ####
emf_notify_add_dwarf_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = dwarf }
			log = "WARNING: emf_notify_add_dwarf_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (dwarf)!"
		}
	}
	if = {
		limit = { NOT = { trait = dwarf } }
		add_trait = dwarf
		hidden_tooltip = { character_event = { id = emf_trait.53 } }
	}
}
emf_notify_remove_dwarf_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = dwarf } }
			log = "WARNING: emf_notify_remove_dwarf_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (dwarf)!"
		}
	}
	if = {
		limit = { trait = dwarf }
		remove_trait = dwarf
		hidden_tooltip = { character_event = { id = emf_trait.1053 } }
	}
}

#### [54] Genius: genius ####
emf_notify_add_genius_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = genius }
			log = "WARNING: emf_notify_add_genius_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (genius)!"
		}
	}
	if = {
		limit = { NOT = { trait = genius } }
		add_trait = genius
		hidden_tooltip = { character_event = { id = emf_trait.54 } }
	}
}
emf_notify_remove_genius_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = genius } }
			log = "WARNING: emf_notify_remove_genius_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (genius)!"
		}
	}
	if = {
		limit = { trait = genius }
		remove_trait = genius
		hidden_tooltip = { character_event = { id = emf_trait.1054 } }
	}
}

#### [55] Quick: quick ####
emf_notify_add_quick_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = quick }
			log = "WARNING: emf_notify_add_quick_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (quick)!"
		}
	}
	if = {
		limit = { NOT = { trait = quick } }
		add_trait = quick
		hidden_tooltip = { character_event = { id = emf_trait.55 } }
	}
}
emf_notify_remove_quick_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = quick } }
			log = "WARNING: emf_notify_remove_quick_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (quick)!"
		}
	}
	if = {
		limit = { trait = quick }
		remove_trait = quick
		hidden_tooltip = { character_event = { id = emf_trait.1055 } }
	}
}

#### [56] Slow: slow ####
emf_notify_add_slow_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = slow }
			log = "WARNING: emf_notify_add_slow_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (slow)!"
		}
	}
	if = {
		limit = { NOT = { trait = slow } }
		add_trait = slow
		hidden_tooltip = { character_event = { id = emf_trait.56 } }
	}
}
emf_notify_remove_slow_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = slow } }
			log = "WARNING: emf_notify_remove_slow_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (slow)!"
		}
	}
	if = {
		limit = { trait = slow }
		remove_trait = slow
		hidden_tooltip = { character_event = { id = emf_trait.1056 } }
	}
}

#### [57] Imbecile: imbecile ####
emf_notify_add_imbecile_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = imbecile }
			log = "WARNING: emf_notify_add_imbecile_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (imbecile)!"
		}
	}
	if = {
		limit = { NOT = { trait = imbecile } }
		add_trait = imbecile
		hidden_tooltip = { character_event = { id = emf_trait.57 } }
	}
}
emf_notify_remove_imbecile_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = imbecile } }
			log = "WARNING: emf_notify_remove_imbecile_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (imbecile)!"
		}
	}
	if = {
		limit = { trait = imbecile }
		remove_trait = imbecile
		hidden_tooltip = { character_event = { id = emf_trait.1057 } }
	}
}

#### [58] Inbred: inbred ####
emf_notify_add_inbred_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = inbred }
			log = "WARNING: emf_notify_add_inbred_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (inbred)!"
		}
	}
	if = {
		limit = { NOT = { trait = inbred } }
		add_trait = inbred
		hidden_tooltip = { character_event = { id = emf_trait.58 } }
	}
}
emf_notify_remove_inbred_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = inbred } }
			log = "WARNING: emf_notify_remove_inbred_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (inbred)!"
		}
	}
	if = {
		limit = { trait = inbred }
		remove_trait = inbred
		hidden_tooltip = { character_event = { id = emf_trait.1058 } }
	}
}

#### [59] Strong: strong ####
emf_notify_add_strong_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = strong }
			log = "WARNING: emf_notify_add_strong_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (strong)!"
		}
	}
	if = {
		limit = { NOT = { trait = strong } }
		add_trait = strong
		hidden_tooltip = { character_event = { id = emf_trait.59 } }
	}
}
emf_notify_remove_strong_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = strong } }
			log = "WARNING: emf_notify_remove_strong_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (strong)!"
		}
	}
	if = {
		limit = { trait = strong }
		remove_trait = strong
		hidden_tooltip = { character_event = { id = emf_trait.1059 } }
	}
}

#### [60] Weak: weak ####
emf_notify_add_weak_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = weak }
			log = "WARNING: emf_notify_add_weak_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (weak)!"
		}
	}
	if = {
		limit = { NOT = { trait = weak } }
		add_trait = weak
		hidden_tooltip = { character_event = { id = emf_trait.60 } }
	}
}
emf_notify_remove_weak_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = weak } }
			log = "WARNING: emf_notify_remove_weak_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (weak)!"
		}
	}
	if = {
		limit = { trait = weak }
		remove_trait = weak
		hidden_tooltip = { character_event = { id = emf_trait.1060 } }
	}
}

#### [61] Celibate: celibate ####
emf_notify_add_celibate_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = celibate }
			log = "WARNING: emf_notify_add_celibate_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (celibate)!"
		}
	}
	if = {
		limit = { NOT = { trait = celibate } }
		add_trait = celibate
		hidden_tooltip = { character_event = { id = emf_trait.61 } }
	}
}
emf_notify_remove_celibate_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = celibate } }
			log = "WARNING: emf_notify_remove_celibate_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (celibate)!"
		}
	}
	if = {
		limit = { trait = celibate }
		remove_trait = celibate
		hidden_tooltip = { character_event = { id = emf_trait.1061 } }
	}
}

#### [62] Hedonist: hedonist ####
emf_notify_add_hedonist_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = hedonist }
			log = "WARNING: emf_notify_add_hedonist_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (hedonist)!"
		}
	}
	if = {
		limit = { NOT = { trait = hedonist } }
		add_trait = hedonist
		hidden_tooltip = { character_event = { id = emf_trait.62 } }
	}
}
emf_notify_remove_hedonist_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = hedonist } }
			log = "WARNING: emf_notify_remove_hedonist_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (hedonist)!"
		}
	}
	if = {
		limit = { trait = hedonist }
		remove_trait = hedonist
		hidden_tooltip = { character_event = { id = emf_trait.1062 } }
	}
}

#### [63] Scholar: scholar ####
emf_notify_add_scholar_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = scholar }
			log = "WARNING: emf_notify_add_scholar_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (scholar)!"
		}
	}
	if = {
		limit = { NOT = { trait = scholar } }
		add_trait = scholar
		hidden_tooltip = { character_event = { id = emf_trait.63 } }
	}
}
emf_notify_remove_scholar_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = scholar } }
			log = "WARNING: emf_notify_remove_scholar_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (scholar)!"
		}
	}
	if = {
		limit = { trait = scholar }
		remove_trait = scholar
		hidden_tooltip = { character_event = { id = emf_trait.1063 } }
	}
}

#### [64] Gardener: gardener ####
emf_notify_add_gardener_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = gardener }
			log = "WARNING: emf_notify_add_gardener_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (gardener)!"
		}
	}
	if = {
		limit = { NOT = { trait = gardener } }
		add_trait = gardener
		hidden_tooltip = { character_event = { id = emf_trait.64 } }
	}
}
emf_notify_remove_gardener_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = gardener } }
			log = "WARNING: emf_notify_remove_gardener_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (gardener)!"
		}
	}
	if = {
		limit = { trait = gardener }
		remove_trait = gardener
		hidden_tooltip = { character_event = { id = emf_trait.1064 } }
	}
}

#### [65] Mystic: mystic ####
emf_notify_add_mystic_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = mystic }
			log = "WARNING: emf_notify_add_mystic_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (mystic)!"
		}
	}
	if = {
		limit = { NOT = { trait = mystic } }
		add_trait = mystic
		hidden_tooltip = { character_event = { id = emf_trait.65 } }
	}
}
emf_notify_remove_mystic_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = mystic } }
			log = "WARNING: emf_notify_remove_mystic_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (mystic)!"
		}
	}
	if = {
		limit = { trait = mystic }
		remove_trait = mystic
		hidden_tooltip = { character_event = { id = emf_trait.1065 } }
	}
}

#### [66] Impaler: impaler ####
emf_notify_add_impaler_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = impaler }
			log = "WARNING: emf_notify_add_impaler_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (impaler)!"
		}
	}
	if = {
		limit = { NOT = { trait = impaler } }
		add_trait = impaler
		hidden_tooltip = { character_event = { id = emf_trait.66 } }
	}
}
emf_notify_remove_impaler_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = impaler } }
			log = "WARNING: emf_notify_remove_impaler_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (impaler)!"
		}
	}
	if = {
		limit = { trait = impaler }
		remove_trait = impaler
		hidden_tooltip = { character_event = { id = emf_trait.1066 } }
	}
}

#### [67] Duelist: duelist ####
emf_notify_add_duelist_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = duelist }
			log = "WARNING: emf_notify_add_duelist_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (duelist)!"
		}
	}
	if = {
		limit = { NOT = { trait = duelist } }
		add_trait = duelist
		hidden_tooltip = { character_event = { id = emf_trait.67 } }
	}
}
emf_notify_remove_duelist_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = duelist } }
			log = "WARNING: emf_notify_remove_duelist_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (duelist)!"
		}
	}
	if = {
		limit = { trait = duelist }
		remove_trait = duelist
		hidden_tooltip = { character_event = { id = emf_trait.1067 } }
	}
}

#### [68] Hunter: hunter ####
emf_notify_add_hunter_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = hunter }
			log = "WARNING: emf_notify_add_hunter_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (hunter)!"
		}
	}
	if = {
		limit = { NOT = { trait = hunter } }
		add_trait = hunter
		hidden_tooltip = { character_event = { id = emf_trait.68 } }
	}
}
emf_notify_remove_hunter_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = hunter } }
			log = "WARNING: emf_notify_remove_hunter_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (hunter)!"
		}
	}
	if = {
		limit = { trait = hunter }
		remove_trait = hunter
		hidden_tooltip = { character_event = { id = emf_trait.1068 } }
	}
}

#### [69] Poet: poet ####
emf_notify_add_poet_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = poet }
			log = "WARNING: emf_notify_add_poet_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (poet)!"
		}
	}
	if = {
		limit = { NOT = { trait = poet } }
		add_trait = poet
		hidden_tooltip = { character_event = { id = emf_trait.69 } }
	}
}
emf_notify_remove_poet_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = poet } }
			log = "WARNING: emf_notify_remove_poet_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (poet)!"
		}
	}
	if = {
		limit = { trait = poet }
		remove_trait = poet
		hidden_tooltip = { character_event = { id = emf_trait.1069 } }
	}
}

#### [70] Falconer: falconer ####
emf_notify_add_falconer_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = falconer }
			log = "WARNING: emf_notify_add_falconer_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (falconer)!"
		}
	}
	if = {
		limit = { NOT = { trait = falconer } }
		add_trait = falconer
		hidden_tooltip = { character_event = { id = emf_trait.70 } }
	}
}
emf_notify_remove_falconer_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = falconer } }
			log = "WARNING: emf_notify_remove_falconer_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (falconer)!"
		}
	}
	if = {
		limit = { trait = falconer }
		remove_trait = falconer
		hidden_tooltip = { character_event = { id = emf_trait.1070 } }
	}
}

#### [71] Lustful: lustful ####
emf_notify_add_lustful_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = lustful }
			log = "WARNING: emf_notify_add_lustful_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (lustful)!"
		}
	}
	if = {
		limit = { NOT = { trait = lustful } }
		add_trait = lustful
		hidden_tooltip = { character_event = { id = emf_trait.71 } }
	}
}
emf_notify_remove_lustful_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = lustful } }
			log = "WARNING: emf_notify_remove_lustful_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (lustful)!"
		}
	}
	if = {
		limit = { trait = lustful }
		remove_trait = lustful
		hidden_tooltip = { character_event = { id = emf_trait.1071 } }
	}
}

#### [72] Chaste: chaste ####
emf_notify_add_chaste_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = chaste }
			log = "WARNING: emf_notify_add_chaste_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (chaste)!"
		}
	}
	if = {
		limit = { NOT = { trait = chaste } }
		add_trait = chaste
		hidden_tooltip = { character_event = { id = emf_trait.72 } }
	}
}
emf_notify_remove_chaste_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = chaste } }
			log = "WARNING: emf_notify_remove_chaste_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (chaste)!"
		}
	}
	if = {
		limit = { trait = chaste }
		remove_trait = chaste
		hidden_tooltip = { character_event = { id = emf_trait.1072 } }
	}
}

#### [73] Gluttonous: gluttonous ####
emf_notify_add_gluttonous_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = gluttonous }
			log = "WARNING: emf_notify_add_gluttonous_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (gluttonous)!"
		}
	}
	if = {
		limit = { NOT = { trait = gluttonous } }
		add_trait = gluttonous
		hidden_tooltip = { character_event = { id = emf_trait.73 } }
	}
}
emf_notify_remove_gluttonous_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = gluttonous } }
			log = "WARNING: emf_notify_remove_gluttonous_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (gluttonous)!"
		}
	}
	if = {
		limit = { trait = gluttonous }
		remove_trait = gluttonous
		hidden_tooltip = { character_event = { id = emf_trait.1073 } }
	}
}

#### [74] Temperate: temperate ####
emf_notify_add_temperate_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = temperate }
			log = "WARNING: emf_notify_add_temperate_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (temperate)!"
		}
	}
	if = {
		limit = { NOT = { trait = temperate } }
		add_trait = temperate
		hidden_tooltip = { character_event = { id = emf_trait.74 } }
	}
}
emf_notify_remove_temperate_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = temperate } }
			log = "WARNING: emf_notify_remove_temperate_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (temperate)!"
		}
	}
	if = {
		limit = { trait = temperate }
		remove_trait = temperate
		hidden_tooltip = { character_event = { id = emf_trait.1074 } }
	}
}

#### [75] Greedy: greedy ####
emf_notify_add_greedy_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = greedy }
			log = "WARNING: emf_notify_add_greedy_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (greedy)!"
		}
	}
	if = {
		limit = { NOT = { trait = greedy } }
		add_trait = greedy
		hidden_tooltip = { character_event = { id = emf_trait.75 } }
	}
}
emf_notify_remove_greedy_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = greedy } }
			log = "WARNING: emf_notify_remove_greedy_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (greedy)!"
		}
	}
	if = {
		limit = { trait = greedy }
		remove_trait = greedy
		hidden_tooltip = { character_event = { id = emf_trait.1075 } }
	}
}

#### [76] Charitable: charitable ####
emf_notify_add_charitable_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = charitable }
			log = "WARNING: emf_notify_add_charitable_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (charitable)!"
		}
	}
	if = {
		limit = { NOT = { trait = charitable } }
		add_trait = charitable
		hidden_tooltip = { character_event = { id = emf_trait.76 } }
	}
}
emf_notify_remove_charitable_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = charitable } }
			log = "WARNING: emf_notify_remove_charitable_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (charitable)!"
		}
	}
	if = {
		limit = { trait = charitable }
		remove_trait = charitable
		hidden_tooltip = { character_event = { id = emf_trait.1076 } }
	}
}

#### [77] Slothful: slothful ####
emf_notify_add_slothful_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = slothful }
			log = "WARNING: emf_notify_add_slothful_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (slothful)!"
		}
	}
	if = {
		limit = { NOT = { trait = slothful } }
		add_trait = slothful
		hidden_tooltip = { character_event = { id = emf_trait.77 } }
	}
}
emf_notify_remove_slothful_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = slothful } }
			log = "WARNING: emf_notify_remove_slothful_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (slothful)!"
		}
	}
	if = {
		limit = { trait = slothful }
		remove_trait = slothful
		hidden_tooltip = { character_event = { id = emf_trait.1077 } }
	}
}

#### [78] Diligent: diligent ####
emf_notify_add_diligent_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = diligent }
			log = "WARNING: emf_notify_add_diligent_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (diligent)!"
		}
	}
	if = {
		limit = { NOT = { trait = diligent } }
		add_trait = diligent
		hidden_tooltip = { character_event = { id = emf_trait.78 } }
	}
}
emf_notify_remove_diligent_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = diligent } }
			log = "WARNING: emf_notify_remove_diligent_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (diligent)!"
		}
	}
	if = {
		limit = { trait = diligent }
		remove_trait = diligent
		hidden_tooltip = { character_event = { id = emf_trait.1078 } }
	}
}

#### [79] Envious: envious ####
emf_notify_add_envious_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = envious }
			log = "WARNING: emf_notify_add_envious_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (envious)!"
		}
	}
	if = {
		limit = { NOT = { trait = envious } }
		add_trait = envious
		hidden_tooltip = { character_event = { id = emf_trait.79 } }
	}
}
emf_notify_remove_envious_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = envious } }
			log = "WARNING: emf_notify_remove_envious_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (envious)!"
		}
	}
	if = {
		limit = { trait = envious }
		remove_trait = envious
		hidden_tooltip = { character_event = { id = emf_trait.1079 } }
	}
}

#### [80] Kind: kind ####
emf_notify_add_kind_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = kind }
			log = "WARNING: emf_notify_add_kind_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (kind)!"
		}
	}
	if = {
		limit = { NOT = { trait = kind } }
		add_trait = kind
		hidden_tooltip = { character_event = { id = emf_trait.80 } }
	}
}
emf_notify_remove_kind_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = kind } }
			log = "WARNING: emf_notify_remove_kind_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (kind)!"
		}
	}
	if = {
		limit = { trait = kind }
		remove_trait = kind
		hidden_tooltip = { character_event = { id = emf_trait.1080 } }
	}
}

#### [81] Wroth: wroth ####
emf_notify_add_wroth_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = wroth }
			log = "WARNING: emf_notify_add_wroth_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (wroth)!"
		}
	}
	if = {
		limit = { NOT = { trait = wroth } }
		add_trait = wroth
		hidden_tooltip = { character_event = { id = emf_trait.81 } }
	}
}
emf_notify_remove_wroth_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = wroth } }
			log = "WARNING: emf_notify_remove_wroth_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (wroth)!"
		}
	}
	if = {
		limit = { trait = wroth }
		remove_trait = wroth
		hidden_tooltip = { character_event = { id = emf_trait.1081 } }
	}
}

#### [82] Patient: patient ####
emf_notify_add_patient_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = patient }
			log = "WARNING: emf_notify_add_patient_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (patient)!"
		}
	}
	if = {
		limit = { NOT = { trait = patient } }
		add_trait = patient
		hidden_tooltip = { character_event = { id = emf_trait.82 } }
	}
}
emf_notify_remove_patient_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = patient } }
			log = "WARNING: emf_notify_remove_patient_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (patient)!"
		}
	}
	if = {
		limit = { trait = patient }
		remove_trait = patient
		hidden_tooltip = { character_event = { id = emf_trait.1082 } }
	}
}

#### [83] Proud: proud ####
emf_notify_add_proud_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = proud }
			log = "WARNING: emf_notify_add_proud_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (proud)!"
		}
	}
	if = {
		limit = { NOT = { trait = proud } }
		add_trait = proud
		hidden_tooltip = { character_event = { id = emf_trait.83 } }
	}
}
emf_notify_remove_proud_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = proud } }
			log = "WARNING: emf_notify_remove_proud_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (proud)!"
		}
	}
	if = {
		limit = { trait = proud }
		remove_trait = proud
		hidden_tooltip = { character_event = { id = emf_trait.1083 } }
	}
}

#### [84] Humble: humble ####
emf_notify_add_humble_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = humble }
			log = "WARNING: emf_notify_add_humble_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (humble)!"
		}
	}
	if = {
		limit = { NOT = { trait = humble } }
		add_trait = humble
		hidden_tooltip = { character_event = { id = emf_trait.84 } }
	}
}
emf_notify_remove_humble_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = humble } }
			log = "WARNING: emf_notify_remove_humble_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (humble)!"
		}
	}
	if = {
		limit = { trait = humble }
		remove_trait = humble
		hidden_tooltip = { character_event = { id = emf_trait.1084 } }
	}
}

#### [85] Deceitful: deceitful ####
emf_notify_add_deceitful_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = deceitful }
			log = "WARNING: emf_notify_add_deceitful_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (deceitful)!"
		}
	}
	if = {
		limit = { NOT = { trait = deceitful } }
		add_trait = deceitful
		hidden_tooltip = { character_event = { id = emf_trait.85 } }
	}
}
emf_notify_remove_deceitful_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = deceitful } }
			log = "WARNING: emf_notify_remove_deceitful_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (deceitful)!"
		}
	}
	if = {
		limit = { trait = deceitful }
		remove_trait = deceitful
		hidden_tooltip = { character_event = { id = emf_trait.1085 } }
	}
}

#### [86] Honest: honest ####
emf_notify_add_honest_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = honest }
			log = "WARNING: emf_notify_add_honest_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (honest)!"
		}
	}
	if = {
		limit = { NOT = { trait = honest } }
		add_trait = honest
		hidden_tooltip = { character_event = { id = emf_trait.86 } }
	}
}
emf_notify_remove_honest_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = honest } }
			log = "WARNING: emf_notify_remove_honest_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (honest)!"
		}
	}
	if = {
		limit = { trait = honest }
		remove_trait = honest
		hidden_tooltip = { character_event = { id = emf_trait.1086 } }
	}
}

#### [87] Craven: craven ####
emf_notify_add_craven_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = craven }
			log = "WARNING: emf_notify_add_craven_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (craven)!"
		}
	}
	if = {
		limit = { NOT = { trait = craven } }
		add_trait = craven
		hidden_tooltip = { character_event = { id = emf_trait.87 } }
	}
}
emf_notify_remove_craven_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = craven } }
			log = "WARNING: emf_notify_remove_craven_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (craven)!"
		}
	}
	if = {
		limit = { trait = craven }
		remove_trait = craven
		hidden_tooltip = { character_event = { id = emf_trait.1087 } }
	}
}

#### [88] Brave: brave ####
emf_notify_add_brave_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = brave }
			log = "WARNING: emf_notify_add_brave_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (brave)!"
		}
	}
	if = {
		limit = { NOT = { trait = brave } }
		add_trait = brave
		hidden_tooltip = { character_event = { id = emf_trait.88 } }
	}
}
emf_notify_remove_brave_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = brave } }
			log = "WARNING: emf_notify_remove_brave_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (brave)!"
		}
	}
	if = {
		limit = { trait = brave }
		remove_trait = brave
		hidden_tooltip = { character_event = { id = emf_trait.1088 } }
	}
}

#### [89] Shy: shy ####
emf_notify_add_shy_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = shy }
			log = "WARNING: emf_notify_add_shy_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (shy)!"
		}
	}
	if = {
		limit = { NOT = { trait = shy } }
		add_trait = shy
		hidden_tooltip = { character_event = { id = emf_trait.89 } }
	}
}
emf_notify_remove_shy_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = shy } }
			log = "WARNING: emf_notify_remove_shy_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (shy)!"
		}
	}
	if = {
		limit = { trait = shy }
		remove_trait = shy
		hidden_tooltip = { character_event = { id = emf_trait.1089 } }
	}
}

#### [90] Gregarious: gregarious ####
emf_notify_add_gregarious_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = gregarious }
			log = "WARNING: emf_notify_add_gregarious_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (gregarious)!"
		}
	}
	if = {
		limit = { NOT = { trait = gregarious } }
		add_trait = gregarious
		hidden_tooltip = { character_event = { id = emf_trait.90 } }
	}
}
emf_notify_remove_gregarious_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = gregarious } }
			log = "WARNING: emf_notify_remove_gregarious_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (gregarious)!"
		}
	}
	if = {
		limit = { trait = gregarious }
		remove_trait = gregarious
		hidden_tooltip = { character_event = { id = emf_trait.1090 } }
	}
}

#### [91] Ambitious: ambitious ####
emf_notify_add_ambitious_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = ambitious }
			log = "WARNING: emf_notify_add_ambitious_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (ambitious)!"
		}
	}
	if = {
		limit = { NOT = { trait = ambitious } }
		add_trait = ambitious
		hidden_tooltip = { character_event = { id = emf_trait.91 } }
	}
}
emf_notify_remove_ambitious_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = ambitious } }
			log = "WARNING: emf_notify_remove_ambitious_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (ambitious)!"
		}
	}
	if = {
		limit = { trait = ambitious }
		remove_trait = ambitious
		hidden_tooltip = { character_event = { id = emf_trait.1091 } }
	}
}

#### [92] Content: content ####
emf_notify_add_content_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = content }
			log = "WARNING: emf_notify_add_content_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (content)!"
		}
	}
	if = {
		limit = { NOT = { trait = content } }
		add_trait = content
		hidden_tooltip = { character_event = { id = emf_trait.92 } }
	}
}
emf_notify_remove_content_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = content } }
			log = "WARNING: emf_notify_remove_content_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (content)!"
		}
	}
	if = {
		limit = { trait = content }
		remove_trait = content
		hidden_tooltip = { character_event = { id = emf_trait.1092 } }
	}
}

#### [93] Arbitrary: arbitrary ####
emf_notify_add_arbitrary_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = arbitrary }
			log = "WARNING: emf_notify_add_arbitrary_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (arbitrary)!"
		}
	}
	if = {
		limit = { NOT = { trait = arbitrary } }
		add_trait = arbitrary
		hidden_tooltip = { character_event = { id = emf_trait.93 } }
	}
}
emf_notify_remove_arbitrary_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = arbitrary } }
			log = "WARNING: emf_notify_remove_arbitrary_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (arbitrary)!"
		}
	}
	if = {
		limit = { trait = arbitrary }
		remove_trait = arbitrary
		hidden_tooltip = { character_event = { id = emf_trait.1093 } }
	}
}

#### [94] Just: just ####
emf_notify_add_just_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = just }
			log = "WARNING: emf_notify_add_just_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (just)!"
		}
	}
	if = {
		limit = { NOT = { trait = just } }
		add_trait = just
		hidden_tooltip = { character_event = { id = emf_trait.94 } }
	}
}
emf_notify_remove_just_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = just } }
			log = "WARNING: emf_notify_remove_just_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (just)!"
		}
	}
	if = {
		limit = { trait = just }
		remove_trait = just
		hidden_tooltip = { character_event = { id = emf_trait.1094 } }
	}
}

#### [95] Cynical: cynical ####
emf_notify_add_cynical_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = cynical }
			log = "WARNING: emf_notify_add_cynical_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (cynical)!"
		}
	}
	if = {
		limit = { NOT = { trait = cynical } }
		add_trait = cynical
		hidden_tooltip = { character_event = { id = emf_trait.95 } }
	}
}
emf_notify_remove_cynical_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = cynical } }
			log = "WARNING: emf_notify_remove_cynical_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (cynical)!"
		}
	}
	if = {
		limit = { trait = cynical }
		remove_trait = cynical
		hidden_tooltip = { character_event = { id = emf_trait.1095 } }
	}
}

#### [96] Zealous: zealous ####
emf_notify_add_zealous_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = zealous }
			log = "WARNING: emf_notify_add_zealous_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (zealous)!"
		}
	}
	if = {
		limit = { NOT = { trait = zealous } }
		add_trait = zealous
		hidden_tooltip = { character_event = { id = emf_trait.96 } }
	}
}
emf_notify_remove_zealous_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = zealous } }
			log = "WARNING: emf_notify_remove_zealous_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (zealous)!"
		}
	}
	if = {
		limit = { trait = zealous }
		remove_trait = zealous
		hidden_tooltip = { character_event = { id = emf_trait.1096 } }
	}
}

#### [97] Paranoid: paranoid ####
emf_notify_add_paranoid_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = paranoid }
			log = "WARNING: emf_notify_add_paranoid_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (paranoid)!"
		}
	}
	if = {
		limit = { NOT = { trait = paranoid } }
		add_trait = paranoid
		hidden_tooltip = { character_event = { id = emf_trait.97 } }
	}
}
emf_notify_remove_paranoid_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = paranoid } }
			log = "WARNING: emf_notify_remove_paranoid_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (paranoid)!"
		}
	}
	if = {
		limit = { trait = paranoid }
		remove_trait = paranoid
		hidden_tooltip = { character_event = { id = emf_trait.1097 } }
	}
}

#### [98] Trusting: trusting ####
emf_notify_add_trusting_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = trusting }
			log = "WARNING: emf_notify_add_trusting_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (trusting)!"
		}
	}
	if = {
		limit = { NOT = { trait = trusting } }
		add_trait = trusting
		hidden_tooltip = { character_event = { id = emf_trait.98 } }
	}
}
emf_notify_remove_trusting_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = trusting } }
			log = "WARNING: emf_notify_remove_trusting_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (trusting)!"
		}
	}
	if = {
		limit = { trait = trusting }
		remove_trait = trusting
		hidden_tooltip = { character_event = { id = emf_trait.1098 } }
	}
}

#### [99] Cruel: cruel ####
emf_notify_add_cruel_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = cruel }
			log = "WARNING: emf_notify_add_cruel_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (cruel)!"
		}
	}
	if = {
		limit = { NOT = { trait = cruel } }
		add_trait = cruel
		hidden_tooltip = { character_event = { id = emf_trait.99 } }
	}
}
emf_notify_remove_cruel_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = cruel } }
			log = "WARNING: emf_notify_remove_cruel_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (cruel)!"
		}
	}
	if = {
		limit = { trait = cruel }
		remove_trait = cruel
		hidden_tooltip = { character_event = { id = emf_trait.1099 } }
	}
}

#### [100] Light Foot Leader: light_foot_leader ####
emf_notify_add_light_foot_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = light_foot_leader }
			log = "WARNING: emf_notify_add_light_foot_leader_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (light_foot_leader)!"
		}
	}
	if = {
		limit = { NOT = { trait = light_foot_leader } }
		add_trait = light_foot_leader
		hidden_tooltip = { character_event = { id = emf_trait.100 } }
	}
}
emf_notify_remove_light_foot_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = light_foot_leader } }
			log = "WARNING: emf_notify_remove_light_foot_leader_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (light_foot_leader)!"
		}
	}
	if = {
		limit = { trait = light_foot_leader }
		remove_trait = light_foot_leader
		hidden_tooltip = { character_event = { id = emf_trait.1100 } }
	}
}

#### [101] Heavy Infantry Leader: heavy_infantry_leader ####
emf_notify_add_heavy_infantry_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = heavy_infantry_leader }
			log = "WARNING: emf_notify_add_heavy_infantry_leader_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (heavy_infantry_leader)!"
		}
	}
	if = {
		limit = { NOT = { trait = heavy_infantry_leader } }
		add_trait = heavy_infantry_leader
		hidden_tooltip = { character_event = { id = emf_trait.101 } }
	}
}
emf_notify_remove_heavy_infantry_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = heavy_infantry_leader } }
			log = "WARNING: emf_notify_remove_heavy_infantry_leader_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (heavy_infantry_leader)!"
		}
	}
	if = {
		limit = { trait = heavy_infantry_leader }
		remove_trait = heavy_infantry_leader
		hidden_tooltip = { character_event = { id = emf_trait.1101 } }
	}
}

#### [102] Cavalry Leader: cavalry_leader ####
emf_notify_add_cavalry_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = cavalry_leader }
			log = "WARNING: emf_notify_add_cavalry_leader_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (cavalry_leader)!"
		}
	}
	if = {
		limit = { NOT = { trait = cavalry_leader } }
		add_trait = cavalry_leader
		hidden_tooltip = { character_event = { id = emf_trait.102 } }
	}
}
emf_notify_remove_cavalry_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = cavalry_leader } }
			log = "WARNING: emf_notify_remove_cavalry_leader_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (cavalry_leader)!"
		}
	}
	if = {
		limit = { trait = cavalry_leader }
		remove_trait = cavalry_leader
		hidden_tooltip = { character_event = { id = emf_trait.1102 } }
	}
}

#### [103] Inspiring Leader: inspiring_leader ####
emf_notify_add_inspiring_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = inspiring_leader }
			log = "WARNING: emf_notify_add_inspiring_leader_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (inspiring_leader)!"
		}
	}
	if = {
		limit = { NOT = { trait = inspiring_leader } }
		add_trait = inspiring_leader
		hidden_tooltip = { character_event = { id = emf_trait.103 } }
	}
}
emf_notify_remove_inspiring_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = inspiring_leader } }
			log = "WARNING: emf_notify_remove_inspiring_leader_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (inspiring_leader)!"
		}
	}
	if = {
		limit = { trait = inspiring_leader }
		remove_trait = inspiring_leader
		hidden_tooltip = { character_event = { id = emf_trait.1103 } }
	}
}

#### [104] Trickster: trickster ####
emf_notify_add_trickster_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = trickster }
			log = "WARNING: emf_notify_add_trickster_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (trickster)!"
		}
	}
	if = {
		limit = { NOT = { trait = trickster } }
		add_trait = trickster
		hidden_tooltip = { character_event = { id = emf_trait.104 } }
	}
}
emf_notify_remove_trickster_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = trickster } }
			log = "WARNING: emf_notify_remove_trickster_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (trickster)!"
		}
	}
	if = {
		limit = { trait = trickster }
		remove_trait = trickster
		hidden_tooltip = { character_event = { id = emf_trait.1104 } }
	}
}

#### [105] Organizer: organizer ####
emf_notify_add_organizer_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = organizer }
			log = "WARNING: emf_notify_add_organizer_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (organizer)!"
		}
	}
	if = {
		limit = { NOT = { trait = organizer } }
		add_trait = organizer
		hidden_tooltip = { character_event = { id = emf_trait.105 } }
	}
}
emf_notify_remove_organizer_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = organizer } }
			log = "WARNING: emf_notify_remove_organizer_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (organizer)!"
		}
	}
	if = {
		limit = { trait = organizer }
		remove_trait = organizer
		hidden_tooltip = { character_event = { id = emf_trait.1105 } }
	}
}

#### [106] Defender: defensive_leader ####
emf_notify_add_defensive_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = defensive_leader }
			log = "WARNING: emf_notify_add_defensive_leader_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (defensive_leader)!"
		}
	}
	if = {
		limit = { NOT = { trait = defensive_leader } }
		add_trait = defensive_leader
		hidden_tooltip = { character_event = { id = emf_trait.106 } }
	}
}
emf_notify_remove_defensive_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = defensive_leader } }
			log = "WARNING: emf_notify_remove_defensive_leader_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (defensive_leader)!"
		}
	}
	if = {
		limit = { trait = defensive_leader }
		remove_trait = defensive_leader
		hidden_tooltip = { character_event = { id = emf_trait.1106 } }
	}
}

#### [107] Direct Leader: experimenter ####
emf_notify_add_experimenter_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = experimenter }
			log = "WARNING: emf_notify_add_experimenter_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (experimenter)!"
		}
	}
	if = {
		limit = { NOT = { trait = experimenter } }
		add_trait = experimenter
		hidden_tooltip = { character_event = { id = emf_trait.107 } }
	}
}
emf_notify_remove_experimenter_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = experimenter } }
			log = "WARNING: emf_notify_remove_experimenter_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (experimenter)!"
		}
	}
	if = {
		limit = { trait = experimenter }
		remove_trait = experimenter
		hidden_tooltip = { character_event = { id = emf_trait.1107 } }
	}
}

#### [108] Flanker: flanker ####
emf_notify_add_flanker_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = flanker }
			log = "WARNING: emf_notify_add_flanker_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (flanker)!"
		}
	}
	if = {
		limit = { NOT = { trait = flanker } }
		add_trait = flanker
		hidden_tooltip = { character_event = { id = emf_trait.108 } }
	}
}
emf_notify_remove_flanker_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = flanker } }
			log = "WARNING: emf_notify_remove_flanker_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (flanker)!"
		}
	}
	if = {
		limit = { trait = flanker }
		remove_trait = flanker
		hidden_tooltip = { character_event = { id = emf_trait.1108 } }
	}
}

#### [109] Aggressive Leader: aggressive_leader ####
emf_notify_add_aggressive_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = aggressive_leader }
			log = "WARNING: emf_notify_add_aggressive_leader_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (aggressive_leader)!"
		}
	}
	if = {
		limit = { NOT = { trait = aggressive_leader } }
		add_trait = aggressive_leader
		hidden_tooltip = { character_event = { id = emf_trait.109 } }
	}
}
emf_notify_remove_aggressive_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = aggressive_leader } }
			log = "WARNING: emf_notify_remove_aggressive_leader_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (aggressive_leader)!"
		}
	}
	if = {
		limit = { trait = aggressive_leader }
		remove_trait = aggressive_leader
		hidden_tooltip = { character_event = { id = emf_trait.1109 } }
	}
}

#### [110] Siege Leader: siege_leader ####
emf_notify_add_siege_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = siege_leader }
			log = "WARNING: emf_notify_add_siege_leader_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (siege_leader)!"
		}
	}
	if = {
		limit = { NOT = { trait = siege_leader } }
		add_trait = siege_leader
		hidden_tooltip = { character_event = { id = emf_trait.110 } }
	}
}
emf_notify_remove_siege_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = siege_leader } }
			log = "WARNING: emf_notify_remove_siege_leader_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (siege_leader)!"
		}
	}
	if = {
		limit = { trait = siege_leader }
		remove_trait = siege_leader
		hidden_tooltip = { character_event = { id = emf_trait.1110 } }
	}
}

#### [111] Mujahid: mujahid ####
emf_notify_add_mujahid_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = mujahid }
			log = "WARNING: emf_notify_add_mujahid_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (mujahid)!"
		}
	}
	if = {
		limit = { NOT = { trait = mujahid } }
		add_trait = mujahid
		hidden_tooltip = { character_event = { id = emf_trait.111 } }
	}
}
emf_notify_remove_mujahid_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = mujahid } }
			log = "WARNING: emf_notify_remove_mujahid_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (mujahid)!"
		}
	}
	if = {
		limit = { trait = mujahid }
		remove_trait = mujahid
		hidden_tooltip = { character_event = { id = emf_trait.1111 } }
	}
}

#### [112] Hajajj: hajjaj ####
emf_notify_add_hajjaj_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = hajjaj }
			log = "WARNING: emf_notify_add_hajjaj_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (hajjaj)!"
		}
	}
	if = {
		limit = { NOT = { trait = hajjaj } }
		add_trait = hajjaj
		hidden_tooltip = { character_event = { id = emf_trait.112 } }
	}
}
emf_notify_remove_hajjaj_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = hajjaj } }
			log = "WARNING: emf_notify_remove_hajjaj_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (hajjaj)!"
		}
	}
	if = {
		limit = { trait = hajjaj }
		remove_trait = hajjaj
		hidden_tooltip = { character_event = { id = emf_trait.1112 } }
	}
}

#### [113] On Hajj: on_hajj ####
emf_notify_add_on_hajj_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = on_hajj }
			log = "WARNING: emf_notify_add_on_hajj_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (on_hajj)!"
		}
	}
	if = {
		limit = { NOT = { trait = on_hajj } }
		add_trait = on_hajj
		hidden_tooltip = { character_event = { id = emf_trait.113 } }
	}
}
emf_notify_remove_on_hajj_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = on_hajj } }
			log = "WARNING: emf_notify_remove_on_hajj_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (on_hajj)!"
		}
	}
	if = {
		limit = { trait = on_hajj }
		remove_trait = on_hajj
		hidden_tooltip = { character_event = { id = emf_trait.1113 } }
	}
}

#### [114] Faqih: faqih ####
emf_notify_add_faqih_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = faqih }
			log = "WARNING: emf_notify_add_faqih_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (faqih)!"
		}
	}
	if = {
		limit = { NOT = { trait = faqih } }
		add_trait = faqih
		hidden_tooltip = { character_event = { id = emf_trait.114 } }
	}
}
emf_notify_remove_faqih_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = faqih } }
			log = "WARNING: emf_notify_remove_faqih_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (faqih)!"
		}
	}
	if = {
		limit = { trait = faqih }
		remove_trait = faqih
		hidden_tooltip = { character_event = { id = emf_trait.1114 } }
	}
}

#### [115] Hafiz: hafiz ####
emf_notify_add_hafiz_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = hafiz }
			log = "WARNING: emf_notify_add_hafiz_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (hafiz)!"
		}
	}
	if = {
		limit = { NOT = { trait = hafiz } }
		add_trait = hafiz
		hidden_tooltip = { character_event = { id = emf_trait.115 } }
	}
}
emf_notify_remove_hafiz_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = hafiz } }
			log = "WARNING: emf_notify_remove_hafiz_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (hafiz)!"
		}
	}
	if = {
		limit = { trait = hafiz }
		remove_trait = hafiz
		hidden_tooltip = { character_event = { id = emf_trait.1115 } }
	}
}

#### [116] Sayyid: sayyid ####
emf_notify_add_sayyid_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = sayyid }
			log = "WARNING: emf_notify_add_sayyid_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (sayyid)!"
		}
	}
	if = {
		limit = { NOT = { trait = sayyid } }
		add_trait = sayyid
		hidden_tooltip = { character_event = { id = emf_trait.116 } }
	}
}
emf_notify_remove_sayyid_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = sayyid } }
			log = "WARNING: emf_notify_remove_sayyid_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (sayyid)!"
		}
	}
	if = {
		limit = { trait = sayyid }
		remove_trait = sayyid
		hidden_tooltip = { character_event = { id = emf_trait.1116 } }
	}
}

#### [117] Pilgrim: pilgrim ####
emf_notify_add_pilgrim_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = pilgrim }
			log = "WARNING: emf_notify_add_pilgrim_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (pilgrim)!"
		}
	}
	if = {
		limit = { NOT = { trait = pilgrim } }
		add_trait = pilgrim
		hidden_tooltip = { character_event = { id = emf_trait.117 } }
	}
}
emf_notify_remove_pilgrim_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = pilgrim } }
			log = "WARNING: emf_notify_remove_pilgrim_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (pilgrim)!"
		}
	}
	if = {
		limit = { trait = pilgrim }
		remove_trait = pilgrim
		hidden_tooltip = { character_event = { id = emf_trait.1117 } }
	}
}

#### [118] Mirza: mirza ####
emf_notify_add_mirza_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = mirza }
			log = "WARNING: emf_notify_add_mirza_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (mirza)!"
		}
	}
	if = {
		limit = { NOT = { trait = mirza } }
		add_trait = mirza
		hidden_tooltip = { character_event = { id = emf_trait.118 } }
	}
}
emf_notify_remove_mirza_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = mirza } }
			log = "WARNING: emf_notify_remove_mirza_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (mirza)!"
		}
	}
	if = {
		limit = { trait = mirza }
		remove_trait = mirza
		hidden_tooltip = { character_event = { id = emf_trait.1118 } }
	}
}

#### [119] Scarred: scarred ####
emf_notify_add_scarred_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = scarred }
			log = "WARNING: emf_notify_add_scarred_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (scarred)!"
		}
	}
	if = {
		limit = { NOT = { trait = scarred } }
		add_trait = scarred
		hidden_tooltip = { character_event = { id = emf_trait.119 } }
	}
}
emf_notify_remove_scarred_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = scarred } }
			log = "WARNING: emf_notify_remove_scarred_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (scarred)!"
		}
	}
	if = {
		limit = { trait = scarred }
		remove_trait = scarred
		hidden_tooltip = { character_event = { id = emf_trait.1119 } }
	}
}

#### [120] Eunuch: eunuch ####
emf_notify_add_eunuch_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = eunuch }
			log = "WARNING: emf_notify_add_eunuch_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (eunuch)!"
		}
	}
	if = {
		limit = { NOT = { trait = eunuch } }
		add_trait = eunuch
		hidden_tooltip = { character_event = { id = emf_trait.120 } }
	}
}
emf_notify_remove_eunuch_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = eunuch } }
			log = "WARNING: emf_notify_remove_eunuch_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (eunuch)!"
		}
	}
	if = {
		limit = { trait = eunuch }
		remove_trait = eunuch
		hidden_tooltip = { character_event = { id = emf_trait.1120 } }
	}
}

#### [121] Blinded: blinded ####
emf_notify_add_blinded_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = blinded }
			log = "WARNING: emf_notify_add_blinded_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (blinded)!"
		}
	}
	if = {
		limit = { NOT = { trait = blinded } }
		add_trait = blinded
		hidden_tooltip = { character_event = { id = emf_trait.121 } }
	}
}
emf_notify_remove_blinded_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = blinded } }
			log = "WARNING: emf_notify_remove_blinded_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (blinded)!"
		}
	}
	if = {
		limit = { trait = blinded }
		remove_trait = blinded
		hidden_tooltip = { character_event = { id = emf_trait.1121 } }
	}
}

#### [122] Born in the Purple: born_in_the_purple ####
emf_notify_add_born_in_the_purple_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = born_in_the_purple }
			log = "WARNING: emf_notify_add_born_in_the_purple_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (born_in_the_purple)!"
		}
	}
	if = {
		limit = { NOT = { trait = born_in_the_purple } }
		add_trait = born_in_the_purple
		hidden_tooltip = { character_event = { id = emf_trait.122 } }
	}
}
emf_notify_remove_born_in_the_purple_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = born_in_the_purple } }
			log = "WARNING: emf_notify_remove_born_in_the_purple_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (born_in_the_purple)!"
		}
	}
	if = {
		limit = { trait = born_in_the_purple }
		remove_trait = born_in_the_purple
		hidden_tooltip = { character_event = { id = emf_trait.1122 } }
	}
}

#### [123] Augustus: augustus ####
emf_notify_add_augustus_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = augustus }
			log = "WARNING: emf_notify_add_augustus_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (augustus)!"
		}
	}
	if = {
		limit = { NOT = { trait = augustus } }
		add_trait = augustus
		hidden_tooltip = { character_event = { id = emf_trait.123 } }
	}
}
emf_notify_remove_augustus_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = augustus } }
			log = "WARNING: emf_notify_remove_augustus_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (augustus)!"
		}
	}
	if = {
		limit = { trait = augustus }
		remove_trait = augustus
		hidden_tooltip = { character_event = { id = emf_trait.1123 } }
	}
}

#### [124] Flat Terrain Expert: flat_terrain_leader ####
emf_notify_add_flat_terrain_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = flat_terrain_leader }
			log = "WARNING: emf_notify_add_flat_terrain_leader_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (flat_terrain_leader)!"
		}
	}
	if = {
		limit = { NOT = { trait = flat_terrain_leader } }
		add_trait = flat_terrain_leader
		hidden_tooltip = { character_event = { id = emf_trait.124 } }
	}
}
emf_notify_remove_flat_terrain_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = flat_terrain_leader } }
			log = "WARNING: emf_notify_remove_flat_terrain_leader_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (flat_terrain_leader)!"
		}
	}
	if = {
		limit = { trait = flat_terrain_leader }
		remove_trait = flat_terrain_leader
		hidden_tooltip = { character_event = { id = emf_trait.1124 } }
	}
}

#### [125] Rough Terrain Expert: rough_terrain_leader ####
emf_notify_add_rough_terrain_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = rough_terrain_leader }
			log = "WARNING: emf_notify_add_rough_terrain_leader_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (rough_terrain_leader)!"
		}
	}
	if = {
		limit = { NOT = { trait = rough_terrain_leader } }
		add_trait = rough_terrain_leader
		hidden_tooltip = { character_event = { id = emf_trait.125 } }
	}
}
emf_notify_remove_rough_terrain_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = rough_terrain_leader } }
			log = "WARNING: emf_notify_remove_rough_terrain_leader_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (rough_terrain_leader)!"
		}
	}
	if = {
		limit = { trait = rough_terrain_leader }
		remove_trait = rough_terrain_leader
		hidden_tooltip = { character_event = { id = emf_trait.1125 } }
	}
}

#### [126] Mountain Expert: mountain_terrain_leader ####
emf_notify_add_mountain_terrain_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = mountain_terrain_leader }
			log = "WARNING: emf_notify_add_mountain_terrain_leader_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (mountain_terrain_leader)!"
		}
	}
	if = {
		limit = { NOT = { trait = mountain_terrain_leader } }
		add_trait = mountain_terrain_leader
		hidden_tooltip = { character_event = { id = emf_trait.126 } }
	}
}
emf_notify_remove_mountain_terrain_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = mountain_terrain_leader } }
			log = "WARNING: emf_notify_remove_mountain_terrain_leader_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (mountain_terrain_leader)!"
		}
	}
	if = {
		limit = { trait = mountain_terrain_leader }
		remove_trait = mountain_terrain_leader
		hidden_tooltip = { character_event = { id = emf_trait.1126 } }
	}
}

#### [127] Desert Expert: desert_terrain_leader ####
emf_notify_add_desert_terrain_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = desert_terrain_leader }
			log = "WARNING: emf_notify_add_desert_terrain_leader_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (desert_terrain_leader)!"
		}
	}
	if = {
		limit = { NOT = { trait = desert_terrain_leader } }
		add_trait = desert_terrain_leader
		hidden_tooltip = { character_event = { id = emf_trait.127 } }
	}
}
emf_notify_remove_desert_terrain_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = desert_terrain_leader } }
			log = "WARNING: emf_notify_remove_desert_terrain_leader_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (desert_terrain_leader)!"
		}
	}
	if = {
		limit = { trait = desert_terrain_leader }
		remove_trait = desert_terrain_leader
		hidden_tooltip = { character_event = { id = emf_trait.1127 } }
	}
}

#### [128] Holy Warrior: holy_warrior ####
emf_notify_add_holy_warrior_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = holy_warrior }
			log = "WARNING: emf_notify_add_holy_warrior_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (holy_warrior)!"
		}
	}
	if = {
		limit = { NOT = { trait = holy_warrior } }
		add_trait = holy_warrior
		hidden_tooltip = { character_event = { id = emf_trait.128 } }
	}
}
emf_notify_remove_holy_warrior_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = holy_warrior } }
			log = "WARNING: emf_notify_remove_holy_warrior_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (holy_warrior)!"
		}
	}
	if = {
		limit = { trait = holy_warrior }
		remove_trait = holy_warrior
		hidden_tooltip = { character_event = { id = emf_trait.1128 } }
	}
}

#### [129] Unyielding: unyielding_leader ####
emf_notify_add_unyielding_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = unyielding_leader }
			log = "WARNING: emf_notify_add_unyielding_leader_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (unyielding_leader)!"
		}
	}
	if = {
		limit = { NOT = { trait = unyielding_leader } }
		add_trait = unyielding_leader
		hidden_tooltip = { character_event = { id = emf_trait.129 } }
	}
}
emf_notify_remove_unyielding_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = unyielding_leader } }
			log = "WARNING: emf_notify_remove_unyielding_leader_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (unyielding_leader)!"
		}
	}
	if = {
		limit = { trait = unyielding_leader }
		remove_trait = unyielding_leader
		hidden_tooltip = { character_event = { id = emf_trait.1129 } }
	}
}

#### [130] Syphilitic: has_aztec_disease ####
emf_notify_add_has_aztec_disease_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = has_aztec_disease }
			log = "WARNING: emf_notify_add_has_aztec_disease_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (has_aztec_disease)!"
		}
	}
	if = {
		limit = { NOT = { trait = has_aztec_disease } }
		add_trait = has_aztec_disease
		hidden_tooltip = { character_event = { id = emf_trait.130 } }
	}
}
emf_notify_remove_has_aztec_disease_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = has_aztec_disease } }
			log = "WARNING: emf_notify_remove_has_aztec_disease_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (has_aztec_disease)!"
		}
	}
	if = {
		limit = { trait = has_aztec_disease }
		remove_trait = has_aztec_disease
		hidden_tooltip = { character_event = { id = emf_trait.1130 } }
	}
}

#### [131] Child of Concubine: child_of_consort ####
emf_notify_add_child_of_consort_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = child_of_consort }
			log = "WARNING: emf_notify_add_child_of_consort_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (child_of_consort)!"
		}
	}
	if = {
		limit = { NOT = { trait = child_of_consort } }
		add_trait = child_of_consort
		hidden_tooltip = { character_event = { id = emf_trait.131 } }
	}
}
emf_notify_remove_child_of_consort_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = child_of_consort } }
			log = "WARNING: emf_notify_remove_child_of_consort_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (child_of_consort)!"
		}
	}
	if = {
		limit = { trait = child_of_consort }
		remove_trait = child_of_consort
		hidden_tooltip = { character_event = { id = emf_trait.1131 } }
	}
}

#### [132] Peasant Leader: peasant_leader ####
emf_notify_add_peasant_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = peasant_leader }
			log = "WARNING: emf_notify_add_peasant_leader_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (peasant_leader)!"
		}
	}
	if = {
		limit = { NOT = { trait = peasant_leader } }
		add_trait = peasant_leader
		hidden_tooltip = { character_event = { id = emf_trait.132 } }
	}
}
emf_notify_remove_peasant_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = peasant_leader } }
			log = "WARNING: emf_notify_remove_peasant_leader_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (peasant_leader)!"
		}
	}
	if = {
		limit = { trait = peasant_leader }
		remove_trait = peasant_leader
		hidden_tooltip = { character_event = { id = emf_trait.1132 } }
	}
}

#### [133] Heresiarch: heresiarch ####
emf_notify_add_heresiarch_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = heresiarch }
			log = "WARNING: emf_notify_add_heresiarch_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (heresiarch)!"
		}
	}
	if = {
		limit = { NOT = { trait = heresiarch } }
		add_trait = heresiarch
		hidden_tooltip = { character_event = { id = emf_trait.133 } }
	}
}
emf_notify_remove_heresiarch_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = heresiarch } }
			log = "WARNING: emf_notify_remove_heresiarch_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (heresiarch)!"
		}
	}
	if = {
		limit = { trait = heresiarch }
		remove_trait = heresiarch
		hidden_tooltip = { character_event = { id = emf_trait.1133 } }
	}
}

#### [134] Berserker: berserker ####
emf_notify_add_berserker_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = berserker }
			log = "WARNING: emf_notify_add_berserker_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (berserker)!"
		}
	}
	if = {
		limit = { NOT = { trait = berserker } }
		add_trait = berserker
		hidden_tooltip = { character_event = { id = emf_trait.134 } }
	}
}
emf_notify_remove_berserker_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = berserker } }
			log = "WARNING: emf_notify_remove_berserker_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (berserker)!"
		}
	}
	if = {
		limit = { trait = berserker }
		remove_trait = berserker
		hidden_tooltip = { character_event = { id = emf_trait.1134 } }
	}
}

#### [135] Varangian: varangian ####
emf_notify_add_varangian_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = varangian }
			log = "WARNING: emf_notify_add_varangian_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (varangian)!"
		}
	}
	if = {
		limit = { NOT = { trait = varangian } }
		add_trait = varangian
		hidden_tooltip = { character_event = { id = emf_trait.135 } }
	}
}
emf_notify_remove_varangian_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = varangian } }
			log = "WARNING: emf_notify_remove_varangian_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (varangian)!"
		}
	}
	if = {
		limit = { trait = varangian }
		remove_trait = varangian
		hidden_tooltip = { character_event = { id = emf_trait.1135 } }
	}
}

#### [136] Saoshyant: saoshyant ####
emf_notify_add_saoshyant_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = saoshyant }
			log = "WARNING: emf_notify_add_saoshyant_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (saoshyant)!"
		}
	}
	if = {
		limit = { NOT = { trait = saoshyant } }
		add_trait = saoshyant
		hidden_tooltip = { character_event = { id = emf_trait.136 } }
	}
}
emf_notify_remove_saoshyant_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = saoshyant } }
			log = "WARNING: emf_notify_remove_saoshyant_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (saoshyant)!"
		}
	}
	if = {
		limit = { trait = saoshyant }
		remove_trait = saoshyant
		hidden_tooltip = { character_event = { id = emf_trait.1136 } }
	}
}

#### [137] Saoshyant Descendant: saoshyant_descendant ####
emf_notify_add_saoshyant_descendant_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = saoshyant_descendant }
			log = "WARNING: emf_notify_add_saoshyant_descendant_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (saoshyant_descendant)!"
		}
	}
	if = {
		limit = { NOT = { trait = saoshyant_descendant } }
		add_trait = saoshyant_descendant
		hidden_tooltip = { character_event = { id = emf_trait.137 } }
	}
}
emf_notify_remove_saoshyant_descendant_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = saoshyant_descendant } }
			log = "WARNING: emf_notify_remove_saoshyant_descendant_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (saoshyant_descendant)!"
		}
	}
	if = {
		limit = { trait = saoshyant_descendant }
		remove_trait = saoshyant_descendant
		hidden_tooltip = { character_event = { id = emf_trait.1137 } }
	}
}

#### [138] Viking: viking ####
emf_notify_add_viking_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = viking }
			log = "WARNING: emf_notify_add_viking_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (viking)!"
		}
	}
	if = {
		limit = { NOT = { trait = viking } }
		add_trait = viking
		hidden_tooltip = { character_event = { id = emf_trait.138 } }
	}
}
emf_notify_remove_viking_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = viking } }
			log = "WARNING: emf_notify_remove_viking_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (viking)!"
		}
	}
	if = {
		limit = { trait = viking }
		remove_trait = viking
		hidden_tooltip = { character_event = { id = emf_trait.1138 } }
	}
}

#### [139] Monk: monk ####
emf_notify_add_monk_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = monk }
			log = "WARNING: emf_notify_add_monk_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (monk)!"
		}
	}
	if = {
		limit = { NOT = { trait = monk } }
		add_trait = monk
		hidden_tooltip = { character_event = { id = emf_trait.139 } }
	}
}
emf_notify_remove_monk_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = monk } }
			log = "WARNING: emf_notify_remove_monk_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (monk)!"
		}
	}
	if = {
		limit = { trait = monk }
		remove_trait = monk
		hidden_tooltip = { character_event = { id = emf_trait.1139 } }
	}
}

#### [140] Nun: nun ####
emf_notify_add_nun_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = nun }
			log = "WARNING: emf_notify_add_nun_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (nun)!"
		}
	}
	if = {
		limit = { NOT = { trait = nun } }
		add_trait = nun
		hidden_tooltip = { character_event = { id = emf_trait.140 } }
	}
}
emf_notify_remove_nun_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = nun } }
			log = "WARNING: emf_notify_remove_nun_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (nun)!"
		}
	}
	if = {
		limit = { trait = nun }
		remove_trait = nun
		hidden_tooltip = { character_event = { id = emf_trait.1140 } }
	}
}

#### [141] On pilgrimage: on_pilgrimage ####
emf_notify_add_on_pilgrimage_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = on_pilgrimage }
			log = "WARNING: emf_notify_add_on_pilgrimage_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (on_pilgrimage)!"
		}
	}
	if = {
		limit = { NOT = { trait = on_pilgrimage } }
		add_trait = on_pilgrimage
		hidden_tooltip = { character_event = { id = emf_trait.141 } }
	}
}
emf_notify_remove_on_pilgrimage_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = on_pilgrimage } }
			log = "WARNING: emf_notify_remove_on_pilgrimage_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (on_pilgrimage)!"
		}
	}
	if = {
		limit = { trait = on_pilgrimage }
		remove_trait = on_pilgrimage
		hidden_tooltip = { character_event = { id = emf_trait.1141 } }
	}
}

#### [142] Sickly: sickly ####
emf_notify_add_sickly_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = sickly }
			log = "WARNING: emf_notify_add_sickly_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (sickly)!"
		}
	}
	if = {
		limit = { NOT = { trait = sickly } }
		add_trait = sickly
		hidden_tooltip = { character_event = { id = emf_trait.142 } }
	}
}
emf_notify_remove_sickly_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = sickly } }
			log = "WARNING: emf_notify_remove_sickly_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (sickly)!"
		}
	}
	if = {
		limit = { trait = sickly }
		remove_trait = sickly
		hidden_tooltip = { character_event = { id = emf_trait.1142 } }
	}
}

#### [143] Mu'tazilite: mutazilite ####
emf_notify_add_mutazilite_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = mutazilite }
			log = "WARNING: emf_notify_add_mutazilite_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (mutazilite)!"
		}
	}
	if = {
		limit = { NOT = { trait = mutazilite } }
		add_trait = mutazilite
		hidden_tooltip = { character_event = { id = emf_trait.143 } }
	}
}
emf_notify_remove_mutazilite_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = mutazilite } }
			log = "WARNING: emf_notify_remove_mutazilite_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (mutazilite)!"
		}
	}
	if = {
		limit = { trait = mutazilite }
		remove_trait = mutazilite
		hidden_tooltip = { character_event = { id = emf_trait.1143 } }
	}
}

#### [144] Ash'ari: ashari ####
emf_notify_add_ashari_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = ashari }
			log = "WARNING: emf_notify_add_ashari_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (ashari)!"
		}
	}
	if = {
		limit = { NOT = { trait = ashari } }
		add_trait = ashari
		hidden_tooltip = { character_event = { id = emf_trait.144 } }
	}
}
emf_notify_remove_ashari_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = ashari } }
			log = "WARNING: emf_notify_remove_ashari_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (ashari)!"
		}
	}
	if = {
		limit = { trait = ashari }
		remove_trait = ashari
		hidden_tooltip = { character_event = { id = emf_trait.1144 } }
	}
}

#### [145] Sympathy for Christendom: sympathy_christendom ####
emf_notify_add_sympathy_christendom_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = sympathy_christendom }
			log = "WARNING: emf_notify_add_sympathy_christendom_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (sympathy_christendom)!"
		}
	}
	if = {
		limit = { NOT = { trait = sympathy_christendom } }
		add_trait = sympathy_christendom
		hidden_tooltip = { character_event = { id = emf_trait.145 } }
	}
}
emf_notify_remove_sympathy_christendom_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = sympathy_christendom } }
			log = "WARNING: emf_notify_remove_sympathy_christendom_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (sympathy_christendom)!"
		}
	}
	if = {
		limit = { trait = sympathy_christendom }
		remove_trait = sympathy_christendom
		hidden_tooltip = { character_event = { id = emf_trait.1145 } }
	}
}

#### [146] Sympathy for Islam: sympathy_islam ####
emf_notify_add_sympathy_islam_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = sympathy_islam }
			log = "WARNING: emf_notify_add_sympathy_islam_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (sympathy_islam)!"
		}
	}
	if = {
		limit = { NOT = { trait = sympathy_islam } }
		add_trait = sympathy_islam
		hidden_tooltip = { character_event = { id = emf_trait.146 } }
	}
}
emf_notify_remove_sympathy_islam_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = sympathy_islam } }
			log = "WARNING: emf_notify_remove_sympathy_islam_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (sympathy_islam)!"
		}
	}
	if = {
		limit = { trait = sympathy_islam }
		remove_trait = sympathy_islam
		hidden_tooltip = { character_event = { id = emf_trait.1146 } }
	}
}

#### [147] Sympathy for Pagans: sympathy_pagans ####
emf_notify_add_sympathy_pagans_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = sympathy_pagans }
			log = "WARNING: emf_notify_add_sympathy_pagans_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (sympathy_pagans)!"
		}
	}
	if = {
		limit = { NOT = { trait = sympathy_pagans } }
		add_trait = sympathy_pagans
		hidden_tooltip = { character_event = { id = emf_trait.147 } }
	}
}
emf_notify_remove_sympathy_pagans_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = sympathy_pagans } }
			log = "WARNING: emf_notify_remove_sympathy_pagans_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (sympathy_pagans)!"
		}
	}
	if = {
		limit = { trait = sympathy_pagans }
		remove_trait = sympathy_pagans
		hidden_tooltip = { character_event = { id = emf_trait.1147 } }
	}
}

#### [148] Sympathy for Zoroastrianism: sympathy_zoroastrianism ####
emf_notify_add_sympathy_zoroastrianism_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = sympathy_zoroastrianism }
			log = "WARNING: emf_notify_add_sympathy_zoroastrianism_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (sympathy_zoroastrianism)!"
		}
	}
	if = {
		limit = { NOT = { trait = sympathy_zoroastrianism } }
		add_trait = sympathy_zoroastrianism
		hidden_tooltip = { character_event = { id = emf_trait.148 } }
	}
}
emf_notify_remove_sympathy_zoroastrianism_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = sympathy_zoroastrianism } }
			log = "WARNING: emf_notify_remove_sympathy_zoroastrianism_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (sympathy_zoroastrianism)!"
		}
	}
	if = {
		limit = { trait = sympathy_zoroastrianism }
		remove_trait = sympathy_zoroastrianism
		hidden_tooltip = { character_event = { id = emf_trait.1148 } }
	}
}

#### [149] Sympathy for Judaism: sympathy_judaism ####
emf_notify_add_sympathy_judaism_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = sympathy_judaism }
			log = "WARNING: emf_notify_add_sympathy_judaism_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (sympathy_judaism)!"
		}
	}
	if = {
		limit = { NOT = { trait = sympathy_judaism } }
		add_trait = sympathy_judaism
		hidden_tooltip = { character_event = { id = emf_trait.149 } }
	}
}
emf_notify_remove_sympathy_judaism_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = sympathy_judaism } }
			log = "WARNING: emf_notify_remove_sympathy_judaism_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (sympathy_judaism)!"
		}
	}
	if = {
		limit = { trait = sympathy_judaism }
		remove_trait = sympathy_judaism
		hidden_tooltip = { character_event = { id = emf_trait.1149 } }
	}
}

#### [150] Wicked Priest: bad_priest_christian ####
emf_notify_add_bad_priest_christian_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = bad_priest_christian }
			log = "WARNING: emf_notify_add_bad_priest_christian_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (bad_priest_christian)!"
		}
	}
	if = {
		limit = { NOT = { trait = bad_priest_christian } }
		add_trait = bad_priest_christian
		hidden_tooltip = { character_event = { id = emf_trait.150 } }
	}
}
emf_notify_remove_bad_priest_christian_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = bad_priest_christian } }
			log = "WARNING: emf_notify_remove_bad_priest_christian_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (bad_priest_christian)!"
		}
	}
	if = {
		limit = { trait = bad_priest_christian }
		remove_trait = bad_priest_christian
		hidden_tooltip = { character_event = { id = emf_trait.1150 } }
	}
}

#### [151] Wicked Caliph: bad_priest_muslim ####
emf_notify_add_bad_priest_muslim_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = bad_priest_muslim }
			log = "WARNING: emf_notify_add_bad_priest_muslim_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (bad_priest_muslim)!"
		}
	}
	if = {
		limit = { NOT = { trait = bad_priest_muslim } }
		add_trait = bad_priest_muslim
		hidden_tooltip = { character_event = { id = emf_trait.151 } }
	}
}
emf_notify_remove_bad_priest_muslim_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = bad_priest_muslim } }
			log = "WARNING: emf_notify_remove_bad_priest_muslim_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (bad_priest_muslim)!"
		}
	}
	if = {
		limit = { trait = bad_priest_muslim }
		remove_trait = bad_priest_muslim
		hidden_tooltip = { character_event = { id = emf_trait.1151 } }
	}
}

#### [152] Unworthy Priest: bad_priest_norse ####
emf_notify_add_bad_priest_norse_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = bad_priest_norse }
			log = "WARNING: emf_notify_add_bad_priest_norse_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (bad_priest_norse)!"
		}
	}
	if = {
		limit = { NOT = { trait = bad_priest_norse } }
		add_trait = bad_priest_norse
		hidden_tooltip = { character_event = { id = emf_trait.152 } }
	}
}
emf_notify_remove_bad_priest_norse_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = bad_priest_norse } }
			log = "WARNING: emf_notify_remove_bad_priest_norse_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (bad_priest_norse)!"
		}
	}
	if = {
		limit = { trait = bad_priest_norse }
		remove_trait = bad_priest_norse
		hidden_tooltip = { character_event = { id = emf_trait.1152 } }
	}
}

#### [153] Unworthy Priest: bad_priest_tengri ####
emf_notify_add_bad_priest_tengri_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = bad_priest_tengri }
			log = "WARNING: emf_notify_add_bad_priest_tengri_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (bad_priest_tengri)!"
		}
	}
	if = {
		limit = { NOT = { trait = bad_priest_tengri } }
		add_trait = bad_priest_tengri
		hidden_tooltip = { character_event = { id = emf_trait.153 } }
	}
}
emf_notify_remove_bad_priest_tengri_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = bad_priest_tengri } }
			log = "WARNING: emf_notify_remove_bad_priest_tengri_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (bad_priest_tengri)!"
		}
	}
	if = {
		limit = { trait = bad_priest_tengri }
		remove_trait = bad_priest_tengri
		hidden_tooltip = { character_event = { id = emf_trait.1153 } }
	}
}

#### [154] Immoral Priest: bad_priest_zoroastrian ####
emf_notify_add_bad_priest_zoroastrian_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = bad_priest_zoroastrian }
			log = "WARNING: emf_notify_add_bad_priest_zoroastrian_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (bad_priest_zoroastrian)!"
		}
	}
	if = {
		limit = { NOT = { trait = bad_priest_zoroastrian } }
		add_trait = bad_priest_zoroastrian
		hidden_tooltip = { character_event = { id = emf_trait.154 } }
	}
}
emf_notify_remove_bad_priest_zoroastrian_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = bad_priest_zoroastrian } }
			log = "WARNING: emf_notify_remove_bad_priest_zoroastrian_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (bad_priest_zoroastrian)!"
		}
	}
	if = {
		limit = { trait = bad_priest_zoroastrian }
		remove_trait = bad_priest_zoroastrian
		hidden_tooltip = { character_event = { id = emf_trait.1154 } }
	}
}

#### [155] Unworthy Priest: bad_priest_aztec ####
emf_notify_add_bad_priest_aztec_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = bad_priest_aztec }
			log = "WARNING: emf_notify_add_bad_priest_aztec_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (bad_priest_aztec)!"
		}
	}
	if = {
		limit = { NOT = { trait = bad_priest_aztec } }
		add_trait = bad_priest_aztec
		hidden_tooltip = { character_event = { id = emf_trait.155 } }
	}
}
emf_notify_remove_bad_priest_aztec_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = bad_priest_aztec } }
			log = "WARNING: emf_notify_remove_bad_priest_aztec_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (bad_priest_aztec)!"
		}
	}
	if = {
		limit = { trait = bad_priest_aztec }
		remove_trait = bad_priest_aztec
		hidden_tooltip = { character_event = { id = emf_trait.1155 } }
	}
}

#### [156] Battlefield Terrain Master: narrow_flank_leader ####
emf_notify_add_narrow_flank_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = narrow_flank_leader }
			log = "WARNING: emf_notify_add_narrow_flank_leader_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (narrow_flank_leader)!"
		}
	}
	if = {
		limit = { NOT = { trait = narrow_flank_leader } }
		add_trait = narrow_flank_leader
		hidden_tooltip = { character_event = { id = emf_trait.156 } }
	}
}
emf_notify_remove_narrow_flank_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = narrow_flank_leader } }
			log = "WARNING: emf_notify_remove_narrow_flank_leader_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (narrow_flank_leader)!"
		}
	}
	if = {
		limit = { trait = narrow_flank_leader }
		remove_trait = narrow_flank_leader
		hidden_tooltip = { character_event = { id = emf_trait.1156 } }
	}
}

#### [157] Brahmin: brahmin ####
emf_notify_add_brahmin_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = brahmin }
			log = "WARNING: emf_notify_add_brahmin_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (brahmin)!"
		}
	}
	if = {
		limit = { NOT = { trait = brahmin } }
		add_trait = brahmin
		hidden_tooltip = { character_event = { id = emf_trait.157 } }
	}
}
emf_notify_remove_brahmin_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = brahmin } }
			log = "WARNING: emf_notify_remove_brahmin_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (brahmin)!"
		}
	}
	if = {
		limit = { trait = brahmin }
		remove_trait = brahmin
		hidden_tooltip = { character_event = { id = emf_trait.1157 } }
	}
}

#### [158] Kshatriya: kshatriya ####
emf_notify_add_kshatriya_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = kshatriya }
			log = "WARNING: emf_notify_add_kshatriya_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (kshatriya)!"
		}
	}
	if = {
		limit = { NOT = { trait = kshatriya } }
		add_trait = kshatriya
		hidden_tooltip = { character_event = { id = emf_trait.158 } }
	}
}
emf_notify_remove_kshatriya_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = kshatriya } }
			log = "WARNING: emf_notify_remove_kshatriya_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (kshatriya)!"
		}
	}
	if = {
		limit = { trait = kshatriya }
		remove_trait = kshatriya
		hidden_tooltip = { character_event = { id = emf_trait.1158 } }
	}
}

#### [159] Vaishya: vaishya ####
emf_notify_add_vaishya_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = vaishya }
			log = "WARNING: emf_notify_add_vaishya_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (vaishya)!"
		}
	}
	if = {
		limit = { NOT = { trait = vaishya } }
		add_trait = vaishya
		hidden_tooltip = { character_event = { id = emf_trait.159 } }
	}
}
emf_notify_remove_vaishya_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = vaishya } }
			log = "WARNING: emf_notify_remove_vaishya_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (vaishya)!"
		}
	}
	if = {
		limit = { trait = vaishya }
		remove_trait = vaishya
		hidden_tooltip = { character_event = { id = emf_trait.1159 } }
	}
}

#### [160] Sympathy for Indian religons: sympathy_indian ####
emf_notify_add_sympathy_indian_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = sympathy_indian }
			log = "WARNING: emf_notify_add_sympathy_indian_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (sympathy_indian)!"
		}
	}
	if = {
		limit = { NOT = { trait = sympathy_indian } }
		add_trait = sympathy_indian
		hidden_tooltip = { character_event = { id = emf_trait.160 } }
	}
}
emf_notify_remove_sympathy_indian_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = sympathy_indian } }
			log = "WARNING: emf_notify_remove_sympathy_indian_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (sympathy_indian)!"
		}
	}
	if = {
		limit = { trait = sympathy_indian }
		remove_trait = sympathy_indian
		hidden_tooltip = { character_event = { id = emf_trait.1160 } }
	}
}

#### [161] Shaivist: shaivist_hindu ####
emf_notify_add_shaivist_hindu_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = shaivist_hindu }
			log = "WARNING: emf_notify_add_shaivist_hindu_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (shaivist_hindu)!"
		}
	}
	if = {
		limit = { NOT = { trait = shaivist_hindu } }
		add_trait = shaivist_hindu
		hidden_tooltip = { character_event = { id = emf_trait.161 } }
	}
}
emf_notify_remove_shaivist_hindu_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = shaivist_hindu } }
			log = "WARNING: emf_notify_remove_shaivist_hindu_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (shaivist_hindu)!"
		}
	}
	if = {
		limit = { trait = shaivist_hindu }
		remove_trait = shaivist_hindu
		hidden_tooltip = { character_event = { id = emf_trait.1161 } }
	}
}

#### [162] Vaishnavist: vaishnavist_hindu ####
emf_notify_add_vaishnavist_hindu_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = vaishnavist_hindu }
			log = "WARNING: emf_notify_add_vaishnavist_hindu_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (vaishnavist_hindu)!"
		}
	}
	if = {
		limit = { NOT = { trait = vaishnavist_hindu } }
		add_trait = vaishnavist_hindu
		hidden_tooltip = { character_event = { id = emf_trait.162 } }
	}
}
emf_notify_remove_vaishnavist_hindu_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = vaishnavist_hindu } }
			log = "WARNING: emf_notify_remove_vaishnavist_hindu_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (vaishnavist_hindu)!"
		}
	}
	if = {
		limit = { trait = vaishnavist_hindu }
		remove_trait = vaishnavist_hindu
		hidden_tooltip = { character_event = { id = emf_trait.1162 } }
	}
}

#### [163] Shaktist: shaktist_hindu ####
emf_notify_add_shaktist_hindu_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = shaktist_hindu }
			log = "WARNING: emf_notify_add_shaktist_hindu_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (shaktist_hindu)!"
		}
	}
	if = {
		limit = { NOT = { trait = shaktist_hindu } }
		add_trait = shaktist_hindu
		hidden_tooltip = { character_event = { id = emf_trait.163 } }
	}
}
emf_notify_remove_shaktist_hindu_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = shaktist_hindu } }
			log = "WARNING: emf_notify_remove_shaktist_hindu_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (shaktist_hindu)!"
		}
	}
	if = {
		limit = { trait = shaktist_hindu }
		remove_trait = shaktist_hindu
		hidden_tooltip = { character_event = { id = emf_trait.1163 } }
	}
}

#### [164] Smartist: smartist_hindu ####
emf_notify_add_smartist_hindu_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = smartist_hindu }
			log = "WARNING: emf_notify_add_smartist_hindu_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (smartist_hindu)!"
		}
	}
	if = {
		limit = { NOT = { trait = smartist_hindu } }
		add_trait = smartist_hindu
		hidden_tooltip = { character_event = { id = emf_trait.164 } }
	}
}
emf_notify_remove_smartist_hindu_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = smartist_hindu } }
			log = "WARNING: emf_notify_remove_smartist_hindu_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (smartist_hindu)!"
		}
	}
	if = {
		limit = { trait = smartist_hindu }
		remove_trait = smartist_hindu
		hidden_tooltip = { character_event = { id = emf_trait.1164 } }
	}
}

#### [165] Mahayana Buddhist: mahayana_buddhist ####
emf_notify_add_mahayana_buddhist_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = mahayana_buddhist }
			log = "WARNING: emf_notify_add_mahayana_buddhist_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (mahayana_buddhist)!"
		}
	}
	if = {
		limit = { NOT = { trait = mahayana_buddhist } }
		add_trait = mahayana_buddhist
		hidden_tooltip = { character_event = { id = emf_trait.165 } }
	}
}
emf_notify_remove_mahayana_buddhist_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = mahayana_buddhist } }
			log = "WARNING: emf_notify_remove_mahayana_buddhist_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (mahayana_buddhist)!"
		}
	}
	if = {
		limit = { trait = mahayana_buddhist }
		remove_trait = mahayana_buddhist
		hidden_tooltip = { character_event = { id = emf_trait.1165 } }
	}
}

#### [166] Vajrayana Buddhist: vajrayana_buddhist ####
emf_notify_add_vajrayana_buddhist_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = vajrayana_buddhist }
			log = "WARNING: emf_notify_add_vajrayana_buddhist_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (vajrayana_buddhist)!"
		}
	}
	if = {
		limit = { NOT = { trait = vajrayana_buddhist } }
		add_trait = vajrayana_buddhist
		hidden_tooltip = { character_event = { id = emf_trait.166 } }
	}
}
emf_notify_remove_vajrayana_buddhist_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = vajrayana_buddhist } }
			log = "WARNING: emf_notify_remove_vajrayana_buddhist_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (vajrayana_buddhist)!"
		}
	}
	if = {
		limit = { trait = vajrayana_buddhist }
		remove_trait = vajrayana_buddhist
		hidden_tooltip = { character_event = { id = emf_trait.1166 } }
	}
}

#### [167] Theravada Buddhist: theravada_buddhist ####
emf_notify_add_theravada_buddhist_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = theravada_buddhist }
			log = "WARNING: emf_notify_add_theravada_buddhist_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (theravada_buddhist)!"
		}
	}
	if = {
		limit = { NOT = { trait = theravada_buddhist } }
		add_trait = theravada_buddhist
		hidden_tooltip = { character_event = { id = emf_trait.167 } }
	}
}
emf_notify_remove_theravada_buddhist_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = theravada_buddhist } }
			log = "WARNING: emf_notify_remove_theravada_buddhist_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (theravada_buddhist)!"
		}
	}
	if = {
		limit = { trait = theravada_buddhist }
		remove_trait = theravada_buddhist
		hidden_tooltip = { character_event = { id = emf_trait.1167 } }
	}
}

#### [168] Digambara Jain: digambara_jain ####
emf_notify_add_digambara_jain_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = digambara_jain }
			log = "WARNING: emf_notify_add_digambara_jain_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (digambara_jain)!"
		}
	}
	if = {
		limit = { NOT = { trait = digambara_jain } }
		add_trait = digambara_jain
		hidden_tooltip = { character_event = { id = emf_trait.168 } }
	}
}
emf_notify_remove_digambara_jain_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = digambara_jain } }
			log = "WARNING: emf_notify_remove_digambara_jain_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (digambara_jain)!"
		}
	}
	if = {
		limit = { trait = digambara_jain }
		remove_trait = digambara_jain
		hidden_tooltip = { character_event = { id = emf_trait.1168 } }
	}
}

#### [169] Svetambara Jain: svetambara_jain ####
emf_notify_add_svetambara_jain_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = svetambara_jain }
			log = "WARNING: emf_notify_add_svetambara_jain_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (svetambara_jain)!"
		}
	}
	if = {
		limit = { NOT = { trait = svetambara_jain } }
		add_trait = svetambara_jain
		hidden_tooltip = { character_event = { id = emf_trait.169 } }
	}
}
emf_notify_remove_svetambara_jain_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = svetambara_jain } }
			log = "WARNING: emf_notify_remove_svetambara_jain_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (svetambara_jain)!"
		}
	}
	if = {
		limit = { trait = svetambara_jain }
		remove_trait = svetambara_jain
		hidden_tooltip = { character_event = { id = emf_trait.1169 } }
	}
}

#### [170] Decadent: decadent ####
emf_notify_add_decadent_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = decadent }
			log = "WARNING: emf_notify_add_decadent_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (decadent)!"
		}
	}
	if = {
		limit = { NOT = { trait = decadent } }
		add_trait = decadent
		hidden_tooltip = { character_event = { id = emf_trait.170 } }
	}
}
emf_notify_remove_decadent_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = decadent } }
			log = "WARNING: emf_notify_remove_decadent_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (decadent)!"
		}
	}
	if = {
		limit = { trait = decadent }
		remove_trait = decadent
		hidden_tooltip = { character_event = { id = emf_trait.1170 } }
	}
}

#### [171] Reincarnation of [Reincarnation.GetBestName]: reincarnation ####
emf_notify_add_reincarnation_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = reincarnation }
			log = "WARNING: emf_notify_add_reincarnation_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (reincarnation)!"
		}
	}
	if = {
		limit = { NOT = { trait = reincarnation } }
		add_trait = reincarnation
		hidden_tooltip = { character_event = { id = emf_trait.171 } }
	}
}
emf_notify_remove_reincarnation_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = reincarnation } }
			log = "WARNING: emf_notify_remove_reincarnation_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (reincarnation)!"
		}
	}
	if = {
		limit = { trait = reincarnation }
		remove_trait = reincarnation
		hidden_tooltip = { character_event = { id = emf_trait.1171 } }
	}
}

#### [172] War Elephant Leader: war_elephant_leader ####
emf_notify_add_war_elephant_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = war_elephant_leader }
			log = "WARNING: emf_notify_add_war_elephant_leader_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (war_elephant_leader)!"
		}
	}
	if = {
		limit = { NOT = { trait = war_elephant_leader } }
		add_trait = war_elephant_leader
		hidden_tooltip = { character_event = { id = emf_trait.172 } }
	}
}
emf_notify_remove_war_elephant_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = war_elephant_leader } }
			log = "WARNING: emf_notify_remove_war_elephant_leader_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (war_elephant_leader)!"
		}
	}
	if = {
		limit = { trait = war_elephant_leader }
		remove_trait = war_elephant_leader
		hidden_tooltip = { character_event = { id = emf_trait.1172 } }
	}
}

#### [173] Jungle Expert: jungle_terrain_leader ####
emf_notify_add_jungle_terrain_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = jungle_terrain_leader }
			log = "WARNING: emf_notify_add_jungle_terrain_leader_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (jungle_terrain_leader)!"
		}
	}
	if = {
		limit = { NOT = { trait = jungle_terrain_leader } }
		add_trait = jungle_terrain_leader
		hidden_tooltip = { character_event = { id = emf_trait.173 } }
	}
}
emf_notify_remove_jungle_terrain_leader_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = jungle_terrain_leader } }
			log = "WARNING: emf_notify_remove_jungle_terrain_leader_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (jungle_terrain_leader)!"
		}
	}
	if = {
		limit = { trait = jungle_terrain_leader }
		remove_trait = jungle_terrain_leader
		hidden_tooltip = { character_event = { id = emf_trait.1173 } }
	}
}

#### [174] In Hiding: in_hiding ####
emf_notify_add_in_hiding_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = in_hiding }
			log = "WARNING: emf_notify_add_in_hiding_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (in_hiding)!"
		}
	}
	if = {
		limit = { NOT = { trait = in_hiding } }
		add_trait = in_hiding
		hidden_tooltip = { character_event = { id = emf_trait.174 } }
	}
}
emf_notify_remove_in_hiding_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = in_hiding } }
			log = "WARNING: emf_notify_remove_in_hiding_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (in_hiding)!"
		}
	}
	if = {
		limit = { trait = in_hiding }
		remove_trait = in_hiding
		hidden_tooltip = { character_event = { id = emf_trait.1174 } }
	}
}

#### [175] Winter Soldier: winter_soldier ####
emf_notify_add_winter_soldier_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = winter_soldier }
			log = "WARNING: emf_notify_add_winter_soldier_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (winter_soldier)!"
		}
	}
	if = {
		limit = { NOT = { trait = winter_soldier } }
		add_trait = winter_soldier
		hidden_tooltip = { character_event = { id = emf_trait.175 } }
	}
}
emf_notify_remove_winter_soldier_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = winter_soldier } }
			log = "WARNING: emf_notify_remove_winter_soldier_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (winter_soldier)!"
		}
	}
	if = {
		limit = { trait = winter_soldier }
		remove_trait = winter_soldier
		hidden_tooltip = { character_event = { id = emf_trait.1175 } }
	}
}

#### [176] Master Seducer: seducer ####
emf_notify_add_seducer_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = seducer }
			log = "WARNING: emf_notify_add_seducer_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (seducer)!"
		}
	}
	if = {
		limit = { NOT = { trait = seducer } }
		add_trait = seducer
		hidden_tooltip = { character_event = { id = emf_trait.176 } }
	}
}
emf_notify_remove_seducer_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = seducer } }
			log = "WARNING: emf_notify_remove_seducer_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (seducer)!"
		}
	}
	if = {
		limit = { trait = seducer }
		remove_trait = seducer
		hidden_tooltip = { character_event = { id = emf_trait.1176 } }
	}
}

#### [177] Master Seductress: seductress ####
emf_notify_add_seductress_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = seductress }
			log = "WARNING: emf_notify_add_seductress_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (seductress)!"
		}
	}
	if = {
		limit = { NOT = { trait = seductress } }
		add_trait = seductress
		hidden_tooltip = { character_event = { id = emf_trait.177 } }
	}
}
emf_notify_remove_seductress_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = seductress } }
			log = "WARNING: emf_notify_remove_seductress_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (seductress)!"
		}
	}
	if = {
		limit = { trait = seductress }
		remove_trait = seductress
		hidden_tooltip = { character_event = { id = emf_trait.1177 } }
	}
}

#### [178] Administrator: administrator ####
emf_notify_add_administrator_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = administrator }
			log = "WARNING: emf_notify_add_administrator_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (administrator)!"
		}
	}
	if = {
		limit = { NOT = { trait = administrator } }
		add_trait = administrator
		hidden_tooltip = { character_event = { id = emf_trait.178 } }
	}
}
emf_notify_remove_administrator_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = administrator } }
			log = "WARNING: emf_notify_remove_administrator_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (administrator)!"
		}
	}
	if = {
		limit = { trait = administrator }
		remove_trait = administrator
		hidden_tooltip = { character_event = { id = emf_trait.1178 } }
	}
}

#### [179] Architect: architect ####
emf_notify_add_architect_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = architect }
			log = "WARNING: emf_notify_add_architect_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (architect)!"
		}
	}
	if = {
		limit = { NOT = { trait = architect } }
		add_trait = architect
		hidden_tooltip = { character_event = { id = emf_trait.179 } }
	}
}
emf_notify_remove_architect_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = architect } }
			log = "WARNING: emf_notify_remove_architect_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (architect)!"
		}
	}
	if = {
		limit = { trait = architect }
		remove_trait = architect
		hidden_tooltip = { character_event = { id = emf_trait.1179 } }
	}
}

#### [180] Strategist: strategist ####
emf_notify_add_strategist_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = strategist }
			log = "WARNING: emf_notify_add_strategist_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (strategist)!"
		}
	}
	if = {
		limit = { NOT = { trait = strategist } }
		add_trait = strategist
		hidden_tooltip = { character_event = { id = emf_trait.180 } }
	}
}
emf_notify_remove_strategist_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = strategist } }
			log = "WARNING: emf_notify_remove_strategist_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (strategist)!"
		}
	}
	if = {
		limit = { trait = strategist }
		remove_trait = strategist
		hidden_tooltip = { character_event = { id = emf_trait.1180 } }
	}
}

#### [181] Socializer: socializer ####
emf_notify_add_socializer_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = socializer }
			log = "WARNING: emf_notify_add_socializer_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (socializer)!"
		}
	}
	if = {
		limit = { NOT = { trait = socializer } }
		add_trait = socializer
		hidden_tooltip = { character_event = { id = emf_trait.181 } }
	}
}
emf_notify_remove_socializer_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = socializer } }
			log = "WARNING: emf_notify_remove_socializer_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (socializer)!"
		}
	}
	if = {
		limit = { trait = socializer }
		remove_trait = socializer
		hidden_tooltip = { character_event = { id = emf_trait.1181 } }
	}
}

#### [182] Master Schemer: schemer ####
emf_notify_add_schemer_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = schemer }
			log = "WARNING: emf_notify_add_schemer_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (schemer)!"
		}
	}
	if = {
		limit = { NOT = { trait = schemer } }
		add_trait = schemer
		hidden_tooltip = { character_event = { id = emf_trait.182 } }
	}
}
emf_notify_remove_schemer_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = schemer } }
			log = "WARNING: emf_notify_remove_schemer_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (schemer)!"
		}
	}
	if = {
		limit = { trait = schemer }
		remove_trait = schemer
		hidden_tooltip = { character_event = { id = emf_trait.1182 } }
	}
}

#### [183] Theologian: theologian ####
emf_notify_add_theologian_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = theologian }
			log = "WARNING: emf_notify_add_theologian_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (theologian)!"
		}
	}
	if = {
		limit = { NOT = { trait = theologian } }
		add_trait = theologian
		hidden_tooltip = { character_event = { id = emf_trait.183 } }
	}
}
emf_notify_remove_theologian_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = theologian } }
			log = "WARNING: emf_notify_remove_theologian_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (theologian)!"
		}
	}
	if = {
		limit = { trait = theologian }
		remove_trait = theologian
		hidden_tooltip = { character_event = { id = emf_trait.1183 } }
	}
}

#### [184] Game Master: gamer ####
emf_notify_add_gamer_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = gamer }
			log = "WARNING: emf_notify_add_gamer_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (gamer)!"
		}
	}
	if = {
		limit = { NOT = { trait = gamer } }
		add_trait = gamer
		hidden_tooltip = { character_event = { id = emf_trait.184 } }
	}
}
emf_notify_remove_gamer_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = gamer } }
			log = "WARNING: emf_notify_remove_gamer_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (gamer)!"
		}
	}
	if = {
		limit = { trait = gamer }
		remove_trait = gamer
		hidden_tooltip = { character_event = { id = emf_trait.1184 } }
	}
}

#### [185] Adventurer: adventurer ####
emf_notify_add_adventurer_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = adventurer }
			log = "WARNING: emf_notify_add_adventurer_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (adventurer)!"
		}
	}
	if = {
		limit = { NOT = { trait = adventurer } }
		add_trait = adventurer
		hidden_tooltip = { character_event = { id = emf_trait.185 } }
	}
}
emf_notify_remove_adventurer_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = adventurer } }
			log = "WARNING: emf_notify_remove_adventurer_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (adventurer)!"
		}
	}
	if = {
		limit = { trait = adventurer }
		remove_trait = adventurer
		hidden_tooltip = { character_event = { id = emf_trait.1185 } }
	}
}

#### [186] Erudite: erudite ####
emf_notify_add_erudite_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = erudite }
			log = "WARNING: emf_notify_add_erudite_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (erudite)!"
		}
	}
	if = {
		limit = { NOT = { trait = erudite } }
		add_trait = erudite
		hidden_tooltip = { character_event = { id = emf_trait.186 } }
	}
}
emf_notify_remove_erudite_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = erudite } }
			log = "WARNING: emf_notify_remove_erudite_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (erudite)!"
		}
	}
	if = {
		limit = { trait = erudite }
		remove_trait = erudite
		hidden_tooltip = { character_event = { id = emf_trait.1186 } }
	}
}

#### [187] Stubborn: stubborn ####
emf_notify_add_stubborn_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = stubborn }
			log = "WARNING: emf_notify_add_stubborn_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (stubborn)!"
		}
	}
	if = {
		limit = { NOT = { trait = stubborn } }
		add_trait = stubborn
		hidden_tooltip = { character_event = { id = emf_trait.187 } }
	}
}
emf_notify_remove_stubborn_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = stubborn } }
			log = "WARNING: emf_notify_remove_stubborn_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (stubborn)!"
		}
	}
	if = {
		limit = { trait = stubborn }
		remove_trait = stubborn
		hidden_tooltip = { character_event = { id = emf_trait.1187 } }
	}
}

#### [188] Haughty: haughty ####
emf_notify_add_haughty_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = haughty }
			log = "WARNING: emf_notify_add_haughty_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (haughty)!"
		}
	}
	if = {
		limit = { NOT = { trait = haughty } }
		add_trait = haughty
		hidden_tooltip = { character_event = { id = emf_trait.188 } }
	}
}
emf_notify_remove_haughty_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = haughty } }
			log = "WARNING: emf_notify_remove_haughty_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (haughty)!"
		}
	}
	if = {
		limit = { trait = haughty }
		remove_trait = haughty
		hidden_tooltip = { character_event = { id = emf_trait.1188 } }
	}
}

#### [189] Affectionate: affectionate ####
emf_notify_add_affectionate_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = affectionate }
			log = "WARNING: emf_notify_add_affectionate_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (affectionate)!"
		}
	}
	if = {
		limit = { NOT = { trait = affectionate } }
		add_trait = affectionate
		hidden_tooltip = { character_event = { id = emf_trait.189 } }
	}
}
emf_notify_remove_affectionate_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = affectionate } }
			log = "WARNING: emf_notify_remove_affectionate_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (affectionate)!"
		}
	}
	if = {
		limit = { trait = affectionate }
		remove_trait = affectionate
		hidden_tooltip = { character_event = { id = emf_trait.1189 } }
	}
}

#### [190] Timid: timid ####
emf_notify_add_timid_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = timid }
			log = "WARNING: emf_notify_add_timid_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (timid)!"
		}
	}
	if = {
		limit = { NOT = { trait = timid } }
		add_trait = timid
		hidden_tooltip = { character_event = { id = emf_trait.190 } }
	}
}
emf_notify_remove_timid_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = timid } }
			log = "WARNING: emf_notify_remove_timid_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (timid)!"
		}
	}
	if = {
		limit = { trait = timid }
		remove_trait = timid
		hidden_tooltip = { character_event = { id = emf_trait.1190 } }
	}
}

#### [191] Rowdy: rowdy ####
emf_notify_add_rowdy_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = rowdy }
			log = "WARNING: emf_notify_add_rowdy_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (rowdy)!"
		}
	}
	if = {
		limit = { NOT = { trait = rowdy } }
		add_trait = rowdy
		hidden_tooltip = { character_event = { id = emf_trait.191 } }
	}
}
emf_notify_remove_rowdy_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = rowdy } }
			log = "WARNING: emf_notify_remove_rowdy_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (rowdy)!"
		}
	}
	if = {
		limit = { trait = rowdy }
		remove_trait = rowdy
		hidden_tooltip = { character_event = { id = emf_trait.1191 } }
	}
}

#### [192] Willful: willful ####
emf_notify_add_willful_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = willful }
			log = "WARNING: emf_notify_add_willful_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (willful)!"
		}
	}
	if = {
		limit = { NOT = { trait = willful } }
		add_trait = willful
		hidden_tooltip = { character_event = { id = emf_trait.192 } }
	}
}
emf_notify_remove_willful_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = willful } }
			log = "WARNING: emf_notify_remove_willful_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (willful)!"
		}
	}
	if = {
		limit = { trait = willful }
		remove_trait = willful
		hidden_tooltip = { character_event = { id = emf_trait.1192 } }
	}
}

#### [193] Brooding: brooding ####
emf_notify_add_brooding_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = brooding }
			log = "WARNING: emf_notify_add_brooding_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (brooding)!"
		}
	}
	if = {
		limit = { NOT = { trait = brooding } }
		add_trait = brooding
		hidden_tooltip = { character_event = { id = emf_trait.193 } }
	}
}
emf_notify_remove_brooding_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = brooding } }
			log = "WARNING: emf_notify_remove_brooding_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (brooding)!"
		}
	}
	if = {
		limit = { trait = brooding }
		remove_trait = brooding
		hidden_tooltip = { character_event = { id = emf_trait.1193 } }
	}
}

#### [194] Indolent: indolent ####
emf_notify_add_indolent_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = indolent }
			log = "WARNING: emf_notify_add_indolent_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (indolent)!"
		}
	}
	if = {
		limit = { NOT = { trait = indolent } }
		add_trait = indolent
		hidden_tooltip = { character_event = { id = emf_trait.194 } }
	}
}
emf_notify_remove_indolent_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = indolent } }
			log = "WARNING: emf_notify_remove_indolent_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (indolent)!"
		}
	}
	if = {
		limit = { trait = indolent }
		remove_trait = indolent
		hidden_tooltip = { character_event = { id = emf_trait.1194 } }
	}
}

#### [195] Playful: playful ####
emf_notify_add_playful_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = playful }
			log = "WARNING: emf_notify_add_playful_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (playful)!"
		}
	}
	if = {
		limit = { NOT = { trait = playful } }
		add_trait = playful
		hidden_tooltip = { character_event = { id = emf_trait.195 } }
	}
}
emf_notify_remove_playful_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = playful } }
			log = "WARNING: emf_notify_remove_playful_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (playful)!"
		}
	}
	if = {
		limit = { trait = playful }
		remove_trait = playful
		hidden_tooltip = { character_event = { id = emf_trait.1195 } }
	}
}

#### [196] Conscientious: conscientious ####
emf_notify_add_conscientious_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = conscientious }
			log = "WARNING: emf_notify_add_conscientious_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (conscientious)!"
		}
	}
	if = {
		limit = { NOT = { trait = conscientious } }
		add_trait = conscientious
		hidden_tooltip = { character_event = { id = emf_trait.196 } }
	}
}
emf_notify_remove_conscientious_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = conscientious } }
			log = "WARNING: emf_notify_remove_conscientious_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (conscientious)!"
		}
	}
	if = {
		limit = { trait = conscientious }
		remove_trait = conscientious
		hidden_tooltip = { character_event = { id = emf_trait.1196 } }
	}
}

#### [197] Fussy: fussy ####
emf_notify_add_fussy_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = fussy }
			log = "WARNING: emf_notify_add_fussy_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (fussy)!"
		}
	}
	if = {
		limit = { NOT = { trait = fussy } }
		add_trait = fussy
		hidden_tooltip = { character_event = { id = emf_trait.197 } }
	}
}
emf_notify_remove_fussy_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = fussy } }
			log = "WARNING: emf_notify_remove_fussy_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (fussy)!"
		}
	}
	if = {
		limit = { trait = fussy }
		remove_trait = fussy
		hidden_tooltip = { character_event = { id = emf_trait.1197 } }
	}
}

#### [198] Curious: curious ####
emf_notify_add_curious_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = curious }
			log = "WARNING: emf_notify_add_curious_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (curious)!"
		}
	}
	if = {
		limit = { NOT = { trait = curious } }
		add_trait = curious
		hidden_tooltip = { character_event = { id = emf_trait.198 } }
	}
}
emf_notify_remove_curious_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = curious } }
			log = "WARNING: emf_notify_remove_curious_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (curious)!"
		}
	}
	if = {
		limit = { trait = curious }
		remove_trait = curious
		hidden_tooltip = { character_event = { id = emf_trait.1198 } }
	}
}

#### [199] Idolizer: idolizer ####
emf_notify_add_idolizer_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = idolizer }
			log = "WARNING: emf_notify_add_idolizer_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (idolizer)!"
		}
	}
	if = {
		limit = { NOT = { trait = idolizer } }
		add_trait = idolizer
		hidden_tooltip = { character_event = { id = emf_trait.199 } }
	}
}
emf_notify_remove_idolizer_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = idolizer } }
			log = "WARNING: emf_notify_remove_idolizer_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (idolizer)!"
		}
	}
	if = {
		limit = { trait = idolizer }
		remove_trait = idolizer
		hidden_tooltip = { character_event = { id = emf_trait.1199 } }
	}
}

#### [200] Horse: horse ####
emf_notify_add_horse_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = horse }
			log = "WARNING: emf_notify_add_horse_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (horse)!"
		}
	}
	if = {
		limit = { NOT = { trait = horse } }
		add_trait = horse
		hidden_tooltip = { character_event = { id = emf_trait.200 } }
	}
}
emf_notify_remove_horse_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = horse } }
			log = "WARNING: emf_notify_remove_horse_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (horse)!"
		}
	}
	if = {
		limit = { trait = horse }
		remove_trait = horse
		hidden_tooltip = { character_event = { id = emf_trait.1200 } }
	}
}

#### [201] Brawny: robust ####
emf_notify_add_robust_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = robust }
			log = "WARNING: emf_notify_add_robust_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (robust)!"
		}
	}
	if = {
		limit = { NOT = { trait = robust } }
		add_trait = robust
		hidden_tooltip = { character_event = { id = emf_trait.201 } }
	}
}
emf_notify_remove_robust_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = robust } }
			log = "WARNING: emf_notify_remove_robust_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (robust)!"
		}
	}
	if = {
		limit = { trait = robust }
		remove_trait = robust
		hidden_tooltip = { character_event = { id = emf_trait.1201 } }
	}
}

#### [202] Frail: feeble ####
emf_notify_add_feeble_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = feeble }
			log = "WARNING: emf_notify_add_feeble_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (feeble)!"
		}
	}
	if = {
		limit = { NOT = { trait = feeble } }
		add_trait = feeble
		hidden_tooltip = { character_event = { id = emf_trait.202 } }
	}
}
emf_notify_remove_feeble_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = feeble } }
			log = "WARNING: emf_notify_remove_feeble_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (feeble)!"
		}
	}
	if = {
		limit = { trait = feeble }
		remove_trait = feeble
		hidden_tooltip = { character_event = { id = emf_trait.1202 } }
	}
}

#### [203] Shrewd: shrewd ####
emf_notify_add_shrewd_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = shrewd }
			log = "WARNING: emf_notify_add_shrewd_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (shrewd)!"
		}
	}
	if = {
		limit = { NOT = { trait = shrewd } }
		add_trait = shrewd
		hidden_tooltip = { character_event = { id = emf_trait.203 } }
	}
}
emf_notify_remove_shrewd_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = shrewd } }
			log = "WARNING: emf_notify_remove_shrewd_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (shrewd)!"
		}
	}
	if = {
		limit = { trait = shrewd }
		remove_trait = shrewd
		hidden_tooltip = { character_event = { id = emf_trait.1203 } }
	}
}

#### [204] Dull: dull ####
emf_notify_add_dull_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = dull }
			log = "WARNING: emf_notify_add_dull_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (dull)!"
		}
	}
	if = {
		limit = { NOT = { trait = dull } }
		add_trait = dull
		hidden_tooltip = { character_event = { id = emf_trait.204 } }
	}
}
emf_notify_remove_dull_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = dull } }
			log = "WARNING: emf_notify_remove_dull_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (dull)!"
		}
	}
	if = {
		limit = { trait = dull }
		remove_trait = dull
		hidden_tooltip = { character_event = { id = emf_trait.1204 } }
	}
}

#### [205] Novice Diplomat: novice_diplomat ####
emf_notify_add_novice_diplomat_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = novice_diplomat }
			log = "WARNING: emf_notify_add_novice_diplomat_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (novice_diplomat)!"
		}
	}
	if = {
		limit = { NOT = { trait = novice_diplomat } }
		add_trait = novice_diplomat
		hidden_tooltip = { character_event = { id = emf_trait.205 } }
	}
}
emf_notify_remove_novice_diplomat_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = novice_diplomat } }
			log = "WARNING: emf_notify_remove_novice_diplomat_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (novice_diplomat)!"
		}
	}
	if = {
		limit = { trait = novice_diplomat }
		remove_trait = novice_diplomat
		hidden_tooltip = { character_event = { id = emf_trait.1205 } }
	}
}

#### [206] Trained Diplomat: trained_diplomat ####
emf_notify_add_trained_diplomat_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = trained_diplomat }
			log = "WARNING: emf_notify_add_trained_diplomat_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (trained_diplomat)!"
		}
	}
	if = {
		limit = { NOT = { trait = trained_diplomat } }
		add_trait = trained_diplomat
		hidden_tooltip = { character_event = { id = emf_trait.206 } }
	}
}
emf_notify_remove_trained_diplomat_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = trained_diplomat } }
			log = "WARNING: emf_notify_remove_trained_diplomat_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (trained_diplomat)!"
		}
	}
	if = {
		limit = { trait = trained_diplomat }
		remove_trait = trained_diplomat
		hidden_tooltip = { character_event = { id = emf_trait.1206 } }
	}
}

#### [207] Skilled Diplomat: skilled_diplomat ####
emf_notify_add_skilled_diplomat_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = skilled_diplomat }
			log = "WARNING: emf_notify_add_skilled_diplomat_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (skilled_diplomat)!"
		}
	}
	if = {
		limit = { NOT = { trait = skilled_diplomat } }
		add_trait = skilled_diplomat
		hidden_tooltip = { character_event = { id = emf_trait.207 } }
	}
}
emf_notify_remove_skilled_diplomat_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = skilled_diplomat } }
			log = "WARNING: emf_notify_remove_skilled_diplomat_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (skilled_diplomat)!"
		}
	}
	if = {
		limit = { trait = skilled_diplomat }
		remove_trait = skilled_diplomat
		hidden_tooltip = { character_event = { id = emf_trait.1207 } }
	}
}

#### [208] Master Diplomat: master_diplomat ####
emf_notify_add_master_diplomat_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = master_diplomat }
			log = "WARNING: emf_notify_add_master_diplomat_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (master_diplomat)!"
		}
	}
	if = {
		limit = { NOT = { trait = master_diplomat } }
		add_trait = master_diplomat
		hidden_tooltip = { character_event = { id = emf_trait.208 } }
	}
}
emf_notify_remove_master_diplomat_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = master_diplomat } }
			log = "WARNING: emf_notify_remove_master_diplomat_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (master_diplomat)!"
		}
	}
	if = {
		limit = { trait = master_diplomat }
		remove_trait = master_diplomat
		hidden_tooltip = { character_event = { id = emf_trait.1208 } }
	}
}

#### [209] Novice Fighter: poor_warrior ####
emf_notify_add_poor_warrior_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = poor_warrior }
			log = "WARNING: emf_notify_add_poor_warrior_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (poor_warrior)!"
		}
	}
	if = {
		limit = { NOT = { trait = poor_warrior } }
		add_trait = poor_warrior
		hidden_tooltip = { character_event = { id = emf_trait.209 } }
	}
}
emf_notify_remove_poor_warrior_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = poor_warrior } }
			log = "WARNING: emf_notify_remove_poor_warrior_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (poor_warrior)!"
		}
	}
	if = {
		limit = { trait = poor_warrior }
		remove_trait = poor_warrior
		hidden_tooltip = { character_event = { id = emf_trait.1209 } }
	}
}

#### [210] Trained Fighter: trained_warrior ####
emf_notify_add_trained_warrior_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = trained_warrior }
			log = "WARNING: emf_notify_add_trained_warrior_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (trained_warrior)!"
		}
	}
	if = {
		limit = { NOT = { trait = trained_warrior } }
		add_trait = trained_warrior
		hidden_tooltip = { character_event = { id = emf_trait.210 } }
	}
}
emf_notify_remove_trained_warrior_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = trained_warrior } }
			log = "WARNING: emf_notify_remove_trained_warrior_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (trained_warrior)!"
		}
	}
	if = {
		limit = { trait = trained_warrior }
		remove_trait = trained_warrior
		hidden_tooltip = { character_event = { id = emf_trait.1210 } }
	}
}

#### [211] Skilled Fighter: skilled_warrior ####
emf_notify_add_skilled_warrior_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = skilled_warrior }
			log = "WARNING: emf_notify_add_skilled_warrior_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (skilled_warrior)!"
		}
	}
	if = {
		limit = { NOT = { trait = skilled_warrior } }
		add_trait = skilled_warrior
		hidden_tooltip = { character_event = { id = emf_trait.211 } }
	}
}
emf_notify_remove_skilled_warrior_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = skilled_warrior } }
			log = "WARNING: emf_notify_remove_skilled_warrior_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (skilled_warrior)!"
		}
	}
	if = {
		limit = { trait = skilled_warrior }
		remove_trait = skilled_warrior
		hidden_tooltip = { character_event = { id = emf_trait.1211 } }
	}
}

#### [212] Formidable Fighter: master_warrior ####
emf_notify_add_master_warrior_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = master_warrior }
			log = "WARNING: emf_notify_add_master_warrior_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (master_warrior)!"
		}
	}
	if = {
		limit = { NOT = { trait = master_warrior } }
		add_trait = master_warrior
		hidden_tooltip = { character_event = { id = emf_trait.212 } }
	}
}
emf_notify_remove_master_warrior_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = master_warrior } }
			log = "WARNING: emf_notify_remove_master_warrior_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (master_warrior)!"
		}
	}
	if = {
		limit = { trait = master_warrior }
		remove_trait = master_warrior
		hidden_tooltip = { character_event = { id = emf_trait.1212 } }
	}
}

#### [213] Tall: tall ####
emf_notify_add_tall_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = tall }
			log = "WARNING: emf_notify_add_tall_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (tall)!"
		}
	}
	if = {
		limit = { NOT = { trait = tall } }
		add_trait = tall
		hidden_tooltip = { character_event = { id = emf_trait.213 } }
	}
}
emf_notify_remove_tall_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = tall } }
			log = "WARNING: emf_notify_remove_tall_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (tall)!"
		}
	}
	if = {
		limit = { trait = tall }
		remove_trait = tall
		hidden_tooltip = { character_event = { id = emf_trait.1213 } }
	}
}

#### [214] Agile: agile ####
emf_notify_add_agile_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = agile }
			log = "WARNING: emf_notify_add_agile_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (agile)!"
		}
	}
	if = {
		limit = { NOT = { trait = agile } }
		add_trait = agile
		hidden_tooltip = { character_event = { id = emf_trait.214 } }
	}
}
emf_notify_remove_agile_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = agile } }
			log = "WARNING: emf_notify_remove_agile_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (agile)!"
		}
	}
	if = {
		limit = { trait = agile }
		remove_trait = agile
		hidden_tooltip = { character_event = { id = emf_trait.1214 } }
	}
}

#### [215] Perceptive: perceptive ####
emf_notify_add_perceptive_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = perceptive }
			log = "WARNING: emf_notify_add_perceptive_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (perceptive)!"
		}
	}
	if = {
		limit = { NOT = { trait = perceptive } }
		add_trait = perceptive
		hidden_tooltip = { character_event = { id = emf_trait.215 } }
	}
}
emf_notify_remove_perceptive_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = perceptive } }
			log = "WARNING: emf_notify_remove_perceptive_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (perceptive)!"
		}
	}
	if = {
		limit = { trait = perceptive }
		remove_trait = perceptive
		hidden_tooltip = { character_event = { id = emf_trait.1215 } }
	}
}

#### [216] Melodious Voice: melodious_voice ####
emf_notify_add_melodious_voice_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = melodious_voice }
			log = "WARNING: emf_notify_add_melodious_voice_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (melodious_voice)!"
		}
	}
	if = {
		limit = { NOT = { trait = melodious_voice } }
		add_trait = melodious_voice
		hidden_tooltip = { character_event = { id = emf_trait.216 } }
	}
}
emf_notify_remove_melodious_voice_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = melodious_voice } }
			log = "WARNING: emf_notify_remove_melodious_voice_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (melodious_voice)!"
		}
	}
	if = {
		limit = { trait = melodious_voice }
		remove_trait = melodious_voice
		hidden_tooltip = { character_event = { id = emf_trait.1216 } }
	}
}

#### [217] Powerful Voice: powerful_voice ####
emf_notify_add_powerful_voice_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = powerful_voice }
			log = "WARNING: emf_notify_add_powerful_voice_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (powerful_voice)!"
		}
	}
	if = {
		limit = { NOT = { trait = powerful_voice } }
		add_trait = powerful_voice
		hidden_tooltip = { character_event = { id = emf_trait.217 } }
	}
}
emf_notify_remove_powerful_voice_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = powerful_voice } }
			log = "WARNING: emf_notify_remove_powerful_voice_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (powerful_voice)!"
		}
	}
	if = {
		limit = { trait = powerful_voice }
		remove_trait = powerful_voice
		hidden_tooltip = { character_event = { id = emf_trait.1217 } }
	}
}

#### [218] Calm Voice: calm_voice ####
emf_notify_add_calm_voice_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = calm_voice }
			log = "WARNING: emf_notify_add_calm_voice_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (calm_voice)!"
		}
	}
	if = {
		limit = { NOT = { trait = calm_voice } }
		add_trait = calm_voice
		hidden_tooltip = { character_event = { id = emf_trait.218 } }
	}
}
emf_notify_remove_calm_voice_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = calm_voice } }
			log = "WARNING: emf_notify_remove_calm_voice_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (calm_voice)!"
		}
	}
	if = {
		limit = { trait = calm_voice }
		remove_trait = calm_voice
		hidden_tooltip = { character_event = { id = emf_trait.1218 } }
	}
}

#### [219] Ambidextrous: ambidextrous ####
emf_notify_add_ambidextrous_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = ambidextrous }
			log = "WARNING: emf_notify_add_ambidextrous_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (ambidextrous)!"
		}
	}
	if = {
		limit = { NOT = { trait = ambidextrous } }
		add_trait = ambidextrous
		hidden_tooltip = { character_event = { id = emf_trait.219 } }
	}
}
emf_notify_remove_ambidextrous_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = ambidextrous } }
			log = "WARNING: emf_notify_remove_ambidextrous_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (ambidextrous)!"
		}
	}
	if = {
		limit = { trait = ambidextrous }
		remove_trait = ambidextrous
		hidden_tooltip = { character_event = { id = emf_trait.1219 } }
	}
}

#### [220] Lefthanded: lefthanded ####
emf_notify_add_lefthanded_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = lefthanded }
			log = "WARNING: emf_notify_add_lefthanded_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (lefthanded)!"
		}
	}
	if = {
		limit = { NOT = { trait = lefthanded } }
		add_trait = lefthanded
		hidden_tooltip = { character_event = { id = emf_trait.220 } }
	}
}
emf_notify_remove_lefthanded_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = lefthanded } }
			log = "WARNING: emf_notify_remove_lefthanded_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (lefthanded)!"
		}
	}
	if = {
		limit = { trait = lefthanded }
		remove_trait = lefthanded
		hidden_tooltip = { character_event = { id = emf_trait.1220 } }
	}
}

#### [221] Wrymouth: wrymouth ####
emf_notify_add_wrymouth_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = wrymouth }
			log = "WARNING: emf_notify_add_wrymouth_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (wrymouth)!"
		}
	}
	if = {
		limit = { NOT = { trait = wrymouth } }
		add_trait = wrymouth
		hidden_tooltip = { character_event = { id = emf_trait.221 } }
	}
}
emf_notify_remove_wrymouth_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = wrymouth } }
			log = "WARNING: emf_notify_remove_wrymouth_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (wrymouth)!"
		}
	}
	if = {
		limit = { trait = wrymouth }
		remove_trait = wrymouth
		hidden_tooltip = { character_event = { id = emf_trait.1221 } }
	}
}

#### [222] Deaf: deaf ####
emf_notify_add_deaf_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = deaf }
			log = "WARNING: emf_notify_add_deaf_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (deaf)!"
		}
	}
	if = {
		limit = { NOT = { trait = deaf } }
		add_trait = deaf
		hidden_tooltip = { character_event = { id = emf_trait.222 } }
	}
}
emf_notify_remove_deaf_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = deaf } }
			log = "WARNING: emf_notify_remove_deaf_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (deaf)!"
		}
	}
	if = {
		limit = { trait = deaf }
		remove_trait = deaf
		hidden_tooltip = { character_event = { id = emf_trait.1222 } }
	}
}

#### [223] Absent-Minded: absentminded ####
emf_notify_add_absentminded_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = absentminded }
			log = "WARNING: emf_notify_add_absentminded_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (absentminded)!"
		}
	}
	if = {
		limit = { NOT = { trait = absentminded } }
		add_trait = absentminded
		hidden_tooltip = { character_event = { id = emf_trait.223 } }
	}
}
emf_notify_remove_absentminded_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = absentminded } }
			log = "WARNING: emf_notify_remove_absentminded_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (absentminded)!"
		}
	}
	if = {
		limit = { trait = absentminded }
		remove_trait = absentminded
		hidden_tooltip = { character_event = { id = emf_trait.1223 } }
	}
}

#### [224] Fertile: fertile ####
emf_notify_add_fertile_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = fertile }
			log = "WARNING: emf_notify_add_fertile_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (fertile)!"
		}
	}
	if = {
		limit = { NOT = { trait = fertile } }
		add_trait = fertile
		hidden_tooltip = { character_event = { id = emf_trait.224 } }
	}
}
emf_notify_remove_fertile_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = fertile } }
			log = "WARNING: emf_notify_remove_fertile_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (fertile)!"
		}
	}
	if = {
		limit = { trait = fertile }
		remove_trait = fertile
		hidden_tooltip = { character_event = { id = emf_trait.1224 } }
	}
}

#### [225] Imposing: imposing ####
emf_notify_add_imposing_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = imposing }
			log = "WARNING: emf_notify_add_imposing_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (imposing)!"
		}
	}
	if = {
		limit = { NOT = { trait = imposing } }
		add_trait = imposing
		hidden_tooltip = { character_event = { id = emf_trait.225 } }
	}
}
emf_notify_remove_imposing_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = imposing } }
			log = "WARNING: emf_notify_remove_imposing_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (imposing)!"
		}
	}
	if = {
		limit = { trait = imposing }
		remove_trait = imposing
		hidden_tooltip = { character_event = { id = emf_trait.1225 } }
	}
}

#### [226] Mother of the Universe: isis ####
emf_notify_add_isis_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = isis }
			log = "WARNING: emf_notify_add_isis_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (isis)!"
		}
	}
	if = {
		limit = { NOT = { trait = isis } }
		add_trait = isis
		hidden_tooltip = { character_event = { id = emf_trait.226 } }
	}
}
emf_notify_remove_isis_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = isis } }
			log = "WARNING: emf_notify_remove_isis_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (isis)!"
		}
	}
	if = {
		limit = { trait = isis }
		remove_trait = isis
		hidden_tooltip = { character_event = { id = emf_trait.1226 } }
	}
}

#### [227] Immortal: immortal ####
emf_notify_add_immortal_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = immortal }
			log = "WARNING: emf_notify_add_immortal_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (immortal)!"
		}
	}
	if = {
		limit = { NOT = { trait = immortal } }
		add_trait = immortal
		hidden_tooltip = { character_event = { id = emf_trait.227 } }
	}
}
emf_notify_remove_immortal_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = immortal } }
			log = "WARNING: emf_notify_remove_immortal_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (immortal)!"
		}
	}
	if = {
		limit = { trait = immortal }
		remove_trait = immortal
		hidden_tooltip = { character_event = { id = emf_trait.1227 } }
	}
}

#### [228] Traveling: traveling ####
emf_notify_add_traveling_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = traveling }
			log = "WARNING: emf_notify_add_traveling_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (traveling)!"
		}
	}
	if = {
		limit = { NOT = { trait = traveling } }
		add_trait = traveling
		hidden_tooltip = { character_event = { id = emf_trait.228 } }
	}
}
emf_notify_remove_traveling_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = traveling } }
			log = "WARNING: emf_notify_remove_traveling_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (traveling)!"
		}
	}
	if = {
		limit = { trait = traveling }
		remove_trait = traveling
		hidden_tooltip = { character_event = { id = emf_trait.1228 } }
	}
}

#### [229] Lucky Ruler: lucky_ruler ####
emf_notify_add_lucky_ruler_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = lucky_ruler }
			log = "WARNING: emf_notify_add_lucky_ruler_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (lucky_ruler)!"
		}
	}
	if = {
		limit = { NOT = { trait = lucky_ruler } }
		add_trait = lucky_ruler
		hidden_tooltip = { character_event = { id = emf_trait.229 } }
	}
}
emf_notify_remove_lucky_ruler_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = lucky_ruler } }
			log = "WARNING: emf_notify_remove_lucky_ruler_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (lucky_ruler)!"
		}
	}
	if = {
		limit = { trait = lucky_ruler }
		remove_trait = lucky_ruler
		hidden_tooltip = { character_event = { id = emf_trait.1229 } }
	}
}

#### [230] Conqueror: conqueror ####
emf_notify_add_conqueror_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = conqueror }
			log = "WARNING: emf_notify_add_conqueror_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (conqueror)!"
		}
	}
	if = {
		limit = { NOT = { trait = conqueror } }
		add_trait = conqueror
		hidden_tooltip = { character_event = { id = emf_trait.230 } }
	}
}
emf_notify_remove_conqueror_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = conqueror } }
			log = "WARNING: emf_notify_remove_conqueror_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (conqueror)!"
		}
	}
	if = {
		limit = { trait = conqueror }
		remove_trait = conqueror
		hidden_tooltip = { character_event = { id = emf_trait.1230 } }
	}
}

#### [231] Respected Commander: commander1 ####
emf_notify_add_commander1_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = commander1 }
			log = "WARNING: emf_notify_add_commander1_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (commander1)!"
		}
	}
	if = {
		limit = { NOT = { trait = commander1 } }
		add_trait = commander1
		hidden_tooltip = { character_event = { id = emf_trait.231 } }
	}
}
emf_notify_remove_commander1_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = commander1 } }
			log = "WARNING: emf_notify_remove_commander1_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (commander1)!"
		}
	}
	if = {
		limit = { trait = commander1 }
		remove_trait = commander1
		hidden_tooltip = { character_event = { id = emf_trait.1231 } }
	}
}

#### [232] Distinguished Commander: commander2 ####
emf_notify_add_commander2_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = commander2 }
			log = "WARNING: emf_notify_add_commander2_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (commander2)!"
		}
	}
	if = {
		limit = { NOT = { trait = commander2 } }
		add_trait = commander2
		hidden_tooltip = { character_event = { id = emf_trait.232 } }
	}
}
emf_notify_remove_commander2_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = commander2 } }
			log = "WARNING: emf_notify_remove_commander2_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (commander2)!"
		}
	}
	if = {
		limit = { trait = commander2 }
		remove_trait = commander2
		hidden_tooltip = { character_event = { id = emf_trait.1232 } }
	}
}

#### [233] Renowned Commander: commander3 ####
emf_notify_add_commander3_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = commander3 }
			log = "WARNING: emf_notify_add_commander3_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (commander3)!"
		}
	}
	if = {
		limit = { NOT = { trait = commander3 } }
		add_trait = commander3
		hidden_tooltip = { character_event = { id = emf_trait.233 } }
	}
}
emf_notify_remove_commander3_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = commander3 } }
			log = "WARNING: emf_notify_remove_commander3_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (commander3)!"
		}
	}
	if = {
		limit = { trait = commander3 }
		remove_trait = commander3
		hidden_tooltip = { character_event = { id = emf_trait.1233 } }
	}
}

#### [234] Legendary Commander: commander4 ####
emf_notify_add_commander4_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = commander4 }
			log = "WARNING: emf_notify_add_commander4_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (commander4)!"
		}
	}
	if = {
		limit = { NOT = { trait = commander4 } }
		add_trait = commander4
		hidden_tooltip = { character_event = { id = emf_trait.234 } }
	}
}
emf_notify_remove_commander4_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = commander4 } }
			log = "WARNING: emf_notify_remove_commander4_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (commander4)!"
		}
	}
	if = {
		limit = { trait = commander4 }
		remove_trait = commander4
		hidden_tooltip = { character_event = { id = emf_trait.1234 } }
	}
}

#### [235] Cannot Inherit: inheritance_disabled ####
emf_notify_add_inheritance_disabled_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = inheritance_disabled }
			log = "WARNING: emf_notify_add_inheritance_disabled_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (inheritance_disabled)!"
		}
	}
	if = {
		limit = { NOT = { trait = inheritance_disabled } }
		add_trait = inheritance_disabled
		hidden_tooltip = { character_event = { id = emf_trait.235 } }
	}
}
emf_notify_remove_inheritance_disabled_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = inheritance_disabled } }
			log = "WARNING: emf_notify_remove_inheritance_disabled_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (inheritance_disabled)!"
		}
	}
	if = {
		limit = { trait = inheritance_disabled }
		remove_trait = inheritance_disabled
		hidden_tooltip = { character_event = { id = emf_trait.1235 } }
	}
}

#### [236] Raider: raider ####
emf_notify_add_raider_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = raider }
			log = "WARNING: emf_notify_add_raider_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (raider)!"
		}
	}
	if = {
		limit = { NOT = { trait = raider } }
		add_trait = raider
		hidden_tooltip = { character_event = { id = emf_trait.236 } }
	}
}
emf_notify_remove_raider_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = raider } }
			log = "WARNING: emf_notify_remove_raider_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (raider)!"
		}
	}
	if = {
		limit = { trait = raider }
		remove_trait = raider
		hidden_tooltip = { character_event = { id = emf_trait.1236 } }
	}
}

#### [237] Roma Victrix: roma_victrix ####
emf_notify_add_roma_victrix_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = roma_victrix }
			log = "WARNING: emf_notify_add_roma_victrix_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (roma_victrix)!"
		}
	}
	if = {
		limit = { NOT = { trait = roma_victrix } }
		add_trait = roma_victrix
		hidden_tooltip = { character_event = { id = emf_trait.237 } }
	}
}
emf_notify_remove_roma_victrix_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = roma_victrix } }
			log = "WARNING: emf_notify_remove_roma_victrix_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (roma_victrix)!"
		}
	}
	if = {
		limit = { trait = roma_victrix }
		remove_trait = roma_victrix
		hidden_tooltip = { character_event = { id = emf_trait.1237 } }
	}
}

#### [238] Loyalist: pf_loyalist ####
emf_notify_add_pf_loyalist_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = pf_loyalist }
			log = "WARNING: emf_notify_add_pf_loyalist_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (pf_loyalist)!"
		}
	}
	if = {
		limit = { NOT = { trait = pf_loyalist } }
		add_trait = pf_loyalist
		hidden_tooltip = { character_event = { id = emf_trait.238 } }
	}
}
emf_notify_remove_pf_loyalist_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = pf_loyalist } }
			log = "WARNING: emf_notify_remove_pf_loyalist_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (pf_loyalist)!"
		}
	}
	if = {
		limit = { trait = pf_loyalist }
		remove_trait = pf_loyalist
		hidden_tooltip = { character_event = { id = emf_trait.1238 } }
	}
}

#### [239] Neutral: pf_neutral ####
emf_notify_add_pf_neutral_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = pf_neutral }
			log = "WARNING: emf_notify_add_pf_neutral_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (pf_neutral)!"
		}
	}
	if = {
		limit = { NOT = { trait = pf_neutral } }
		add_trait = pf_neutral
		hidden_tooltip = { character_event = { id = emf_trait.239 } }
	}
}
emf_notify_remove_pf_neutral_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = pf_neutral } }
			log = "WARNING: emf_notify_remove_pf_neutral_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (pf_neutral)!"
		}
	}
	if = {
		limit = { trait = pf_neutral }
		remove_trait = pf_neutral
		hidden_tooltip = { character_event = { id = emf_trait.1239 } }
	}
}

#### [240] Rebel: pf_rebel ####
emf_notify_add_pf_rebel_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = pf_rebel }
			log = "WARNING: emf_notify_add_pf_rebel_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (pf_rebel)!"
		}
	}
	if = {
		limit = { NOT = { trait = pf_rebel } }
		add_trait = pf_rebel
		hidden_tooltip = { character_event = { id = emf_trait.240 } }
	}
}
emf_notify_remove_pf_rebel_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = pf_rebel } }
			log = "WARNING: emf_notify_remove_pf_rebel_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (pf_rebel)!"
		}
	}
	if = {
		limit = { trait = pf_rebel }
		remove_trait = pf_rebel
		hidden_tooltip = { character_event = { id = emf_trait.1240 } }
	}
}

#### [241] Imperial Bureaucrat: imperial_bureaucrat ####
emf_notify_add_imperial_bureaucrat_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = imperial_bureaucrat }
			log = "WARNING: emf_notify_add_imperial_bureaucrat_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (imperial_bureaucrat)!"
		}
	}
	if = {
		limit = { NOT = { trait = imperial_bureaucrat } }
		add_trait = imperial_bureaucrat
		hidden_tooltip = { character_event = { id = emf_trait.241 } }
	}
}
emf_notify_remove_imperial_bureaucrat_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = imperial_bureaucrat } }
			log = "WARNING: emf_notify_remove_imperial_bureaucrat_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (imperial_bureaucrat)!"
		}
	}
	if = {
		limit = { trait = imperial_bureaucrat }
		remove_trait = imperial_bureaucrat
		hidden_tooltip = { character_event = { id = emf_trait.1241 } }
	}
}

#### [242] Imperial Courtier: imperial_courtier ####
emf_notify_add_imperial_courtier_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = imperial_courtier }
			log = "WARNING: emf_notify_add_imperial_courtier_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (imperial_courtier)!"
		}
	}
	if = {
		limit = { NOT = { trait = imperial_courtier } }
		add_trait = imperial_courtier
		hidden_tooltip = { character_event = { id = emf_trait.242 } }
	}
}
emf_notify_remove_imperial_courtier_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = imperial_courtier } }
			log = "WARNING: emf_notify_remove_imperial_courtier_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (imperial_courtier)!"
		}
	}
	if = {
		limit = { trait = imperial_courtier }
		remove_trait = imperial_courtier
		hidden_tooltip = { character_event = { id = emf_trait.1242 } }
	}
}

#### [243] Imperial Guard: imperial_guard ####
emf_notify_add_imperial_guard_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = imperial_guard }
			log = "WARNING: emf_notify_add_imperial_guard_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (imperial_guard)!"
		}
	}
	if = {
		limit = { NOT = { trait = imperial_guard } }
		add_trait = imperial_guard
		hidden_tooltip = { character_event = { id = emf_trait.243 } }
	}
}
emf_notify_remove_imperial_guard_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = imperial_guard } }
			log = "WARNING: emf_notify_remove_imperial_guard_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (imperial_guard)!"
		}
	}
	if = {
		limit = { trait = imperial_guard }
		remove_trait = imperial_guard
		hidden_tooltip = { character_event = { id = emf_trait.1243 } }
	}
}

#### [244] Decadent Dynasty: dynasty_decadence_low ####
emf_notify_add_dynasty_decadence_low_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = dynasty_decadence_low }
			log = "WARNING: emf_notify_add_dynasty_decadence_low_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (dynasty_decadence_low)!"
		}
	}
	if = {
		limit = { NOT = { trait = dynasty_decadence_low } }
		add_trait = dynasty_decadence_low
		hidden_tooltip = { character_event = { id = emf_trait.244 } }
	}
}
emf_notify_remove_dynasty_decadence_low_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = dynasty_decadence_low } }
			log = "WARNING: emf_notify_remove_dynasty_decadence_low_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (dynasty_decadence_low)!"
		}
	}
	if = {
		limit = { trait = dynasty_decadence_low }
		remove_trait = dynasty_decadence_low
		hidden_tooltip = { character_event = { id = emf_trait.1244 } }
	}
}

#### [245] Very Decadent Dynasty: dynasty_decadence_med ####
emf_notify_add_dynasty_decadence_med_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = dynasty_decadence_med }
			log = "WARNING: emf_notify_add_dynasty_decadence_med_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (dynasty_decadence_med)!"
		}
	}
	if = {
		limit = { NOT = { trait = dynasty_decadence_med } }
		add_trait = dynasty_decadence_med
		hidden_tooltip = { character_event = { id = emf_trait.245 } }
	}
}
emf_notify_remove_dynasty_decadence_med_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = dynasty_decadence_med } }
			log = "WARNING: emf_notify_remove_dynasty_decadence_med_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (dynasty_decadence_med)!"
		}
	}
	if = {
		limit = { trait = dynasty_decadence_med }
		remove_trait = dynasty_decadence_med
		hidden_tooltip = { character_event = { id = emf_trait.1245 } }
	}
}

#### [246] Extremely Decadent Dynasty: dynasty_decadence_high ####
emf_notify_add_dynasty_decadence_high_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = dynasty_decadence_high }
			log = "WARNING: emf_notify_add_dynasty_decadence_high_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (dynasty_decadence_high)!"
		}
	}
	if = {
		limit = { NOT = { trait = dynasty_decadence_high } }
		add_trait = dynasty_decadence_high
		hidden_tooltip = { character_event = { id = emf_trait.246 } }
	}
}
emf_notify_remove_dynasty_decadence_high_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = dynasty_decadence_high } }
			log = "WARNING: emf_notify_remove_dynasty_decadence_high_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (dynasty_decadence_high)!"
		}
	}
	if = {
		limit = { trait = dynasty_decadence_high }
		remove_trait = dynasty_decadence_high
		hidden_tooltip = { character_event = { id = emf_trait.1246 } }
	}
}

#### [247] Adopted: adopted ####
emf_notify_add_adopted_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = adopted }
			log = "WARNING: emf_notify_add_adopted_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (adopted)!"
		}
	}
	if = {
		limit = { NOT = { trait = adopted } }
		add_trait = adopted
		hidden_tooltip = { character_event = { id = emf_trait.247 } }
	}
}
emf_notify_remove_adopted_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = adopted } }
			log = "WARNING: emf_notify_remove_adopted_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (adopted)!"
		}
	}
	if = {
		limit = { trait = adopted }
		remove_trait = adopted
		hidden_tooltip = { character_event = { id = emf_trait.1247 } }
	}
}

#### [248] Alexandrine: alexandrine ####
emf_notify_add_alexandrine_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = alexandrine }
			log = "WARNING: emf_notify_add_alexandrine_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (alexandrine)!"
		}
	}
	if = {
		limit = { NOT = { trait = alexandrine } }
		add_trait = alexandrine
		hidden_tooltip = { character_event = { id = emf_trait.248 } }
	}
}
emf_notify_remove_alexandrine_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = alexandrine } }
			log = "WARNING: emf_notify_remove_alexandrine_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (alexandrine)!"
		}
	}
	if = {
		limit = { trait = alexandrine }
		remove_trait = alexandrine
		hidden_tooltip = { character_event = { id = emf_trait.1248 } }
	}
}

#### [249] Antiochian: antiochene ####
emf_notify_add_antiochene_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = antiochene }
			log = "WARNING: emf_notify_add_antiochene_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (antiochene)!"
		}
	}
	if = {
		limit = { NOT = { trait = antiochene } }
		add_trait = antiochene
		hidden_tooltip = { character_event = { id = emf_trait.249 } }
	}
}
emf_notify_remove_antiochene_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = antiochene } }
			log = "WARNING: emf_notify_remove_antiochene_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (antiochene)!"
		}
	}
	if = {
		limit = { trait = antiochene }
		remove_trait = antiochene
		hidden_tooltip = { character_event = { id = emf_trait.1249 } }
	}
}

#### [250] Court Anarchy: court_anarchy ####
emf_notify_add_court_anarchy_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = court_anarchy }
			log = "WARNING: emf_notify_add_court_anarchy_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (court_anarchy)!"
		}
	}
	if = {
		limit = { NOT = { trait = court_anarchy } }
		add_trait = court_anarchy
		hidden_tooltip = { character_event = { id = emf_trait.250 } }
	}
}
emf_notify_remove_court_anarchy_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = court_anarchy } }
			log = "WARNING: emf_notify_remove_court_anarchy_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (court_anarchy)!"
		}
	}
	if = {
		limit = { trait = court_anarchy }
		remove_trait = court_anarchy
		hidden_tooltip = { character_event = { id = emf_trait.1250 } }
	}
}

#### [251] Cult of Lucifer: cult_of_lucifer ####
emf_notify_add_cult_of_lucifer_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = cult_of_lucifer }
			log = "WARNING: emf_notify_add_cult_of_lucifer_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (cult_of_lucifer)!"
		}
	}
	if = {
		limit = { NOT = { trait = cult_of_lucifer } }
		add_trait = cult_of_lucifer
		hidden_tooltip = { character_event = { id = emf_trait.251 } }
	}
}
emf_notify_remove_cult_of_lucifer_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = cult_of_lucifer } }
			log = "WARNING: emf_notify_remove_cult_of_lucifer_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (cult_of_lucifer)!"
		}
	}
	if = {
		limit = { trait = cult_of_lucifer }
		remove_trait = cult_of_lucifer
		hidden_tooltip = { character_event = { id = emf_trait.1251 } }
	}
}

#### [252] Desert Father: desert_father ####
emf_notify_add_desert_father_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = desert_father }
			log = "WARNING: emf_notify_add_desert_father_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (desert_father)!"
		}
	}
	if = {
		limit = { NOT = { trait = desert_father } }
		add_trait = desert_father
		hidden_tooltip = { character_event = { id = emf_trait.252 } }
	}
}
emf_notify_remove_desert_father_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = desert_father } }
			log = "WARNING: emf_notify_remove_desert_father_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (desert_father)!"
		}
	}
	if = {
		limit = { trait = desert_father }
		remove_trait = desert_father
		hidden_tooltip = { character_event = { id = emf_trait.1252 } }
	}
}

#### [253] Desert Mother: desert_mother ####
emf_notify_add_desert_mother_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = desert_mother }
			log = "WARNING: emf_notify_add_desert_mother_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (desert_mother)!"
		}
	}
	if = {
		limit = { NOT = { trait = desert_mother } }
		add_trait = desert_mother
		hidden_tooltip = { character_event = { id = emf_trait.253 } }
	}
}
emf_notify_remove_desert_mother_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = desert_mother } }
			log = "WARNING: emf_notify_remove_desert_mother_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (desert_mother)!"
		}
	}
	if = {
		limit = { trait = desert_mother }
		remove_trait = desert_mother
		hidden_tooltip = { character_event = { id = emf_trait.1253 } }
	}
}

#### [254] Doctores: doctores ####
emf_notify_add_doctores_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = doctores }
			log = "WARNING: emf_notify_add_doctores_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (doctores)!"
		}
	}
	if = {
		limit = { NOT = { trait = doctores } }
		add_trait = doctores
		hidden_tooltip = { character_event = { id = emf_trait.254 } }
	}
}
emf_notify_remove_doctores_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = doctores } }
			log = "WARNING: emf_notify_remove_doctores_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (doctores)!"
		}
	}
	if = {
		limit = { trait = doctores }
		remove_trait = doctores
		hidden_tooltip = { character_event = { id = emf_trait.1254 } }
	}
}

#### [255] Ajawid: druze_ajawid ####
emf_notify_add_druze_ajawid_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = druze_ajawid }
			log = "WARNING: emf_notify_add_druze_ajawid_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (druze_ajawid)!"
		}
	}
	if = {
		limit = { NOT = { trait = druze_ajawid } }
		add_trait = druze_ajawid
		hidden_tooltip = { character_event = { id = emf_trait.255 } }
	}
}
emf_notify_remove_druze_ajawid_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = druze_ajawid } }
			log = "WARNING: emf_notify_remove_druze_ajawid_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (druze_ajawid)!"
		}
	}
	if = {
		limit = { trait = druze_ajawid }
		remove_trait = druze_ajawid
		hidden_tooltip = { character_event = { id = emf_trait.1255 } }
	}
}

#### [256] Juhhal: druze_juhhal ####
emf_notify_add_druze_juhhal_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = druze_juhhal }
			log = "WARNING: emf_notify_add_druze_juhhal_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (druze_juhhal)!"
		}
	}
	if = {
		limit = { NOT = { trait = druze_juhhal } }
		add_trait = druze_juhhal
		hidden_tooltip = { character_event = { id = emf_trait.256 } }
	}
}
emf_notify_remove_druze_juhhal_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = druze_juhhal } }
			log = "WARNING: emf_notify_remove_druze_juhhal_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (druze_juhhal)!"
		}
	}
	if = {
		limit = { trait = druze_juhhal }
		remove_trait = druze_juhhal
		hidden_tooltip = { character_event = { id = emf_trait.1256 } }
	}
}

#### [257] Uqqal: druze_uqqal ####
emf_notify_add_druze_uqqal_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = druze_uqqal }
			log = "WARNING: emf_notify_add_druze_uqqal_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (druze_uqqal)!"
		}
	}
	if = {
		limit = { NOT = { trait = druze_uqqal } }
		add_trait = druze_uqqal
		hidden_tooltip = { character_event = { id = emf_trait.257 } }
	}
}
emf_notify_remove_druze_uqqal_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = druze_uqqal } }
			log = "WARNING: emf_notify_remove_druze_uqqal_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (druze_uqqal)!"
		}
	}
	if = {
		limit = { trait = druze_uqqal }
		remove_trait = druze_uqqal
		hidden_tooltip = { character_event = { id = emf_trait.1257 } }
	}
}

#### [258] Diwan Poet: hurufi_diwan ####
emf_notify_add_hurufi_diwan_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = hurufi_diwan }
			log = "WARNING: emf_notify_add_hurufi_diwan_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (hurufi_diwan)!"
		}
	}
	if = {
		limit = { NOT = { trait = hurufi_diwan } }
		add_trait = hurufi_diwan
		hidden_tooltip = { character_event = { id = emf_trait.258 } }
	}
}
emf_notify_remove_hurufi_diwan_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = hurufi_diwan } }
			log = "WARNING: emf_notify_remove_hurufi_diwan_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (hurufi_diwan)!"
		}
	}
	if = {
		limit = { trait = hurufi_diwan }
		remove_trait = hurufi_diwan
		hidden_tooltip = { character_event = { id = emf_trait.1258 } }
	}
}

#### [259] Sufi Initiate: hurufi_mystic_a ####
emf_notify_add_hurufi_mystic_a_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = hurufi_mystic_a }
			log = "WARNING: emf_notify_add_hurufi_mystic_a_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (hurufi_mystic_a)!"
		}
	}
	if = {
		limit = { NOT = { trait = hurufi_mystic_a } }
		add_trait = hurufi_mystic_a
		hidden_tooltip = { character_event = { id = emf_trait.259 } }
	}
}
emf_notify_remove_hurufi_mystic_a_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = hurufi_mystic_a } }
			log = "WARNING: emf_notify_remove_hurufi_mystic_a_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (hurufi_mystic_a)!"
		}
	}
	if = {
		limit = { trait = hurufi_mystic_a }
		remove_trait = hurufi_mystic_a
		hidden_tooltip = { character_event = { id = emf_trait.1259 } }
	}
}

#### [260] Sufi Adept: hurufi_mystic_b ####
emf_notify_add_hurufi_mystic_b_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = hurufi_mystic_b }
			log = "WARNING: emf_notify_add_hurufi_mystic_b_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (hurufi_mystic_b)!"
		}
	}
	if = {
		limit = { NOT = { trait = hurufi_mystic_b } }
		add_trait = hurufi_mystic_b
		hidden_tooltip = { character_event = { id = emf_trait.260 } }
	}
}
emf_notify_remove_hurufi_mystic_b_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = hurufi_mystic_b } }
			log = "WARNING: emf_notify_remove_hurufi_mystic_b_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (hurufi_mystic_b)!"
		}
	}
	if = {
		limit = { trait = hurufi_mystic_b }
		remove_trait = hurufi_mystic_b
		hidden_tooltip = { character_event = { id = emf_trait.1260 } }
	}
}

#### [261] Sufi Master: hurufi_mystic_c ####
emf_notify_add_hurufi_mystic_c_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = hurufi_mystic_c }
			log = "WARNING: emf_notify_add_hurufi_mystic_c_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (hurufi_mystic_c)!"
		}
	}
	if = {
		limit = { NOT = { trait = hurufi_mystic_c } }
		add_trait = hurufi_mystic_c
		hidden_tooltip = { character_event = { id = emf_trait.261 } }
	}
}
emf_notify_remove_hurufi_mystic_c_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = hurufi_mystic_c } }
			log = "WARNING: emf_notify_remove_hurufi_mystic_c_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (hurufi_mystic_c)!"
		}
	}
	if = {
		limit = { trait = hurufi_mystic_c }
		remove_trait = hurufi_mystic_c
		hidden_tooltip = { character_event = { id = emf_trait.1261 } }
	}
}

#### [262] Icon-Breaker: iconoclast_trait ####
emf_notify_add_iconoclast_trait_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = iconoclast_trait }
			log = "WARNING: emf_notify_add_iconoclast_trait_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (iconoclast_trait)!"
		}
	}
	if = {
		limit = { NOT = { trait = iconoclast_trait } }
		add_trait = iconoclast_trait
		hidden_tooltip = { character_event = { id = emf_trait.262 } }
	}
}
emf_notify_remove_iconoclast_trait_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = iconoclast_trait } }
			log = "WARNING: emf_notify_remove_iconoclast_trait_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (iconoclast_trait)!"
		}
	}
	if = {
		limit = { trait = iconoclast_trait }
		remove_trait = iconoclast_trait
		hidden_tooltip = { character_event = { id = emf_trait.1262 } }
	}
}

#### [263] Isma'ili Claimant: imamah_claimant ####
emf_notify_add_imamah_claimant_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = imamah_claimant }
			log = "WARNING: emf_notify_add_imamah_claimant_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (imamah_claimant)!"
		}
	}
	if = {
		limit = { NOT = { trait = imamah_claimant } }
		add_trait = imamah_claimant
		hidden_tooltip = { character_event = { id = emf_trait.263 } }
	}
}
emf_notify_remove_imamah_claimant_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = imamah_claimant } }
			log = "WARNING: emf_notify_remove_imamah_claimant_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (imamah_claimant)!"
		}
	}
	if = {
		limit = { trait = imamah_claimant }
		remove_trait = imamah_claimant
		hidden_tooltip = { character_event = { id = emf_trait.1263 } }
	}
}

#### [264] Kafir: kharijite_kafir ####
emf_notify_add_kharijite_kafir_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = kharijite_kafir }
			log = "WARNING: emf_notify_add_kharijite_kafir_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (kharijite_kafir)!"
		}
	}
	if = {
		limit = { NOT = { trait = kharijite_kafir } }
		add_trait = kharijite_kafir
		hidden_tooltip = { character_event = { id = emf_trait.264 } }
	}
}
emf_notify_remove_kharijite_kafir_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = kharijite_kafir } }
			log = "WARNING: emf_notify_remove_kharijite_kafir_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (kharijite_kafir)!"
		}
	}
	if = {
		limit = { trait = kharijite_kafir }
		remove_trait = kharijite_kafir
		hidden_tooltip = { character_event = { id = emf_trait.1264 } }
	}
}

#### [265] Koasasa: koasasa_taus ####
emf_notify_add_koasasa_taus_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = koasasa_taus }
			log = "WARNING: emf_notify_add_koasasa_taus_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (koasasa_taus)!"
		}
	}
	if = {
		limit = { NOT = { trait = koasasa_taus } }
		add_trait = koasasa_taus
		hidden_tooltip = { character_event = { id = emf_trait.265 } }
	}
}
emf_notify_remove_koasasa_taus_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = koasasa_taus } }
			log = "WARNING: emf_notify_remove_koasasa_taus_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (koasasa_taus)!"
		}
	}
	if = {
		limit = { trait = koasasa_taus }
		remove_trait = koasasa_taus
		hidden_tooltip = { character_event = { id = emf_trait.1265 } }
	}
}

#### [266] Koasasa: koasasa_israfel ####
emf_notify_add_koasasa_israfel_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = koasasa_israfel }
			log = "WARNING: emf_notify_add_koasasa_israfel_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (koasasa_israfel)!"
		}
	}
	if = {
		limit = { NOT = { trait = koasasa_israfel } }
		add_trait = koasasa_israfel
		hidden_tooltip = { character_event = { id = emf_trait.266 } }
	}
}
emf_notify_remove_koasasa_israfel_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = koasasa_israfel } }
			log = "WARNING: emf_notify_remove_koasasa_israfel_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (koasasa_israfel)!"
		}
	}
	if = {
		limit = { trait = koasasa_israfel }
		remove_trait = koasasa_israfel
		hidden_tooltip = { character_event = { id = emf_trait.1266 } }
	}
}

#### [267] Koasasa: koasasa_azrael ####
emf_notify_add_koasasa_azrael_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = koasasa_azrael }
			log = "WARNING: emf_notify_add_koasasa_azrael_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (koasasa_azrael)!"
		}
	}
	if = {
		limit = { NOT = { trait = koasasa_azrael } }
		add_trait = koasasa_azrael
		hidden_tooltip = { character_event = { id = emf_trait.267 } }
	}
}
emf_notify_remove_koasasa_azrael_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = koasasa_azrael } }
			log = "WARNING: emf_notify_remove_koasasa_azrael_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (koasasa_azrael)!"
		}
	}
	if = {
		limit = { trait = koasasa_azrael }
		remove_trait = koasasa_azrael
		hidden_tooltip = { character_event = { id = emf_trait.1267 } }
	}
}

#### [268] Koasasa: koasasa_mihael ####
emf_notify_add_koasasa_mihael_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = koasasa_mihael }
			log = "WARNING: emf_notify_add_koasasa_mihael_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (koasasa_mihael)!"
		}
	}
	if = {
		limit = { NOT = { trait = koasasa_mihael } }
		add_trait = koasasa_mihael
		hidden_tooltip = { character_event = { id = emf_trait.268 } }
	}
}
emf_notify_remove_koasasa_mihael_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = koasasa_mihael } }
			log = "WARNING: emf_notify_remove_koasasa_mihael_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (koasasa_mihael)!"
		}
	}
	if = {
		limit = { trait = koasasa_mihael }
		remove_trait = koasasa_mihael
		hidden_tooltip = { character_event = { id = emf_trait.1268 } }
	}
}

#### [269] Koasasa: koasasa_dardael ####
emf_notify_add_koasasa_dardael_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = koasasa_dardael }
			log = "WARNING: emf_notify_add_koasasa_dardael_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (koasasa_dardael)!"
		}
	}
	if = {
		limit = { NOT = { trait = koasasa_dardael } }
		add_trait = koasasa_dardael
		hidden_tooltip = { character_event = { id = emf_trait.269 } }
	}
}
emf_notify_remove_koasasa_dardael_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = koasasa_dardael } }
			log = "WARNING: emf_notify_remove_koasasa_dardael_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (koasasa_dardael)!"
		}
	}
	if = {
		limit = { trait = koasasa_dardael }
		remove_trait = koasasa_dardael
		hidden_tooltip = { character_event = { id = emf_trait.1269 } }
	}
}

#### [270] Koasasa: koasasa_samael ####
emf_notify_add_koasasa_samael_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = koasasa_samael }
			log = "WARNING: emf_notify_add_koasasa_samael_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (koasasa_samael)!"
		}
	}
	if = {
		limit = { NOT = { trait = koasasa_samael } }
		add_trait = koasasa_samael
		hidden_tooltip = { character_event = { id = emf_trait.270 } }
	}
}
emf_notify_remove_koasasa_samael_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = koasasa_samael } }
			log = "WARNING: emf_notify_remove_koasasa_samael_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (koasasa_samael)!"
		}
	}
	if = {
		limit = { trait = koasasa_samael }
		remove_trait = koasasa_samael
		hidden_tooltip = { character_event = { id = emf_trait.1270 } }
	}
}

#### [271] Koasasa: koasasa_nurael ####
emf_notify_add_koasasa_nurael_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = koasasa_nurael }
			log = "WARNING: emf_notify_add_koasasa_nurael_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (koasasa_nurael)!"
		}
	}
	if = {
		limit = { NOT = { trait = koasasa_nurael } }
		add_trait = koasasa_nurael
		hidden_tooltip = { character_event = { id = emf_trait.271 } }
	}
}
emf_notify_remove_koasasa_nurael_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = koasasa_nurael } }
			log = "WARNING: emf_notify_remove_koasasa_nurael_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (koasasa_nurael)!"
		}
	}
	if = {
		limit = { trait = koasasa_nurael }
		remove_trait = koasasa_nurael
		hidden_tooltip = { character_event = { id = emf_trait.1271 } }
	}
}

#### [272] Perfection: messalian_master ####
emf_notify_add_messalian_master_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = messalian_master }
			log = "WARNING: emf_notify_add_messalian_master_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (messalian_master)!"
		}
	}
	if = {
		limit = { NOT = { trait = messalian_master } }
		add_trait = messalian_master
		hidden_tooltip = { character_event = { id = emf_trait.272 } }
	}
}
emf_notify_remove_messalian_master_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = messalian_master } }
			log = "WARNING: emf_notify_remove_messalian_master_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (messalian_master)!"
		}
	}
	if = {
		limit = { trait = messalian_master }
		remove_trait = messalian_master
		hidden_tooltip = { character_event = { id = emf_trait.1272 } }
	}
}

#### [273] Novellani: novellani ####
emf_notify_add_novellani_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = novellani }
			log = "WARNING: emf_notify_add_novellani_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (novellani)!"
		}
	}
	if = {
		limit = { NOT = { trait = novellani } }
		add_trait = novellani
		hidden_tooltip = { character_event = { id = emf_trait.273 } }
	}
}
emf_notify_remove_novellani_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = novellani } }
			log = "WARNING: emf_notify_remove_novellani_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (novellani)!"
		}
	}
	if = {
		limit = { trait = novellani }
		remove_trait = novellani
		hidden_tooltip = { character_event = { id = emf_trait.1273 } }
	}
}

#### [274] Perfecti: perfecti ####
emf_notify_add_perfecti_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = perfecti }
			log = "WARNING: emf_notify_add_perfecti_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (perfecti)!"
		}
	}
	if = {
		limit = { NOT = { trait = perfecti } }
		add_trait = perfecti
		hidden_tooltip = { character_event = { id = emf_trait.274 } }
	}
}
emf_notify_remove_perfecti_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = perfecti } }
			log = "WARNING: emf_notify_remove_perfecti_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (perfecti)!"
		}
	}
	if = {
		limit = { trait = perfecti }
		remove_trait = perfecti
		hidden_tooltip = { character_event = { id = emf_trait.1274 } }
	}
}

#### [275] Has Been Reincarnated: reincarnated ####
emf_notify_add_reincarnated_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = reincarnated }
			log = "WARNING: emf_notify_add_reincarnated_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (reincarnated)!"
		}
	}
	if = {
		limit = { NOT = { trait = reincarnated } }
		add_trait = reincarnated
		hidden_tooltip = { character_event = { id = emf_trait.275 } }
	}
}
emf_notify_remove_reincarnated_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = reincarnated } }
			log = "WARNING: emf_notify_remove_reincarnated_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (reincarnated)!"
		}
	}
	if = {
		limit = { trait = reincarnated }
		remove_trait = reincarnated
		hidden_tooltip = { character_event = { id = emf_trait.1275 } }
	}
}

#### [276] Saint: saint ####
emf_notify_add_saint_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = saint }
			log = "WARNING: emf_notify_add_saint_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (saint)!"
		}
	}
	if = {
		limit = { NOT = { trait = saint } }
		add_trait = saint
		hidden_tooltip = { character_event = { id = emf_trait.276 } }
	}
}
emf_notify_remove_saint_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = saint } }
			log = "WARNING: emf_notify_remove_saint_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (saint)!"
		}
	}
	if = {
		limit = { trait = saint }
		remove_trait = saint
		hidden_tooltip = { character_event = { id = emf_trait.1276 } }
	}
}

#### [277] Sandaliati: sandaliati ####
emf_notify_add_sandaliati_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = sandaliati }
			log = "WARNING: emf_notify_add_sandaliati_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (sandaliati)!"
		}
	}
	if = {
		limit = { NOT = { trait = sandaliati } }
		add_trait = sandaliati
		hidden_tooltip = { character_event = { id = emf_trait.277 } }
	}
}
emf_notify_remove_sandaliati_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = sandaliati } }
			log = "WARNING: emf_notify_remove_sandaliati_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (sandaliati)!"
		}
	}
	if = {
		limit = { trait = sandaliati }
		remove_trait = sandaliati
		hidden_tooltip = { character_event = { id = emf_trait.1277 } }
	}
}

#### [278] Yazidi Pilgrim: yazidi_pilgrim ####
emf_notify_add_yazidi_pilgrim_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = yazidi_pilgrim }
			log = "WARNING: emf_notify_add_yazidi_pilgrim_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (yazidi_pilgrim)!"
		}
	}
	if = {
		limit = { NOT = { trait = yazidi_pilgrim } }
		add_trait = yazidi_pilgrim
		hidden_tooltip = { character_event = { id = emf_trait.278 } }
	}
}
emf_notify_remove_yazidi_pilgrim_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = yazidi_pilgrim } }
			log = "WARNING: emf_notify_remove_yazidi_pilgrim_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (yazidi_pilgrim)!"
		}
	}
	if = {
		limit = { trait = yazidi_pilgrim }
		remove_trait = yazidi_pilgrim
		hidden_tooltip = { character_event = { id = emf_trait.1278 } }
	}
}

#### [279] Crowned: crowned ####
emf_notify_add_crowned_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = crowned }
			log = "WARNING: emf_notify_add_crowned_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (crowned)!"
		}
	}
	if = {
		limit = { NOT = { trait = crowned } }
		add_trait = crowned
		hidden_tooltip = { character_event = { id = emf_trait.279 } }
	}
}
emf_notify_remove_crowned_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = crowned } }
			log = "WARNING: emf_notify_remove_crowned_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (crowned)!"
		}
	}
	if = {
		limit = { trait = crowned }
		remove_trait = crowned
		hidden_tooltip = { character_event = { id = emf_trait.1279 } }
	}
}

#### [280] Crowned By The Pope: crowned_by_pope ####
emf_notify_add_crowned_by_pope_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = crowned_by_pope }
			log = "WARNING: emf_notify_add_crowned_by_pope_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (crowned_by_pope)!"
		}
	}
	if = {
		limit = { NOT = { trait = crowned_by_pope } }
		add_trait = crowned_by_pope
		hidden_tooltip = { character_event = { id = emf_trait.280 } }
	}
}
emf_notify_remove_crowned_by_pope_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = crowned_by_pope } }
			log = "WARNING: emf_notify_remove_crowned_by_pope_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (crowned_by_pope)!"
		}
	}
	if = {
		limit = { trait = crowned_by_pope }
		remove_trait = crowned_by_pope
		hidden_tooltip = { character_event = { id = emf_trait.1280 } }
	}
}

#### [281] Strong Basileus: strong_basileus ####
emf_notify_add_strong_basileus_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = strong_basileus }
			log = "WARNING: emf_notify_add_strong_basileus_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (strong_basileus)!"
		}
	}
	if = {
		limit = { NOT = { trait = strong_basileus } }
		add_trait = strong_basileus
		hidden_tooltip = { character_event = { id = emf_trait.281 } }
	}
}
emf_notify_remove_strong_basileus_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = strong_basileus } }
			log = "WARNING: emf_notify_remove_strong_basileus_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (strong_basileus)!"
		}
	}
	if = {
		limit = { trait = strong_basileus }
		remove_trait = strong_basileus
		hidden_tooltip = { character_event = { id = emf_trait.1281 } }
	}
}

#### [282] Uncrowned: uncrowned ####
emf_notify_add_uncrowned_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = uncrowned }
			log = "WARNING: emf_notify_add_uncrowned_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (uncrowned)!"
		}
	}
	if = {
		limit = { NOT = { trait = uncrowned } }
		add_trait = uncrowned
		hidden_tooltip = { character_event = { id = emf_trait.282 } }
	}
}
emf_notify_remove_uncrowned_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = uncrowned } }
			log = "WARNING: emf_notify_remove_uncrowned_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (uncrowned)!"
		}
	}
	if = {
		limit = { trait = uncrowned }
		remove_trait = uncrowned
		hidden_tooltip = { character_event = { id = emf_trait.1282 } }
	}
}

#### [283] Coronated Basileus: weak_basileus ####
emf_notify_add_weak_basileus_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = weak_basileus }
			log = "WARNING: emf_notify_add_weak_basileus_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (weak_basileus)!"
		}
	}
	if = {
		limit = { NOT = { trait = weak_basileus } }
		add_trait = weak_basileus
		hidden_tooltip = { character_event = { id = emf_trait.283 } }
	}
}
emf_notify_remove_weak_basileus_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = weak_basileus } }
			log = "WARNING: emf_notify_remove_weak_basileus_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (weak_basileus)!"
		}
	}
	if = {
		limit = { trait = weak_basileus }
		remove_trait = weak_basileus
		hidden_tooltip = { character_event = { id = emf_trait.1283 } }
	}
}

#### [284] Martyr: martyr ####
emf_notify_add_martyr_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = martyr }
			log = "WARNING: emf_notify_add_martyr_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (martyr)!"
		}
	}
	if = {
		limit = { NOT = { trait = martyr } }
		add_trait = martyr
		hidden_tooltip = { character_event = { id = emf_trait.284 } }
	}
}
emf_notify_remove_martyr_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = martyr } }
			log = "WARNING: emf_notify_remove_martyr_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (martyr)!"
		}
	}
	if = {
		limit = { trait = martyr }
		remove_trait = martyr
		hidden_tooltip = { character_event = { id = emf_trait.1284 } }
	}
}

#### [285] Paragon: paragon ####
emf_notify_add_paragon_effect = {
	hidden_tooltip = {
		if = {
			limit = { trait = paragon }
			log = "WARNING: emf_notify_add_paragon_effect: [This.GetBestName] ([This.GetID]) already has the trait to be added (paragon)!"
		}
	}
	if = {
		limit = { NOT = { trait = paragon } }
		add_trait = paragon
		hidden_tooltip = { character_event = { id = emf_trait.285 } }
	}
}
emf_notify_remove_paragon_effect = {
	hidden_tooltip = {
		if = {
			limit = { NOT = { trait = paragon } }
			log = "WARNING: emf_notify_remove_paragon_effect: [This.GetBestName] ([This.GetID]) doesn't have the trait to be removed (paragon)!"
		}
	}
	if = {
		limit = { trait = paragon }
		remove_trait = paragon
		hidden_tooltip = { character_event = { id = emf_trait.1285 } }
	}
}
