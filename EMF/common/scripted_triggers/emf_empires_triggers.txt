
# THIS = scope to consider for being same/related religion
emf_empires_no_empire_same_religion_trigger = {
	custom_tooltip = {
		text = emf_empires_ctt_no_empire_same_religion_trigger
		hidden_tooltip = {
			NOT = {
				any_independent_ruler = {
					tier = EMPEROR
					is_nomadic = no
					OR = {
						religion = PREV
						is_parent_religion = PREV
						is_heresy_of = PREV
					}
				}
			}
		}
	}
}

emf_empires_mongol_core_prov_trigger = {
	owner = { NOT = { same_realm = ROOT } } # Border province
	region = emf_region_mongol_core
	owner = {
		top_liege = {
			NOR = {
				# Respect truce
				reverse_has_truce = ROOT
				# Respect suzerainty
				ROOT = { pays_tribute_to = PREV }
				# Respect alliances
				is_allied_with = ROOT
				# Respect NAPs
				has_non_aggression_pact_with = ROOT
			}
		}
	}
}

emf_empires_mongol_extended_prov_trigger = {
	owner = { NOT = { same_realm = ROOT } } # Border province
	region = emf_region_mongol_extended
	owner = {
		top_liege = {
			NOR = {
				# Respect truce
				reverse_has_truce = ROOT
				# Respect suzerainty
				ROOT = { pays_tribute_to = PREV }
				# Respect alliances
				is_allied_with = ROOT
				# Respect NAPs
				has_non_aggression_pact_with = ROOT
			}
		}
	}
}
