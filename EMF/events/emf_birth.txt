# -*- ck2.events -*-

namespace = emf_birth

# emf_birth.0
# Fixes the bug where non-bastard children would only inherit stats from their mothers and none from their (real) fathers
character_event = {
	id = emf_birth.0
	desc = HIDE_EVENT
	hide_window = yes
	is_triggered_only = yes
	
	trigger = {
		is_father_real_father = yes
	}
	
	immediate = {
		set_real_father = father
	}
}
